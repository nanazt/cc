---
phase: quick
plan: 260402-rc9
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/case/step-discuss.md
  - agents/case-briefer.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Neither file contains architecture-specific terms that assume the host project is a service"
    - "Replacement terms are accurate for ALL project types: web services, CLI tools, libraries, embedded systems"
    - "Surrounding content unchanged — surgical edits only"
  artifacts:
    - path: "skills/case/step-discuss.md"
      provides: "Infrastructure failure probe template"
      contains: "External dependency"
    - path: "agents/case-briefer.md"
      provides: "Operation discovery patterns"
      contains: "Interface contract"
  key_links: []
---

<objective>
Fix 2 remaining technology bias instances in the /case skill ecosystem.

Purpose: cckit artifacts must be project-type agnostic. Two terms currently assume the host project uses service-oriented architecture, which excludes CLI tools, libraries, embedded systems, and non-service projects.
Output: Two files with neutral terminology that accurately communicates the same concepts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@skills/case/step-discuss.md
@agents/case-briefer.md
@CLAUDE.md (Technology Neutrality section, Standard Vocabulary)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix technology-biased terms in step-discuss.md and case-briefer.md</name>
  <files>skills/case/step-discuss.md, agents/case-briefer.md</files>
  <action>
Two surgical single-line edits:

**Edit 1 — skills/case/step-discuss.md line 234:**

Current:
```
- Downstream service timeout -> [specific status] (ErrorName)
```

Replace with:
```
- External dependency timeout -> [specific status] (ErrorName)
```

Rationale: The concept is "something this code depends on, outside its control, becomes unresponsive." For a web service that's a downstream service; for a CLI tool it's a network call or subprocess; for a library it's any I/O dependency. "External dependency" captures all of these without assuming service architecture. It pairs naturally with the existing "Database unavailable" line above it — database is one external dependency, this line covers all others.

**Edit 2 — agents/case-briefer.md line 60:**

Current:
```
- Service contract definitions (RPC methods, message handlers, etc.)
```

Replace with:
```
- Interface contract definitions (exported functions, protocol methods, handler registrations, etc.)
```

Rationale: The concept is "formally declared callable interfaces with defined signatures." "Service contract" assumes service-oriented architecture. "Interface contract" is the universal term — it covers REST routes, RPC methods, CLI command handlers, library exports, event listeners, and any other formally defined calling convention. The parenthetical examples are broadened from service-specific (RPC methods, message handlers) to universal patterns that span project types.

**Do NOT change anything else in either file.**
  </action>
  <verify>
    <automated>grep -n "Downstream service\|Service contract" skills/case/step-discuss.md agents/case-briefer.md; echo "EXIT: $?"</automated>
  </verify>
  <done>Both grep matches return zero results. "External dependency timeout" appears at step-discuss.md line 234. "Interface contract definitions" appears at case-briefer.md line 60. No other lines changed.</done>
</task>

</tasks>

<verification>
1. `grep -rn "Downstream service\|Service contract" skills/ agents/` returns no matches
2. `grep -n "External dependency timeout" skills/case/step-discuss.md` returns exactly 1 match
3. `grep -n "Interface contract definitions" agents/case-briefer.md` returns exactly 1 match
4. `git diff --stat` shows exactly 2 files changed, 1 insertion + 1 deletion each
</verification>

<success_criteria>
- Zero instances of "Downstream service" or "Service contract" in skills/ and agents/
- Replacement terms accurately describe the same concepts for any project type
- No unrelated changes in either file
</success_criteria>

<output>
After completion, create `.planning/quick/260402-rc9-fix-remaining-technology-bias-in-case-sk/260402-rc9-SUMMARY.md`
</output>
