---
phase: 11-consolidation-pipeline
plan: "03"
type: execute
wave: 3
depends_on:
  - "11-01"
  - "11-02"
files_modified:
  - docs/IMPL-SPEC.md
autonomous: true
requirements:
  - PIPE-06

must_haves:
  truths:
    - "IMPL-SPEC.md documents the schema-driven pipeline with no archetype references"
    - "IMPL-SPEC.md agent contracts match the actual spec-consolidator.md and e2e-flows.md dispatch tags"
    - "IMPL-SPEC.md uses Component/CR/schema terminology throughout, not Service/SR/archetype"
    - "IMPL-SPEC.md preserves the hash tool section unchanged (already universal)"
  artifacts:
    - path: "docs/IMPL-SPEC.md"
      provides: "Universal design documentation reflecting v2 pipeline"
      contains: "schema-parser.ts"
  key_links:
    - from: "docs/IMPL-SPEC.md"
      to: "skills/consolidate/SKILL.md"
      via: "Documents pipeline steps matching actual SKILL.md"
      pattern: "schema-parser\\.ts"
    - from: "docs/IMPL-SPEC.md"
      to: "agents/spec-consolidator.md"
      via: "Documents agent contract matching actual agent"
      pattern: "spec-consolidator"
    - from: "docs/IMPL-SPEC.md"
      to: "agents/e2e-flows.md"
      via: "Documents agent contract matching actual agent"
      pattern: "e2e-flows"
---

<objective>
Rewrite docs/IMPL-SPEC.md to reflect the universal v2 design. Remove all archetype references, document the schema-driven pipeline, and update all agent contracts and terminology to match the actual deliverables from Plans 01 and 02.

Purpose: PIPE-06 requires IMPL-SPEC to be fully rewritten for universal design. This document serves as reference during the transition period (Phases 11-14) and is deleted after Phase 14. It must accurately describe the actual system, not a planned system.

Output: `docs/IMPL-SPEC.md` -- rewritten to reflect universal design. File remains in docs/ per D-03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-consolidation-pipeline/11-CONTEXT.md
@.planning/phases/11-consolidation-pipeline/11-01-SUMMARY.md
@.planning/phases/11-consolidation-pipeline/11-02-SUMMARY.md

@docs/IMPL-SPEC.md (current v1 content -- rewrite target)
@skills/consolidate/SKILL.md (v2 orchestrator -- source of truth for pipeline)
@agents/spec-consolidator.md (v2 agent -- source of truth for consolidator contract)
@agents/e2e-flows.md (v2 agent -- source of truth for E2E contract)
@docs/MODEL.md (authoritative v2 model)
@tools/schema-parser.ts (lines 26-68 -- interface definitions)

<interfaces>
<!-- The actual deliverables from Plans 01 and 02 are the source of truth.
     IMPL-SPEC must describe what exists, not what was planned. -->

Key references to extract from actual files:
- SKILL.md: frontmatter, pipeline steps, dispatch tag construction
- spec-consolidator.md: frontmatter, input contract, merge rules, return protocol
- e2e-flows.md: frontmatter, input contract, flow format, return protocol
- schema-parser.ts: SchemaOutput interface (meta, components, sections)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite docs/IMPL-SPEC.md for universal v2 design</name>
  <files>docs/IMPL-SPEC.md</files>
  <read_first>
    - docs/IMPL-SPEC.md (full file -- understand current structure for rewrite)
    - skills/consolidate/SKILL.md (full file -- actual v2 orchestrator, source of truth for pipeline documentation)
    - agents/spec-consolidator.md (full file -- actual v2 agent, source of truth for consolidator contract)
    - agents/e2e-flows.md (full file -- actual v2 agent, source of truth for E2E contract)
    - docs/MODEL.md (lines 1-70 -- terminology, section structure)
    - tools/schema-parser.ts (lines 26-68 -- interface definitions for schema output)
    - .planning/phases/11-consolidation-pipeline/11-CONTEXT.md (D-01 through D-03 -- IMPL-SPEC handling decisions)
  </read_first>
  <action>
Rewrite docs/IMPL-SPEC.md. This is a complete rewrite, not a surgical edit. The new document reflects the actual v2 system as built in Plans 01 and 02.

**Structural approach:**
1. Read all three deliverables (SKILL.md, spec-consolidator.md, e2e-flows.md) to understand what was actually built
2. Read the current IMPL-SPEC.md to understand the document structure
3. Rewrite each section to match the actual v2 system

**Sections to rewrite:**

**## Overview**
Update to describe v2: schema-driven dispatch, per-component spec files, universal model. Remove all "per-service" language. Update file inventory to match actual v2 structure:

| # | Path | Purpose |
|---|------|---------|
| 1 | `skills/consolidate/SKILL.md` | Orchestrator skill (7+2 step schema-driven pipeline) |
| 2 | `agents/spec-consolidator.md` | Per-component consolidation agent (sonnet) |
| 3 | `agents/e2e-flows.md` | Cross-component E2E flow agent (sonnet) |
| 4 | `agents/spec-verifier.md` | Verification agent (opus, read-only) -- Phase 13 |
| 5 | `tools/hash-sections.ts` | Deno SHA-256 section hashing |
| 6 | `tools/hash-sections_test.ts` | Hash tool tests |
| 7 | `tools/schema-parser.ts` | Deno schema file parser |
| 8 | `tools/schema-parser_test.ts` | Schema parser tests |
| 9 | `tools/schema-bootstrap.ts` | Deno starter schema generator |
| 10 | `tools/schema-bootstrap_test.ts` | Bootstrap tool tests |

Remove template file entries (7/8/9 from v1 -- templates eliminated in v2).

Output location update:
```
.planning/specs/
  {component}/
    context.md       -- design decisions, architecture, configuration
    cases.md         -- rules, S/F/E tables, error categories
  e2e/
    {flow-name}.md   -- per-user-action cross-component flows (opt-in)
  INDEX.md           -- component table + E2E table + operation index
```

**## Hash Tool**
Preserve this section UNCHANGED. The hash tool is already universal (from Phase 1). Do not modify section content, test case descriptions, algorithm steps, or invocation patterns.

**## Templates -> REMOVE ENTIRELY**
Delete the entire Templates section (three archetypes, domain-service, gateway-bff, event-driven, v1-to-v2 section renames). These are replaced by the schema system. Add a brief note: "Section structure is defined in the project's consolidation schema file. See docs/MODEL.md for the default section structure and override mechanism."

**## Agent: spec-consolidator**
Rewrite to match the actual agents/spec-consolidator.md:
- Frontmatter: copy from actual file
- Input Contract: document dispatch tags as actually implemented (`<component>`, `<sections>`, `<conditional_sections>`, `<rule_prefix>` -- NOT `<service>`, `<template_type>`)
- Merge Rules: all 11 rules with v2 terminology (Component.Op, PR->CR promotion, schema-defined sections)
- Return Protocol: match actual agent's return format
- Quality Gate: match actual agent's checklist

**## Agent: e2e-flows**
Rewrite to match the actual agents/e2e-flows.md:
- Frontmatter: copy from actual file
- Input Contract: `<changed_components>` (not `<changed_services>`), all other tags
- Flow Format: with Component terminology in Spec References table
- Return Protocol: match actual agent's format
- Quality Gate: match actual agent's checklist

**## Agent: spec-verifier**
Keep this section largely intact but update terminology:
- Replace "service" with "component" in descriptions
- Replace "SR" with "CR" in rule references
- Replace "archetype" with "schema-defined" in check descriptions
- Note that the verifier is a Phase 13 deliverable
- Update V-04 description: "Schema-defined sections present" (not "Archetype-appropriate sections")
- Update V-27 description: "specs/ component list matches schema registry" (not "PROJECT.md service topology")
- Keep verification check IDs (V-01 through V-29) stable for Phase 13 traceability

**## SKILL.md -- Orchestrator**
Rewrite to match actual v2 SKILL.md:
- Frontmatter: copy from actual file
- Pipeline steps: document all 7+2 steps as actually implemented
- Step 1: schema-parser.ts invocation, component discovery, schema cross-reference
- Step 2: dispatch with v2 tags
- Step 3: INDEX.md v2 format (Component/Type columns)
- Step 3.5: E2E conditional on meta.e2eFlows
- Step 3.7: orphan cleanup with schema removal suggestion
- Step 4: E2E agent dispatch (conditional)
- Step 5: verifier skip branch
- Step 6: v2 confirmation summary format
- Step 7: commit/rollback

**## INDEX.md v2 Format**
Update to use Component/Type columns (per D-11):
```markdown
## Components

| Component | Type | Description | Files | Last Consolidated |
|-----------|------|-------------|-------|-------------------|
```
Remove "Service"/"Archetype" columns.

**## Rule Tier System**
Update:
- SR (Service Rules) -> CR (Component Rules) throughout
- Location: `specs/{component}/cases.md` (not `specs/{service}/cases.md`)
- PR promotes to CR (not SR)
- Update the "What the Consolidator Does" table accordingly

**## Verification Checks (28)**
Update terminology only -- keep check IDs stable:
- V-04: "Schema-defined sections present" (not "Archetype-appropriate sections")
- V-10: Cross-component operation references (not cross-service)
- V-11: Component routes resolve (not gateway-specific if possible, or note as conditional)
- V-15: Error categories consistent across components (not services)
- V-27: specs/ component list matches schema registry (not PROJECT.md service topology)
- V-29: E2E Spec References validity (terminology update only)

**## Consumer Updates**
Update case-briefer section: `specs/{component}/cases.md` (not `specs/{service}/cases.md`). Update case-validator section with v2 terminology.

**## Backfill Strategy**
Rewrite as project-neutral guidance per D-19. Remove all madome-specific content (Phase 1/2/3A references, auth/user/gateway examples). Keep the general backfill principle: when creating a new spec for a component, check if earlier phases had decisions about it.

**## Resolved Review Items**
Keep items that are still relevant, update terminology. Remove items that reference v1-specific concerns no longer applicable (e.g., C4 archetype selection).

**## Open Questions**
Update: remove resolved questions, keep genuinely open ones (maxTurns calibration, spec-verifier cost). Remove archetype-specific questions (event-driven archetype validation, proto/common services).

**Final terminology sweep:**
After rewriting, search the entire document for remaining v1 terms:
- "service" (except in "cross-service" which becomes "cross-component", and hash tool section which is preserved)
- "archetype" (should be zero except hash tool section)
- "template_type" (should be zero)
- "domain-service", "gateway-bff", "event-driven" (should be zero except hash tool section)
- "SR-" in consolidation context (should be "CR-")
- "SvcR" (should be zero)
  </action>
  <verify>
    <automated>grep -c "archetype" docs/IMPL-SPEC.md | xargs test 0 -eq && grep -c "template_type" docs/IMPL-SPEC.md | xargs test 0 -eq && grep -c "gateway-bff" docs/IMPL-SPEC.md | xargs test 0 -eq && grep -q "schema-parser.ts" docs/IMPL-SPEC.md && grep -q "Component" docs/IMPL-SPEC.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/IMPL-SPEC.md exists and has been rewritten (different content from v1)
    - grep -c "archetype" docs/IMPL-SPEC.md returns 0
    - grep -c "template_type" docs/IMPL-SPEC.md returns 0
    - grep -c "domain-service" docs/IMPL-SPEC.md returns 0
    - grep -c "gateway-bff" docs/IMPL-SPEC.md returns 0
    - grep -c "event-driven" docs/IMPL-SPEC.md returns 0
    - grep -c "schema-parser.ts" docs/IMPL-SPEC.md returns >= 1
    - grep -c "schema-bootstrap.ts" docs/IMPL-SPEC.md returns >= 1
    - Overview section mentions "schema-driven" and "per-component"
    - Hash Tool section is preserved unchanged from v1
    - Templates section is removed (no "Three Archetypes" heading)
    - Agent: spec-consolidator section uses `<component>` tag (not `<service>`)
    - Agent: spec-consolidator section uses `<sections>` tag (not `<template_type>`)
    - Agent: e2e-flows section uses `<changed_components>` (not `<changed_services>`)
    - Rule Tier System uses CR (not SR in consolidation context)
    - INDEX.md format shows "Component" and "Type" columns
    - Verification checks V-01 through V-29 all present (IDs stable)
    - Backfill Strategy section contains no project-specific references (no "madome", "auth", "gateway", "Phase 1/2/3A" backfill plan)
    - deno test --allow-read tools/ passes (existing tools not broken -- sanity check)
  </acceptance_criteria>
  <done>IMPL-SPEC.md fully reflects the universal v2 design: schema-driven pipeline documented, all agent contracts match actual deliverables, no archetype references, hash tool section preserved, verification check IDs stable for Phase 13.</done>
</task>

</tasks>

<verification>
1. `grep -c "archetype" docs/IMPL-SPEC.md` returns 0
2. `grep -c "template_type" docs/IMPL-SPEC.md` returns 0
3. `grep -c "domain-service\|gateway-bff\|event-driven" docs/IMPL-SPEC.md` returns 0
4. `grep -c "schema-parser.ts" docs/IMPL-SPEC.md` returns >= 1
5. `grep -c "<component>" docs/IMPL-SPEC.md` returns >= 1 (in agent contract)
6. `grep -c "<sections>" docs/IMPL-SPEC.md` returns >= 1 (in agent contract)
7. Hash tool section content preserved (spot check: `grep "SHA-256" docs/IMPL-SPEC.md` returns >= 1)
8. Verification check IDs present: `grep -c "V-01\|V-14\|V-28\|V-29" docs/IMPL-SPEC.md` returns >= 4
9. `deno test --allow-read tools/` passes
</verification>

<success_criteria>
IMPL-SPEC.md is a complete v2 reference document:
- Zero archetype references
- Schema-driven pipeline documented throughout
- Agent contracts match actual spec-consolidator.md and e2e-flows.md
- Hash tool section preserved unchanged
- Component/CR terminology throughout
- Verification check IDs stable (V-01 through V-29) for Phase 13 traceability
- Backfill strategy is project-neutral
</success_criteria>

<output>
After completion, create `.planning/phases/11-consolidation-pipeline/11-03-SUMMARY.md`
</output>
