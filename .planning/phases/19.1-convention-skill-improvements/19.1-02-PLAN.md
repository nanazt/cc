---
phase: 19.1-convention-skill-improvements
plan: 02
type: execute
wave: 2
depends_on: [19.1-01]
files_modified:
  - agents/convention-researcher.md
  - skills/convention/step-research.md
  - skills/convention/step-preferences.md
autonomous: true
requirements: [CONV-02, CONV-03, CONV-04]

must_haves:
  truths:
    - "Researcher agent returns structured delta tags [DELTA:new/known/varies] and [HOOK:yes/no] on every practice"
    - "step-research writes research output to .state/research.md"
    - "step-preferences includes hook confirmation question derived from researcher recommendation"
    - "step-preferences writes preferences to .state/preferences.json including hook_confirmed field"
    - "convention_tools from .state/init.json are passed to researcher agent prompt"
  artifacts:
    - path: "agents/convention-researcher.md"
      provides: "Updated researcher with delta classification tags and hook recommendation"
      contains: "[DELTA:new]"
    - path: "skills/convention/step-research.md"
      provides: "Updated research step with .state/ writes and convention_tools pass-through"
      contains: ".state/research.md"
    - path: "skills/convention/step-preferences.md"
      provides: "Updated preferences step with hook confirmation and .state/ writes"
      contains: "hook_confirmed"
  key_links:
    - from: "agents/convention-researcher.md"
      to: "skills/convention/step-research.md"
      via: "Agent dispatch and return parsing"
      pattern: "DELTA"
    - from: "skills/convention/step-research.md"
      to: ".state/research.md"
      via: "Write tool"
      pattern: "\\.state/research\\.md"
    - from: "skills/convention/step-preferences.md"
      to: ".state/preferences.json"
      via: "Write tool"
      pattern: "\\.state/preferences\\.json"
---

<objective>
Update the research and preferences pipeline: modify the convention-researcher agent to return structured delta classification tags and hook recommendations, update step-research to write output to .state/ and pass convention_tools, and update step-preferences to include hook confirmation and write preferences to .state/.

Purpose: The structured delta transfer format (D-11/D-12/D-13) replaces free-text interpretation, preserving researcher classification through to the generator. Hook confirmation in preferences (D-10) enables step-generate to know whether to produce hooks. .state/ persistence (D-14) eliminates implicit context dependency between steps.
Output: Updated convention-researcher.md, step-research.md, step-preferences.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md
@.planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md
@.planning/phases/19.1-convention-skill-improvements/19.1-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: step-init now creates .state/ directory and writes init.json -->
<!-- .state/ location: publisher = conventions/{area}/.state/, consumer = .convention-state/{area}/ -->
<!-- init.json contains: area, lang, publisher, convention_tools, resolved_path, mode, artifact_type, selected_flow, create_base_first -->
<!-- step-init passes forward: area, lang, publisher, convention_tools, resolved_path, mode, selected_flow, create_base_first, artifact_type, state_dir -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update convention-researcher agent with structured delta tags and hook recommendation</name>
  <files>agents/convention-researcher.md</files>
  <read_first>
    - agents/convention-researcher.md (current agent prompt -- full file)
    - .planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md (D-03, D-10, D-11, D-12, D-13)
    - .planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md (delta transfer format section, rule vs skill judgment criteria section)
  </read_first>
  <action>
Modify agents/convention-researcher.md with these specific changes:

**Input Contract table:**
Add a new row:
```
| `<convention_tools>` | No | Additional MCP tool names from cckit.json for enhanced research capability |
```

**Step 4: Identify Delta Test Candidates -- REPLACE the entire section with:**

```markdown
### Step 4: Classify Practices with Structured Tags

For each discovered best practice, assign two tags inline with the practice name:

**Delta Classification Tag** `[DELTA:class]` -- one of three classes:
- `[DELTA:new]` -- Claude would NOT follow this by default without explicit instruction. Must be taught.
- `[DELTA:known]` -- Claude reliably follows this already in every session. Including this rule adds noise.
- `[DELTA:varies]` -- Claude follows this inconsistently across sessions. Worth codifying for consistency.

**Hook Classification Tag** `[HOOK:class]` -- one of two classes:
- `[HOOK:yes]` -- This rule is mechanically verifiable. A script can check compliance with regex, string matching, or structural analysis. Examples: commit format patterns, naming conventions, required sections.
- `[HOOK:no]` -- This rule requires human or AI judgment to verify. A script cannot reliably check compliance. Examples: "explain why in body", "use imperative mood" (ambiguous detection), quality judgments.

Both tags appear on the same line as the practice name heading, enclosed in square brackets:
```
1. **Subject Line Format** [DELTA:new] [HOOK:yes]
   - **Description:** Use Conventional Commits 1.0.0 format
   - **Source:** conventionalcommits.org
   - **Reasoning:** Claude does not default to CC format without instruction. Mechanically verifiable via regex.
```

Classification principles:
- Style divergence (user wants something different from Claude's default) is handled in the preferences step, not by the researcher. The researcher classifies based on Claude's default behavior only.
- For hook classification, err on the side of [HOOK:no] when mechanical verification is ambiguous. False negatives (missing a hookable rule) are better than false positives (generating unreliable hooks).
```

**New Step 4a: Hook Recommendation (add after Step 4):**

```markdown
### Step 4a: Recommend Hook Necessity

After classifying all practices, assess whether the convention area would benefit from a PreToolUse enforcement hook.

A hook is recommended when:
- At least 2 practices are tagged `[HOOK:yes]`
- The convention area has a clear trigger event (e.g., commits trigger on `git commit`, PRs trigger on `gh pr create`)
- Mechanical validation is meaningful for the area (format/structure checking, not quality judgment)

Include a `## Hook Recommendation` section in the output report:

```markdown
## Hook Recommendation

**Recommended:** {Yes / No}
**Trigger event:** {the command or event pattern, e.g., "git commit", or "none identified"}
**Hookable rules:** {count} of {total} practices tagged [HOOK:yes]
**Reasoning:** {one-sentence explanation}
```

If hook is not recommended, still include the section with reasoning (e.g., "No -- only 1 of 8 practices is mechanically verifiable").
```

**Step 2: Research Best Practices -- add convention_tools instruction:**

After the existing search instruction block, add:
```
If `<convention_tools>` contains MCP tool names (e.g., `mcp__context7__resolve_library_id`), 
use those tools alongside the default WebSearch/WebFetch for enhanced research. These tools 
may provide access to additional documentation sources configured by the host project.
```

**Output Contract -- update the report format:**

Replace the `## Best Practices` section format in the output contract:
```markdown
## Best Practices

1. **{Practice Name}** [DELTA:new|known|varies] [HOOK:yes|no]
   - **Description:** {what the rule requires}
   - **Source:** {URL or document name}
   - **Reasoning:** {one-sentence explaining both delta classification and hook classification}

2. **{Practice Name}** [DELTA:known] [HOOK:no]
   ...
```

Remove the `Delta:` field from the old format -- it is now inline as `[DELTA:class]`.

Add the `## Hook Recommendation` section to the output contract template (after `## Common Pitfalls`):
```markdown
## Hook Recommendation

**Recommended:** {Yes / No}
**Trigger event:** {command pattern or "none identified"}
**Hookable rules:** {N} of {M} tagged [HOOK:yes]
**Reasoning:** {explanation}
```

**Delta Test Summary table -- update the output contract:**
```markdown
## Delta Test Summary

| Category | Count |
|----------|-------|
| Total practices | {N} |
| [DELTA:new] | {N} |
| [DELTA:varies] | {N} |
| [DELTA:known] | {N} |
| [HOOK:yes] | {N} |
| [HOOK:no] | {N} |
```

**Return Protocol -- update success line:**
```
## RESEARCH COMPLETE
Area: {area} | Language: {language} | Practices: {count} | Sources: {count} | Hook recommended: {yes/no}
```

**Quality Gate -- add new items:**
```
- [ ] Every practice has both [DELTA:class] and [HOOK:class] tags inline with the practice name
- [ ] Delta tags use exactly one of: [DELTA:new], [DELTA:known], [DELTA:varies]
- [ ] Hook tags use exactly one of: [HOOK:yes], [HOOK:no]
- [ ] Hook Recommendation section present with recommended/trigger/count/reasoning
- [ ] Tags appear on the same line as the practice name heading (parseable by generator)
```

Remove the old quality gate item: "Delta assessment provided for every practice with one-sentence reasoning" (superseded by the new tag-based items).
  </action>
  <verify>
    <automated>grep -q '\[DELTA:new\]' agents/convention-researcher.md && grep -q '\[HOOK:yes\]' agents/convention-researcher.md && grep -q 'Hook Recommendation' agents/convention-researcher.md && grep -q 'convention_tools' agents/convention-researcher.md && grep -q 'Step 4a' agents/convention-researcher.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - agents/convention-researcher.md contains [DELTA:new], [DELTA:known], [DELTA:varies] tag examples
    - agents/convention-researcher.md contains [HOOK:yes] and [HOOK:no] tag examples
    - agents/convention-researcher.md contains "Step 4a: Recommend Hook Necessity" section
    - agents/convention-researcher.md output contract shows tags inline with practice name heading
    - agents/convention-researcher.md contains `<convention_tools>` in input contract table
    - agents/convention-researcher.md quality gate includes tag verification items
    - agents/convention-researcher.md return protocol includes "Hook recommended" field
    - agents/convention-researcher.md Delta Test Summary table includes [HOOK:yes] and [HOOK:no] rows
  </acceptance_criteria>
  <done>Researcher agent returns structured delta and hook classification tags on every practice, includes hook recommendation section, and accepts convention_tools input</done>
</task>

<task type="auto">
  <name>Task 2: Update step-research and step-preferences with .state/ persistence and hook confirmation</name>
  <files>skills/convention/step-research.md, skills/convention/step-preferences.md</files>
  <read_first>
    - skills/convention/step-research.md (current research step -- full file)
    - skills/convention/step-preferences.md (current preferences step -- full file)
    - agents/convention-researcher.md (just-updated agent -- verify new output contract for parsing)
    - .planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md (D-10, D-11, D-12, D-13, D-14, D-19)
    - .planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md (.state/ directory structure, preferences.json schema)
  </read_first>
  <action>
**step-research.md updates:**

1. **Step 1: Build Host Project Context** -- Add .state/ reading.
   At the start of Step 1, add:
   ```
   Read `.state/init.json` from `state_dir` (received from step-init) to confirm:
   - `convention_tools` for research agent dispatch
   - `artifact_type` to frame research appropriately (e.g., skill-type conventions may need event-trigger research)
   - `area`, `lang` for research framing
   
   If `state_dir` is available, read init.json:
   ```bash
   cat {state_dir}/init.json
   ```
   ```

2. **Step 2: Dispatch Research Agent** -- Add convention_tools to agent prompt.
   In the Agent dispatch prompt template, update `<research_tools>`:
   ```
   <research_tools>
   Default: WebSearch, WebFetch, Read, Grep, Glob
   Additional: {convention_tools from init.json or step-init pass-through, joined by comma, or 'none'}
   </research_tools>
   ```

   Also add a new XML tag in the dispatch prompt:
   ```
   <convention_tools>{convention_tools from init.json, comma-separated, or 'none'}</convention_tools>
   ```

3. **Step 4: Present Research Summary** -- Update delta classification display.
   Replace the delta display format. Old format shows "HIGH-DELTA / LOW-DELTA / NO-DELTA". New format:
   ```
   **Best practices:** Display as a numbered list. For each practice, show the name 
   (with [DELTA:class] and [HOOK:class] tags), description, and source.
   
   **Delta summary:**
   ```
   Delta summary: {total} practices found
     - {N} [DELTA:new] -- will teach (Claude does not do this by default)
     - {N} [DELTA:varies] -- consistency value (Claude does this inconsistently)
     - {N} [DELTA:known] -- filtered (Claude already does this by default)
   
   Hook summary: {N} of {total} rules are mechanically verifiable [HOOK:yes]
   Hook recommended: {Yes/No} -- {trigger event or reasoning}
   ```
   ```

4. **New Step 4a: Write Research to .state/** (insert after Step 4, before Step 5):
   ```markdown
   ## Step 4a: Persist Research Results
   
   Write the full research report to `.state/research.md`:
   
   ```bash
   # Write research_results to state_dir
   ```
   
   Use the Write tool to write `research_results` (the full research report text from the 
   researcher agent) to `{state_dir}/research.md`.
   
   This makes research results available to step-generate without relying on implicit 
   context passing. The generator will read this file directly.
   ```

5. **Step 5: Transition** -- Update to reference .state/.
   Add to the "Pass forward" list:
   ```
   - `state_dir` -- path to .state/ directory
   - `artifact_type` -- "rule" or "skill"
   ```
   
   Add note: "Research results are also persisted at `{state_dir}/research.md` for downstream steps to read independently."

**step-preferences.md updates:**

1. **Step 2: Adaptive Question Loop** -- Add hook confirmation.
   After the existing loop description and before Step 3, add a new section:
   ```markdown
   ## Step 2a: Hook Confirmation (When Research Recommends Hook)
   
   Skip this step if `research_results` does not contain a `## Hook Recommendation` 
   section, or if the recommendation is "No".
   
   When the researcher recommends a hook:
   
   Present in conversation text:
   ```
   The research identified {N} mechanically verifiable rules in this convention area.
   A PreToolUse hook can enforce these rules automatically:
   
   Hookable rules:
   {list each [HOOK:yes] rule from research by name}
   
   Trigger event: {trigger event from hook recommendation}
   ```
   
   Then ask via AskUserQuestion:
   
   > Generate an enforcement hook for this convention?
   
   Options:
   1. "Yes, generate hook + tests"
   2. "No, convention only"
   
   Store the selection as `hook_confirmed` (true or false).
   
   If `hook_confirmed` is true, also store the list of [HOOK:yes] rule names as
   `hook_rules` for the generator.
   ```

2. **Step 3: Preference Summary and Confirmation** -- Include hook in summary.
   Update the summary format:
   ```
   Here's what I'll use to generate your convention:
   
   - {Preference 1}: {value}
   - {Preference 2}: {value}
   - ...
   {if hook_confirmed is set:}
   - Hook generation: {Yes -- {N} hookable rules / No}
   ```

3. **New Step 3a: Write Preferences to .state/** (insert after Step 3, before Step 4):
   ```markdown
   ## Step 3a: Persist Preferences
   
   Write all collected preferences to `.state/preferences.json`:
   
   ```json
   {
     "preferences": [
       { "key": "{preference_key}", "value": "{value}", "source": "{user/research-confirmed}" }
     ],
     "hook_confirmed": {true/false},
     "hook_rules": ["{rule_name}", ...],
     "artifact_type": "{rule or skill from init}"
   }
   ```
   
   Use the Write tool to write preferences JSON to `{state_dir}/preferences.json`.
   
   This makes preferences available to step-generate for post-generation verification 
   without relying on implicit context.
   ```

4. **Step 4: Transition** -- Update pass-forward list.
   Add to both "Research first" and "Preferences first" pass-forward lists:
   ```
   - `state_dir` -- path to .state/ directory
   - `artifact_type` -- "rule" or "skill"
   - `hook_confirmed` -- boolean (whether to generate hook)
   - `hook_rules` -- list of hookable rule names (if hook_confirmed)
   ```
  </action>
  <verify>
    <automated>grep -q '\.state/research\.md' skills/convention/step-research.md && grep -q '\[DELTA:new\]' skills/convention/step-research.md && grep -q 'hook_confirmed' skills/convention/step-preferences.md && grep -q '\.state/preferences\.json' skills/convention/step-preferences.md && grep -q 'Step 2a.*Hook Confirmation' skills/convention/step-preferences.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/convention/step-research.md reads init.json from state_dir at start of Step 1
    - skills/convention/step-research.md passes convention_tools to researcher agent dispatch prompt
    - skills/convention/step-research.md displays [DELTA:new/known/varies] tags in Step 4 presentation
    - skills/convention/step-research.md contains "Step 4a: Persist Research Results" writing to .state/research.md
    - skills/convention/step-research.md transition passes state_dir and artifact_type forward
    - skills/convention/step-preferences.md contains "Step 2a: Hook Confirmation" section
    - skills/convention/step-preferences.md hook confirmation asks via AskUserQuestion with Yes/No options
    - skills/convention/step-preferences.md contains "Step 3a: Persist Preferences" writing to .state/preferences.json
    - skills/convention/step-preferences.md preferences.json includes hook_confirmed and hook_rules fields
    - skills/convention/step-preferences.md transition passes hook_confirmed and hook_rules forward
  </acceptance_criteria>
  <done>Research pipeline writes structured delta/hook tagged results to .state/, preferences pipeline confirms hooks and persists to .state/, convention_tools flow through from init to researcher</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No runtime trust boundaries | This plan modifies markdown instruction files only -- no executable code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-03 | Tampering | .state/research.md | accept | Research output is written by the user's own Claude session. No external input. Cleaned up after generation. |
| T-19.1-04 | Information Disclosure | .state/preferences.json | accept | Contains only convention style preferences (e.g., "subject_line_length: 100"). No secrets. Temporary file cleaned up per D-15. |
</threat_model>

<verification>
1. `grep -q '\[DELTA:new\]' agents/convention-researcher.md` -- researcher uses new tag format
2. `grep -q '\[HOOK:yes\]' agents/convention-researcher.md` -- researcher uses hook tags
3. `grep -q '\.state/research\.md' skills/convention/step-research.md` -- research persisted
4. `grep -q 'hook_confirmed' skills/convention/step-preferences.md` -- hook confirmation exists
5. `grep -q '\.state/preferences\.json' skills/convention/step-preferences.md` -- preferences persisted
</verification>

<success_criteria>
- Researcher agent returns structured [DELTA:class] and [HOOK:class] tags on every practice
- Researcher agent includes hook recommendation section in output
- step-research reads .state/init.json, passes convention_tools, writes .state/research.md
- step-preferences includes hook confirmation question, writes .state/preferences.json
- All step outputs are persisted to .state/ for downstream consumption
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-convention-skill-improvements/19.1-02-SUMMARY.md`
</output>
