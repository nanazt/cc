---
phase: 19.1-convention-skill-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/CONVENTIONS.md
  - skills/convention/SKILL.md
  - skills/convention/step-init.md
autonomous: true
requirements: [CONV-01, CONV-04, CONV-09]

must_haves:
  truths:
    - "docs/CONVENTIONS.md describes both rule-type and skill-type conventions with distinct frontmatter formats"
    - "SKILL.md orchestrator routes skill-type conventions to SKILL.md filename, rule-type to CONVENTION.md"
    - "step-init creates .state/ directory and writes init.json with artifact type, convention_tools, and all step variables"
    - "ROADMAP Phase 19.1 scope reflects discussion outcomes including all expanded items"
  artifacts:
    - path: "docs/CONVENTIONS.md"
      provides: "Updated architecture spec with rule/skill binary classification, skill-type frontmatter, unified hook discovery, updated anti-patterns"
      contains: "SKILL.md"
    - path: "skills/convention/SKILL.md"
      provides: "Updated orchestrator with artifact type routing and .state/ references"
      contains: "artifact_type"
    - path: "skills/convention/step-init.md"
      provides: "Init step with artifact type detection, .state/ initialization, convention_tools reading"
      contains: ".state/"
  key_links:
    - from: "skills/convention/step-init.md"
      to: ".state/init.json"
      via: "Bash mkdir + Write"
      pattern: "\\.state/"
    - from: "skills/convention/SKILL.md"
      to: "docs/CONVENTIONS.md"
      via: "Reference for format specification"
      pattern: "docs/CONVENTIONS.md"
---

<objective>
Establish the foundation for the rule/skill binary classification architecture: update docs/CONVENTIONS.md to reflect that conventions can be either rules or skills (per D-01), update the SKILL.md orchestrator for artifact type routing, and update step-init.md to detect artifact type, initialize .state/ persistence, and read convention_tools config. Also update ROADMAP scope for Phase 19.1.

Purpose: All subsequent plans depend on the architecture spec, the orchestrator's routing logic, and step-init's .state/ output. This plan defines the contracts that Plans 02-04 implement against.
Output: Updated docs/CONVENTIONS.md, updated SKILL.md, updated step-init.md, updated ROADMAP.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md
@.planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md

<interfaces>
<!-- Current docs/CONVENTIONS.md structure: Overview, Source Structure, Convention File Format, Base/Lang Relationship, Delta Test, Naming and Installation, Hook Architecture, Override and Precedence, Convention Content Principles, Anti-Patterns, Relationship to Other Artifacts, Summary Table -->

<!-- Current SKILL.md step_routing resolves CONVENTION.md only, no SKILL.md awareness -->
<!-- Current step-init writes no state files, passes variables implicitly to next step -->

From .claude/cckit.json:
```json
{
  "publisher": true,
  "convention": {
    "tools": []
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update docs/CONVENTIONS.md with rule/skill binary classification</name>
  <files>docs/CONVENTIONS.md</files>
  <read_first>
    - docs/CONVENTIONS.md (current architecture spec -- full file, understand all sections)
    - .planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md (decisions D-01 through D-22)
    - .planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md (architecture patterns, rule vs skill criteria, unified discovery, pitfalls)
  </read_first>
  <action>
Update docs/CONVENTIONS.md with these specific changes per D-22:

**Overview section:**
- Change "What conventions are" from "Claude Code rules (.claude/rules/*.md files) -- passive behavioral guidance that Claude loads automatically. Not skills, not agents." to: "Claude Code rules or skills. Rule-type conventions are passive behavioral guidance loaded automatically (always-on). Skill-type conventions are on-demand behavioral guidance loaded when relevant (event-driven). The choice between rule and skill is a binary classification per convention area."
- Add a comparison table after the definition:

| Property | Rule-Type | Skill-Type |
|----------|-----------|------------|
| Format | `.claude/rules/` file | `.claude/skills/{name}/SKILL.md` |
| Loading | Always in context | Description always visible; body loaded on-demand |
| Context cost | Full content every session | Description only (~1 line) until invoked |
| Tool access | None | Optional via allowed-tools |
| Best for | Continuous guidance (coding, security) | Event-driven guidance (commit, workflow) |

**Source Structure section:**
- Update the file table to include SKILL.md:

| File | Purpose | Required? |
|------|---------|----------|
| `CONVENTION.md` | Base convention (rule-type) -- tech-neutral, always-loading | Optional (see Delta Test) |
| `SKILL.md` | Base convention (skill-type) -- tech-neutral, on-demand loading | Optional (see Delta Test) |
| `{lang}.md` | Language-specific convention -- e.g., `rust.md`, `typescript.md` | Optional |
| `hooks/` | Enforcement scripts for this convention area | Optional |

- Note below table: "Each area has either CONVENTION.md (rule-type) or SKILL.md (skill-type), never both. The filename signals the artifact type without reading frontmatter (per D-04)."
- Update the canonical source structure example to show SKILL.md for commit:

```
conventions/
+-- commit/
|   +-- SKILL.md                   # skill-type (event-driven)
|   +-- hooks/
|       +-- validate.sh            # enforcement hook
+-- coding/
|   +-- CONVENTION.md              # rule-type (continuous)
|   +-- rust.md                    # language-specific
```

**Convention File Format section:**
- Add a new subsection "### Skill-Type Convention" after "### Base Convention (Always-Loading)":

```markdown
### Skill-Type Convention (On-Demand)

A skill-type convention loads only when Claude determines it is relevant to the current task. Example frontmatter:

---
name: cckit-{area}
description: >
  {description of what this convention covers and when to apply it}.
  Use when: {specific trigger events or contexts}.
user-invocable: false
---

Skill-type conventions use Claude Code skill frontmatter: `name`, `description`, `user-invocable`. The `user-invocable: false` field means Claude auto-invokes when relevant -- users do not invoke directly.

Optional: `allowed-tools` field when tool access adds value (e.g., Read for context-aware validation). Not all skill-type conventions need tools -- some are skills purely for on-demand loading efficiency.
```

**Naming and Installation section:**
- Update the Source-to-Installed Mapping table to include skill-type:

| Source File | Installed As | Rule |
|-------------|-------------|------|
| `conventions/commit/CONVENTION.md` | `.claude/rules/cckit-commit.md` | `cckit-{area}.md` |
| `conventions/commit/SKILL.md` | `.claude/skills/cckit-commit/SKILL.md` | `cckit-{area}/SKILL.md` |
| `conventions/coding/CONVENTION.md` | `.claude/rules/cckit-coding.md` | `cckit-{area}.md` |
| `conventions/coding/rust.md` | `.claude/rules/cckit-coding-rust.md` | `cckit-{area}-{lang}.md` |

- Note: "Skill-type conventions install as directories (Claude Code skills require `{name}/SKILL.md` structure). Rule-type conventions install as flat files."

**Hook Architecture section:**
- Add a new subsection "### Unified Convention Discovery" before "### Hook Registration":

```markdown
### Unified Convention Discovery

Hook scripts discover convention files using a unified search that checks both skill-type and rule-type locations, in both publisher and consumer modes:

1. `$CWD/conventions/{area}/SKILL.md` (publisher, skill-type)
2. `$CWD/conventions/{area}/CONVENTION.md` (publisher, rule-type)
3. `$CWD/.claude/skills/cckit-{area}/SKILL.md` (consumer, skill-type)
4. `$CWD/.claude/rules/cckit-{area}.md` (consumer, rule-type)

First match wins. This ensures hooks work regardless of convention type or installation mode.
```

**Anti-Patterns table:**
- Remove the "Convention-as-skill" anti-pattern row entirely.
- Add a replacement row:

| Anti-Pattern | Why Avoid | Correct Approach |
|--------------|-----------|-----------------|
| Wrong artifact type | Using a rule for event-driven conventions wastes context. Using a skill for continuous conventions adds invocation overhead. | Apply the three-criteria test: event-driven loading, tool access need, context size. |

**Summary Table:**
- Update the table:
  - Change "Convention format" from "Claude Code rules (.claude/rules/ files)" to "Claude Code rules or skills"
  - Change "Base file name" from "CONVENTION.md" to "CONVENTION.md (rule-type) or SKILL.md (skill-type)"
  - Add row: "Artifact type decision" | "Three-criteria test: event-driven loading, tool access, context size"
  - Update "Installed prefix" to: "cckit- (rules: flat file, skills: directory)"

**Footer:**
- Update last-updated date to current date.
  </action>
  <verify>
    <automated>grep -c "SKILL.md" docs/CONVENTIONS.md | grep -q '[3-9]\|[1-9][0-9]' && grep -q "Skill-Type Convention" docs/CONVENTIONS.md && grep -q "Unified Convention Discovery" docs/CONVENTIONS.md && grep -q "three-criteria test" docs/CONVENTIONS.md && ! grep -q "Convention-as-skill" docs/CONVENTIONS.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/CONVENTIONS.md contains "### Skill-Type Convention (On-Demand)" subsection
    - docs/CONVENTIONS.md contains "### Unified Convention Discovery" subsection
    - docs/CONVENTIONS.md contains "user-invocable: false" in skill-type frontmatter example
    - docs/CONVENTIONS.md contains comparison table with "Rule-Type" and "Skill-Type" columns
    - docs/CONVENTIONS.md does NOT contain "Convention-as-skill" anti-pattern
    - docs/CONVENTIONS.md contains "Wrong artifact type" anti-pattern replacing it
    - docs/CONVENTIONS.md source-to-installed mapping includes `.claude/skills/cckit-commit/SKILL.md`
    - docs/CONVENTIONS.md summary table includes "rules or skills"
  </acceptance_criteria>
  <done>docs/CONVENTIONS.md fully updated with rule/skill binary classification, skill-type frontmatter, unified discovery, and corrected anti-patterns per D-01, D-04, D-05, D-06, D-20, D-22</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md orchestrator and step-init with artifact type + .state/ + convention_tools</name>
  <files>skills/convention/SKILL.md, skills/convention/step-init.md, .planning/ROADMAP.md</files>
  <read_first>
    - skills/convention/SKILL.md (current orchestrator -- full file)
    - skills/convention/step-init.md (current init step -- full file)
    - .planning/ROADMAP.md (Phase 19.1 section)
    - docs/CONVENTIONS.md (just-updated architecture spec from Task 1)
    - .planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md (D-01, D-02, D-04, D-14, D-15, D-19, D-23)
    - .planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md (.state/ structure, init.json schema, modified skill step flow)
  </read_first>
  <action>
**SKILL.md orchestrator updates:**

1. Change the `description` field from "Research-driven convention authoring tool. Creates and updates convention files (Claude Code rules) following the cckit convention architecture." to "Research-driven convention authoring tool. Creates and updates convention files (Claude Code rules or skills) following the cckit convention architecture."

2. Change the `<purpose>` tag content: replace "(Claude Code rules)" with "(Claude Code rules or skills)" in the opening sentence. Replace "One convention per invocation." with "One convention per invocation. Output is either a rule (always-loaded, passive) or a skill (on-demand, context-aware) -- determined during init."

3. In the `<step_routing>` section:
   - Replace the path resolution block. Old logic resolves only CONVENTION.md. New logic:
     ```
     Resolve target file path based on publisher flag, area/lang, and artifact type:
     - publisher == true: 
       - Rule-type base: `conventions/{area}/CONVENTION.md`
       - Skill-type base: `conventions/{area}/SKILL.md`
       - Lang: `conventions/{area}/{lang}.md` (unchanged, always rule-type)
     - publisher == false or absent:
       - Rule-type base: `.claude/rules/cckit-{area}.md`
       - Skill-type base: `.claude/skills/cckit-{area}/SKILL.md`
       - Lang: `.claude/rules/cckit-{area}-{lang}.md` (unchanged)
     
     Note: Artifact type (rule vs skill) is determined in step-init.md. The orchestrator's
     file state detection checks for BOTH CONVENTION.md and SKILL.md to detect update mode.
     ```
   - Update the file state detection. Old: only checks resolved_path. New:
     ```
     Check if target file exists to determine create vs update mode:
     For publisher mode base conventions, check BOTH filenames:
     - `test -f conventions/{area}/CONVENTION.md` (rule-type exists?)
     - `test -f conventions/{area}/SKILL.md` (skill-type exists?)
     If either exists -> mode = "update", resolved_path = the existing file.
     If neither -> mode = "create", resolved_path determined after artifact type selection in step-init.
     ```

4. In the `<philosophy>` section, add after "Technology neutrality for base conventions.":
   ```
   **Right artifact type for the job.** Event-driven conventions (commit, workflow) are skills -- loaded only when the event occurs. Continuous conventions (coding, security) are rules -- loaded every session. The three-criteria test in step-init determines the type.
   ```

5. In `<success_criteria>`, add:
   - "Artifact type (rule or skill) determined correctly for the convention area"
   - "Step output persisted to .state/ directory for downstream step consumption"

**step-init.md updates:**

The entire step-init.md needs significant restructuring. Here are the changes:

1. **Step 2: Read Config** -- Add convention_tools extraction.
   After extracting `publisher` and `convention.tools`, add:
   ```
   Store both values for use throughout this and subsequent steps.
   These will be written to `.state/init.json` in Step 8.
   ```
   The `convention.tools` extraction already exists but the step currently says "Store both values for use throughout this and subsequent steps." -- keep this but update it to reference .state/.

2. **Step 3: Resolve Output Path** -- Update for artifact type.
   Before path resolution, add a note:
   ```
   Path resolution for base conventions depends on `artifact_type` (determined in Step 7a).
   For initial mode detection (Step 4), check BOTH possible filenames.
   Final path is resolved after artifact type is determined.
   ```
   
   Split Step 3 into two phases:
   - **Step 3a: Preliminary path check** (for mode detection only):
     Publisher mode: check both `conventions/{area}/CONVENTION.md` and `conventions/{area}/SKILL.md`
     Consumer mode: check both `.claude/rules/cckit-{area}.md` and `.claude/skills/cckit-{area}/SKILL.md`
   - **Step 3b: Final path resolution** (after artifact type is known, moved to after Step 7a):
     Use the artifact_type to determine the definitive filename.

3. **New Step 7a: Artifact Type Detection** (insert after Step 7: Flow Selection, before Transition):
   ```markdown
   ## Step 7a: Artifact Type Detection (Create Mode Only)
   
   Skip this step entirely if `mode == "update"` (artifact type is determined by the existing file).
   Skip this step if `lang` is present (language-specific conventions are always rule-type).
   
   Apply the three-criteria test to determine whether this convention should be a rule or a skill:
   
   **Criterion 1: Event-Driven Loading** -- Does this convention primarily apply during a
   specific action or event (commit, PR, deploy, test run) rather than continuously?
   - YES -> Skill candidate. Context cost is wasted when the event is not happening.
   - NO -> Rule candidate. Continuous applicability justifies always-on loading.
   
   **Criterion 2: Tool Access Need** -- Would this convention benefit from Claude having
   tool access (Read, Bash, Grep) when applying it?
   - YES -> Skill. Rules cannot grant tool access.
   - NO -> Continue to next criterion.
   
   **Criterion 3: Context Size** -- Is the convention content large enough (>50 lines) that
   always-loading it wastes significant context?
   - YES -> Skill candidate. On-demand loading preserves context budget.
   - NO -> Rule. Small conventions have negligible context cost.
   
   Present the recommendation in conversation text:
   
   ```
   Based on the {area} convention area:
   - Event-driven: {yes/no -- reasoning}
   - Tool access needed: {yes/no -- reasoning}
   - Expected size: {small/large -- reasoning}
   
   Recommendation: {Rule / Skill} -- {one-sentence reasoning}
   ```
   
   Then ask via AskUserQuestion:
   
   > Convention type?
   
   Options:
   1. "Rule (always-loaded, passive)" 
   2. "Skill (on-demand, context-aware)"
   
   Store the selection as `artifact_type` ("rule" or "skill").
   
   **Step 3b: Final Path Resolution**
   
   Now resolve the definitive output path using `artifact_type`:
   
   Publisher mode:
   - Rule-type base: `conventions/{area}/CONVENTION.md`
   - Skill-type base: `conventions/{area}/SKILL.md`
   - Lang: `conventions/{area}/{lang}.md`
   
   Consumer mode:
   - Rule-type base: `.claude/rules/cckit-{area}.md`
   - Skill-type base: `.claude/skills/cckit-{area}/SKILL.md`
   - Lang: `.claude/rules/cckit-{area}-{lang}.md`
   
   Store the resolved target path as `resolved_path`.
   ```

4. **New Step 8: Initialize .state/ Directory** (insert before Transition):
   ```markdown
   ## Step 8: Initialize .state/ Directory (Create Mode Only)
   
   Skip this step entirely if `mode == "update"`.
   
   Create the .state/ directory and write init.json:
   
   If publisher mode:
     state_dir = `conventions/{area}/.state/`
   If consumer mode:
     state_dir = `.convention-state/{area}/`
   
   ```bash
   mkdir -p {state_dir}
   ```
   
   Write the following JSON to `{state_dir}/init.json`:
   ```json
   {
     "area": "{area}",
     "lang": "{lang or null}",
     "publisher": {publisher},
     "convention_tools": {convention_tools array},
     "resolved_path": "{resolved_path}",
     "mode": "{mode}",
     "artifact_type": "{rule or skill}",
     "selected_flow": "{selected_flow}",
     "create_base_first": {create_base_first}
   }
   ```
   
   Store `state_dir` for use in subsequent steps.
   ```

5. **Transition section** -- Update to pass `state_dir` and `artifact_type`:
   Add `artifact_type` and `state_dir` to the list of values passed forward:
   ```
   - `artifact_type` -- "rule" or "skill"
   - `state_dir` -- path to .state/ directory
   ```

**ROADMAP.md update (D-23):**

Update the Phase 19.1 section's scope list to reflect the expanded scope from discussion:

```
Scope:
1. Output artifact type selection -- skill judges whether convention is best expressed as a rule or skill; three-criteria test
2. Hook awareness + scaffolding integrated into step-generate -- generator produces hook + test + fixtures alongside convention
3. CLAUDE.md conflict detection -- step-generate light review cross-references existing project rules
4. Update-mode hook sync -- step-update auto-regenerates hook + test alongside convention
5. Research-to-Generator structured delta transfer -- [DELTA:new/known/varies] + [HOOK:yes/no] classification tags
6. Test infrastructure scaffolding -- auto-generated test.sh + fixtures/ when hooks are created
7. Post-generation user preferences verification -- step-generate validates output against collected preferences
8. convention_tools config activation -- step-init reads MCP tool names from cckit.json, passes to researcher
9. Step-to-step variable persistence -- .state/ directory with init.json, research.md, preferences.json
10. docs/CONVENTIONS.md full architecture update -- rule/skill binary, unified discovery, anti-pattern correction
11. Commit convention migration -- CONVENTION.md to SKILL.md with content preserved
12. Hook unified discovery path -- SKILL.md -> CONVENTION.md -> skills/ -> rules/
13. writing-skills integration -- generator invokes writing-skills for skill-type output
14. Filename convention -- Rule-type = CONVENTION.md, Skill-type = SKILL.md
```

Update `**Plans:** 0 plans` to `**Plans:** 4 plans`

Update the plan list:
```
Plans:
- [ ] 19.1-01-PLAN.md -- Architecture doc, orchestrator, and step-init foundation
- [ ] 19.1-02-PLAN.md -- Research pipeline: researcher agent, step-research, step-preferences
- [ ] 19.1-03-PLAN.md -- Generate pipeline: generator agent, step-generate overhaul
- [ ] 19.1-04-PLAN.md -- Update mode, commit migration, hook discovery, test fixtures
```
  </action>
  <verify>
    <automated>grep -q "artifact_type" skills/convention/step-init.md && grep -q "\.state/" skills/convention/step-init.md && grep -q "init.json" skills/convention/step-init.md && grep -q "rules or skills" skills/convention/SKILL.md && grep -q "4 plans" .planning/ROADMAP.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/convention/SKILL.md description contains "rules or skills"
    - skills/convention/SKILL.md step_routing handles both CONVENTION.md and SKILL.md filenames
    - skills/convention/SKILL.md philosophy section includes artifact type guidance
    - skills/convention/step-init.md contains "Step 7a: Artifact Type Detection"
    - skills/convention/step-init.md contains three-criteria test (event-driven, tool access, context size)
    - skills/convention/step-init.md contains "Step 8: Initialize .state/ Directory"
    - skills/convention/step-init.md writes init.json with fields: area, lang, publisher, convention_tools, resolved_path, mode, artifact_type, selected_flow, create_base_first
    - skills/convention/step-init.md Transition section passes artifact_type and state_dir forward
    - .planning/ROADMAP.md Phase 19.1 shows "4 plans" and lists all 4 plan filenames
    - .planning/ROADMAP.md Phase 19.1 scope has 14 items matching expanded discussion outcomes
  </acceptance_criteria>
  <done>SKILL.md orchestrator routes both artifact types, step-init detects type via three-criteria test, initializes .state/ with init.json, reads convention_tools. ROADMAP updated with expanded scope and plan list.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No runtime trust boundaries | This plan modifies markdown instruction files only -- no executable code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-01 | Information Disclosure | .state/init.json | accept | .state/ contains only convention authoring metadata (area name, preferences). No secrets. Cleaned up after generation per D-15. |
| T-19.1-02 | Tampering | .state/ files | accept | .state/ is a temporary directory within the user's own project. Same trust level as any other project file. |
</threat_model>

<verification>
1. `grep -q "Skill-Type Convention" docs/CONVENTIONS.md` -- architecture doc has skill-type section
2. `grep -q "Unified Convention Discovery" docs/CONVENTIONS.md` -- architecture doc has unified discovery
3. `grep -q "artifact_type" skills/convention/step-init.md` -- init step handles artifact type
4. `grep -q "\.state/" skills/convention/step-init.md` -- init step creates .state/
5. `grep -q "rules or skills" skills/convention/SKILL.md` -- orchestrator updated
6. `grep -q "4 plans" .planning/ROADMAP.md` -- roadmap updated
</verification>

<success_criteria>
- docs/CONVENTIONS.md fully reflects rule/skill binary classification with all sections updated
- SKILL.md orchestrator handles both artifact types in routing
- step-init.md detects artifact type, initializes .state/, reads convention_tools
- ROADMAP.md Phase 19.1 scope expanded and plan list populated
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-convention-skill-improvements/19.1-01-SUMMARY.md`
</output>
