---
phase: 19.1-convention-skill-improvements
plan: 04
type: execute
wave: 3
depends_on: [19.1-01, 19.1-03]
files_modified:
  - skills/convention/step-update.md
  - conventions/commit/SKILL.md
  - conventions/commit/hooks/validate.sh
  - conventions/commit/hooks/test.sh
autonomous: true
requirements: [CONV-06, CONV-10, CONV-11, CONV-12]

must_haves:
  truths:
    - "step-update warns about and auto-regenerates hooks when updating a convention that has hooks"
    - "step-update handles both rule-type and skill-type convention updates"
    - "Commit convention file is SKILL.md with skill frontmatter, CONVENTION.md no longer exists"
    - "Hook validate.sh discovers SKILL.md first in unified discovery path"
    - "All 14+ hook tests pass after migration"
  artifacts:
    - path: "skills/convention/step-update.md"
      provides: "Updated update step with hook sync, skill-type handling, .state/ integration"
      contains: "hook"
    - path: "conventions/commit/SKILL.md"
      provides: "Migrated commit convention in skill format"
      contains: "user-invocable: false"
    - path: "conventions/commit/hooks/validate.sh"
      provides: "Hook script with unified discovery checking SKILL.md first"
      contains: "SKILL.md"
    - path: "conventions/commit/hooks/test.sh"
      provides: "Test suite updated with skill-type discovery test"
      contains: "SKILL.md"
  key_links:
    - from: "conventions/commit/hooks/validate.sh"
      to: "conventions/commit/SKILL.md"
      via: "Unified discovery path"
      pattern: "SKILL\\.md"
    - from: "conventions/commit/hooks/test.sh"
      to: "conventions/commit/hooks/validate.sh"
      via: "Hook execution"
      pattern: "validate\\.sh"
    - from: "skills/convention/step-update.md"
      to: "agents/convention-generator.md"
      via: "Agent dispatch for hook regeneration"
      pattern: "convention-generator"
---

<objective>
Complete the Phase 19.1 improvements: update step-update for hook sync and skill-type handling (D-17), migrate the commit convention from CONVENTION.md to SKILL.md (D-21), update the hook script's discovery path to the unified pattern (D-20), and ensure all existing tests pass after migration.

Purpose: This plan closes the remaining gaps: step-update must auto-regenerate hooks when conventions are updated, the commit convention must be migrated to validate the skill-type architecture end-to-end, and the hook discovery must work with both artifact types.
Output: Updated step-update.md, migrated conventions/commit/SKILL.md, updated validate.sh and test suite
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md
@.planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md
@.planning/phases/19.1-convention-skill-improvements/19.1-01-SUMMARY.md
@.planning/phases/19.1-convention-skill-improvements/19.1-03-SUMMARY.md

<interfaces>
<!-- From Plan 01: docs/CONVENTIONS.md now defines unified discovery path and skill-type format -->
<!-- From Plan 01: step-init passes artifact_type and state_dir -->
<!-- From Plan 03: convention-generator now handles hook generation, skill-type output, and reads [DELTA:class] [HOOK:class] tags -->
<!-- From Plan 03: step-generate handles .state/ cleanup, CLAUDE.md conflict detection, preferences verification -->

From conventions/commit/CONVENTION.md (current content to migrate):
```yaml
---
description: "Commit message format and quality standards following Conventional Commits 1.0.0"
---
```
Body content: Format, Scope, Subject Line, Body, No Project Management References, Breaking Changes, Issue References, Atomic Commits sections.
Footer: `<!-- Generated by /convention, 2026-04-06T12:18:15Z -->`

From conventions/commit/hooks/validate.sh (current discovery -- lines 20-24):
```bash
CONV_FILE=""
if [ -f "$CWD/conventions/commit/CONVENTION.md" ]; then
  CONV_FILE="$CWD/conventions/commit/CONVENTION.md"
elif [ -f "$CWD/.claude/rules/cckit-commit.md" ]; then
  CONV_FILE="$CWD/.claude/rules/cckit-commit.md"
fi
```

From conventions/commit/hooks/test.sh: 14 existing tests (3 passthrough, 4 allow, 7 deny).
Test fixture format: `{"tool_name":"Bash","tool_input":{"command":"{cmd}"},"cwd":"__CWD__"}`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate commit convention to SKILL.md and update hook discovery</name>
  <files>conventions/commit/SKILL.md, conventions/commit/CONVENTION.md, conventions/commit/hooks/validate.sh, conventions/commit/hooks/test.sh</files>
  <read_first>
    - conventions/commit/CONVENTION.md (current convention file -- full content to migrate)
    - conventions/commit/hooks/validate.sh (current hook script -- full file, update discovery)
    - conventions/commit/hooks/test.sh (current test suite -- full file, add skill discovery test)
    - conventions/commit/hooks/fixtures/valid-commit.json (fixture format reference)
    - .planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md (D-20, D-21)
    - .planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md (commit convention migration section, unified hook discovery section)
    - docs/CONVENTIONS.md (updated architecture spec -- skill-type frontmatter format from Plan 01)
  </read_first>
  <action>
**Step 1: Create conventions/commit/SKILL.md**

Create the file with skill-type frontmatter. The body content is identical to the current CONVENTION.md. The frontmatter changes from rule format to skill format:

```yaml
---
name: cckit-commit
description: >
  Commit message format and quality standards following Conventional Commits 1.0.0.
  Use when: writing commit messages, reviewing commits, amending commits.
user-invocable: false
---
```

Copy the entire body content from CONVENTION.md (everything after the frontmatter closing `---`): Format, Scope, Subject Line, Body, No Project Management References, Breaking Changes, Issue References, Atomic Commits sections.

Update the footer timestamp to the current ISO 8601 timestamp:
```
<!-- Generated by /convention, {current_timestamp} -->
```

**Step 2: Delete conventions/commit/CONVENTION.md**

```bash
rm conventions/commit/CONVENTION.md
```

After deletion, verify only SKILL.md exists:
```bash
ls conventions/commit/*.md
```
Expected output: `conventions/commit/SKILL.md` only.

**Step 3: Update conventions/commit/hooks/validate.sh discovery path**

Replace lines 19-24 (the convention file discovery block). Current code:
```bash
CONV_FILE=""
if [ -f "$CWD/conventions/commit/CONVENTION.md" ]; then
  CONV_FILE="$CWD/conventions/commit/CONVENTION.md"
elif [ -f "$CWD/.claude/rules/cckit-commit.md" ]; then
  CONV_FILE="$CWD/.claude/rules/cckit-commit.md"
fi
```

Replace with unified discovery per D-20 (checking all 4 paths, SKILL.md first):
```bash
# --- Convention file discovery (unified: skill first, then rule) ---
CONV_FILE=""
for path in \
  "$CWD/conventions/commit/SKILL.md" \
  "$CWD/conventions/commit/CONVENTION.md" \
  "$CWD/.claude/skills/cckit-commit/SKILL.md" \
  "$CWD/.claude/rules/cckit-commit.md"; do
  if [ -f "$path" ]; then
    CONV_FILE="$path"
    break
  fi
done
```

Also update the comment at the top of the file (line 3). Change:
```
# Reads convention file at runtime and injects via additionalContext.
```
to:
```
# Discovers convention file (skill or rule) at runtime and injects via additionalContext.
```

**Step 4: Add skill-type discovery test to test.sh**

Add a new test after the existing "Allow" section (after `assert_has_context "gsd-tools-commit.json"` around line 88). The test validates that the hook discovers and allows with the SKILL.md file (which now exists after migration).

The existing `valid-commit.json` fixture already uses `__CWD__` which points to the project root. After migration, the project root has `conventions/commit/SKILL.md` (not CONVENTION.md). So the existing `valid-commit.json` test implicitly tests SKILL.md discovery now.

No new fixture is strictly needed because the existing `valid-commit.json` + `__CWD__` replacement already points to the project root where SKILL.md now lives. The hook's unified discovery will find SKILL.md first.

However, add an explicit comment in test.sh noting this:
```bash
# Note: valid-commit.json uses __CWD__ pointing to project root.
# After migration, conventions/commit/SKILL.md is discovered (not CONVENTION.md).
# The unified discovery path checks SKILL.md first.
```

Add this comment in the "Allow tests" section, before the existing `assert_allow "valid-commit.json"` line.

**Step 5: Verify all 14 existing tests still pass**

Run the test suite:
```bash
bash conventions/commit/hooks/test.sh
```

All 14 tests must pass. The key change: valid-commit.json now discovers SKILL.md instead of CONVENTION.md. The content is identical so injection still works.
  </action>
  <verify>
    <automated>test -f conventions/commit/SKILL.md && ! test -f conventions/commit/CONVENTION.md && grep -q 'SKILL.md' conventions/commit/hooks/validate.sh && grep -q 'user-invocable: false' conventions/commit/SKILL.md && bash conventions/commit/hooks/test.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - conventions/commit/SKILL.md exists with frontmatter containing `name: cckit-commit`
    - conventions/commit/SKILL.md frontmatter contains `user-invocable: false`
    - conventions/commit/SKILL.md frontmatter contains `description:` with "Commit message format"
    - conventions/commit/SKILL.md body content is identical to former CONVENTION.md body (Format, Scope, Subject Line, Body, No Project Management References, Breaking Changes, Issue References, Atomic Commits)
    - conventions/commit/CONVENTION.md does NOT exist (deleted)
    - conventions/commit/hooks/validate.sh discovery checks 4 paths in order: SKILL.md, CONVENTION.md, skills/, rules/
    - conventions/commit/hooks/validate.sh uses `for path in` loop pattern (not if/elif)
    - conventions/commit/hooks/test.sh runs with 14+ tests passing (exit code 0)
    - conventions/commit/hooks/test.sh contains comment about SKILL.md discovery
  </acceptance_criteria>
  <done>Commit convention migrated from CONVENTION.md to SKILL.md with skill frontmatter. Hook discovery uses unified 4-path pattern. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Update step-update with hook sync, skill-type handling, and .state/ integration</name>
  <files>skills/convention/step-update.md</files>
  <read_first>
    - skills/convention/step-update.md (current update step -- full file)
    - skills/convention/step-generate.md (from Plan 03 -- the hook integration and .state/ patterns to mirror)
    - agents/convention-generator.md (from Plan 03 -- verify input contract for hook regeneration dispatch)
    - .planning/phases/19.1-convention-skill-improvements/19.1-CONTEXT.md (D-17, D-14, D-01)
    - .planning/phases/19.1-convention-skill-improvements/19.1-RESEARCH.md (modified skill step flow, architecture patterns)
  </read_first>
  <action>
Update skills/convention/step-update.md with these specific changes:

**Step 1: Read Existing Convention -- add artifact type detection:**

After reading the existing convention file, add:
```markdown
Detect the artifact type from the file:
- If `resolved_path` ends with `SKILL.md`: `artifact_type = "skill"`
- If `resolved_path` ends with `CONVENTION.md`: `artifact_type = "rule"`
- If `resolved_path` is a consumer path (`.claude/rules/`): `artifact_type = "rule"`
- If `resolved_path` is a consumer path (`.claude/skills/`): `artifact_type = "skill"`

Store `artifact_type` for use in generator dispatch.
```

**Step 1 -- add hook detection:**

After the artifact type detection, add:
```markdown
Check if hooks exist for this convention area:

**Publisher mode:**
```bash
ls conventions/{area}/hooks/validate.sh 2>/dev/null && echo "HOOKS_EXIST" || echo "NO_HOOKS"
```

**Consumer mode:**
```bash
ls .claude/hooks/cckit-{area}-validate.sh 2>/dev/null && echo "HOOKS_EXIST" || echo "NO_HOOKS"
```

Store `hooks_exist` (true/false).

If hooks exist, display a note:
```
This convention has enforcement hooks at {hook_path}. When you update the convention,
hooks and tests will be automatically regenerated to match the new rules.
```
```

**Step 2: Update Strategy Choice -- update context text:**

Update the "Full rewrite" description to mention hook regeneration:
```
Full rewrite: Re-research the area, re-collect preferences, and generate a
completely new version. The delta test is re-validated. Best when conventions
have drifted significantly or you want a fresh start with current best practices.
{if hooks_exist:}Hooks and tests will be regenerated alongside the convention.{end if}
```

Update the "Surgical edit" description similarly:
```
Surgical edit: Modify specific rules only. Add new rules, change existing ones,
or remove rules you no longer want. Best for targeted changes with minimal
disruption to existing rules.
{if hooks_exist:}Hooks will be regenerated after edits to stay in sync.{end if}
```

**Step 3a: Full Rewrite Path -- add artifact_type and hook context pass-through:**

In both "Research-first" and "Preferences-first" pass-forward lists, add:
```
- `artifact_type` -- "rule" or "skill" (detected from existing file)
- `hooks_exist` -- boolean
- `state_dir` -- path to .state/ directory (if .state/ was initialized)
```

Add note: "The full rewrite path passes through step-research and step-preferences which will initialize .state/ and collect hook confirmation. step-generate handles hook regeneration as part of its standard flow when hook_confirmed is true."

**Step 4b: Dispatch Generator for Surgical Edit -- add hook regeneration:**

After the existing surgical-edit generator dispatch, add a new section:

```markdown
**Step 4c: Hook Regeneration on Surgical Edit**

If `hooks_exist` is true:

After the surgical edit generator returns successfully, re-dispatch the generator 
to regenerate hooks. The generator needs the updated convention content to generate 
hooks that match the new rules.

Present in conversation text:
```
Updating enforcement hooks to match the edited convention...
```

Read the hook recommendation data. If the convention was previously generated with 
research data, read `{state_dir}/research.md` if available. Otherwise, identify 
[HOOK:yes] rules from the updated convention by scanning for rules that are 
mechanically verifiable (format patterns, naming rules, structural requirements).

Dispatch the generator:
```
Agent(
  subagent_type: "convention-generator",
  prompt: "<objective>
Regenerate the enforcement hook and test suite for the '{area}' convention 
to match the updated convention rules.
</objective>

<convention_spec>
Read docs/CONVENTIONS.md for the authoritative convention file format specification.
</convention_spec>

<artifact_type>{artifact_type}</artifact_type>

<hook_confirmed>true</hook_confirmed>
<hook_rules>{list of mechanically verifiable rules from the updated convention}</hook_rules>
<hook_trigger>{original trigger event -- read from existing validate.sh case pattern}</hook_trigger>

<existing_content>
{updated convention file content after surgical edit}
</existing_content>

<output_path>{resolved_path}</output_path>
<mode>hook-regenerate</mode>",
  run_in_background: false
)
```

If hook regeneration fails:
```
AskUserQuestion: "Hook regeneration failed. Hooks may be out of sync." with options:
1. "Retry hook regeneration"
2. "Skip hooks (update convention only)"
```

Skip option leaves the old hooks in place. Note in output that hooks may be 
out of sync with convention rules.
```

**Step 5b: Diff Preview and Approval -- add hook status:**

In the change report display, add:
```
{if hooks_exist and hook regenerated:}
Hook status: Regenerated to match updated rules
- Hookable rules: {list}
- Test fixtures: {count} updated
{end if}
{if hooks_exist and hook NOT regenerated:}
Hook status: NOT updated -- may be out of sync with convention changes
{end if}
```

**Step 5b -- add hook preview option:**

After "Approve changes" option and before the transition, add:
```
If hooks were regenerated, optionally show hook diff:
```
{hook changes}
```

But do NOT add a separate approval gate for hooks -- the hook approval is bundled 
with the convention approval. Hooks are artifacts of the convention, not independent.
```

**Transition section -- update completion display:**

Update to:
```markdown
When the user approves the changes:

Display: "Convention updated at {resolved_path}."

{if hooks_exist and hooks regenerated:}
Display: "Hooks regenerated at {hook_path}."
Display: "Run `bash {test_path}` to verify hook behavior."
{end if}

{if hooks_exist and hooks NOT regenerated:}
Display: "Warning: Hooks at {hook_path} were not regenerated and may be out of sync."
{end if}

Done. Update complete.
```

**Note on create_base_first** -- keep the existing note unchanged.
  </action>
  <verify>
    <automated>grep -q 'hook' skills/convention/step-update.md && grep -q 'artifact_type' skills/convention/step-update.md && grep -q 'Hook Regeneration' skills/convention/step-update.md && grep -q 'hooks_exist' skills/convention/step-update.md && grep -q 'Step 4c' skills/convention/step-update.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - skills/convention/step-update.md Step 1 detects artifact_type from file path
    - skills/convention/step-update.md Step 1 checks for existing hooks and stores hooks_exist boolean
    - skills/convention/step-update.md Step 1 displays note about automatic hook regeneration when hooks exist
    - skills/convention/step-update.md Step 2 strategy descriptions mention hook regeneration when hooks exist
    - skills/convention/step-update.md Step 3a passes artifact_type and hooks_exist to rewrite flow
    - skills/convention/step-update.md contains "Step 4c: Hook Regeneration on Surgical Edit" section
    - skills/convention/step-update.md Step 4c dispatches generator with hook_confirmed, hook_rules, hook_trigger, mode=hook-regenerate
    - skills/convention/step-update.md Step 4c handles hook regeneration failure with retry/skip options
    - skills/convention/step-update.md Step 5b shows hook status in change report
    - skills/convention/step-update.md Transition displays hook regeneration result
  </acceptance_criteria>
  <done>step-update detects artifact type, checks for existing hooks, auto-regenerates hooks on surgical edit (D-17), passes artifact type through full rewrite flow, and reports hook sync status</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hook script execution | validate.sh runs as PreToolUse hook with JSON from Claude Code runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19.1-08 | Tampering | validate.sh discovery path | mitigate | Unified discovery constructs paths with $CWD prefix (absolute), uses for-loop with explicit path list. No user-controlled path components beyond CWD which is provided by Claude Code runtime. |
| T-19.1-09 | Tampering | SKILL.md content injection | accept | Convention content is injected via jq -Rs which properly escapes all special characters. Same pattern as previous CONVENTION.md injection. No change in security posture. |
| T-19.1-10 | Denial of Service | Hook script performance | accept | Unified discovery checks 4 paths via `test -f` (filesystem stat). Negligible performance impact vs previous 2-path check. |
</threat_model>

<verification>
1. `test -f conventions/commit/SKILL.md` -- migrated convention exists
2. `! test -f conventions/commit/CONVENTION.md` -- old file removed
3. `grep -q 'SKILL.md' conventions/commit/hooks/validate.sh` -- unified discovery in hook
4. `bash conventions/commit/hooks/test.sh` -- all tests pass (exit 0)
5. `grep -q 'Hook Regeneration' skills/convention/step-update.md` -- hook sync in step-update
6. `grep -q 'artifact_type' skills/convention/step-update.md` -- skill-type handling in step-update
</verification>

<success_criteria>
- Commit convention fully migrated: SKILL.md with skill frontmatter, CONVENTION.md deleted
- Hook discovery uses unified 4-path pattern, finds SKILL.md first
- All 14+ existing hook tests pass after migration
- step-update auto-regenerates hooks when convention is updated (D-17)
- step-update handles both rule-type and skill-type convention updates
</success_criteria>

<output>
After completion, create `.planning/phases/19.1-convention-skill-improvements/19.1-04-SUMMARY.md`
</output>
