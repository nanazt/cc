---
phase: 12.1-case-example-rewrite
plan: 02
type: execute
wave: 2
depends_on: [12.1-01]
files_modified:
  - skills/case/step-finalize.md
  - agents/case-briefer.md
  - agents/case-validator.md
  - skills/case/README.md
autonomous: true
requirements: [CASE-01]

must_haves:
  truths:
    - "step-finalize.md error consistency table uses error names or checkmarks instead of HTTP status codes"
    - "step-finalize.md Expected Outcome guidance teaches structural rule instead of protocol-specific examples"
    - "step-finalize.md uses Caller: instead of Auth Required:"
    - "step-finalize.md side effects section in output format uses open format instead of fixed categories"
    - "case-briefer.md contains no JWT, ceremony, endpoint, or API-specific examples"
    - "case-validator.md Language Pattern Signals use structural placeholder examples"
    - "README.md is deleted"
  artifacts:
    - path: "skills/case/step-finalize.md"
      provides: "Neutralized output format template with structural Expected Outcome guidance"
    - path: "agents/case-briefer.md"
      provides: "Neutralized inline examples in extraction guidance"
    - path: "agents/case-validator.md"
      provides: "Neutralized Language Pattern Signals examples"
  key_links:
    - from: "skills/case/step-finalize.md"
      to: "skills/case/step-discuss.md"
      via: "Consistent error naming structural rule and side effect open format"
      pattern: "\\[error description\\] \\(ErrorName\\)"
    - from: "agents/case-validator.md"
      to: "skills/case/SKILL.md"
      via: "Consistent placeholder conventions"
      pattern: "\\[status/error code\\]|all operations"
---

<objective>
Neutralize all technology-biased content in step-finalize.md, case-briefer.md, case-validator.md, and delete README.md — completing the full /case technology neutralization.

Purpose: step-finalize.md's output format template and Expected Outcome guidance directly shape what CASES.md looks like. The agent files guide extraction and validation behavior. All must use the same structural placeholder conventions established in Plan 01's SKILL.md and step-discuss.md changes.

Output: Three neutralized Markdown files + one deleted file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-case-example-rewrite/12.1-CONTEXT.md
@.planning/phases/12.1-case-example-rewrite/12.1-RESEARCH.md
@.planning/phases/12.1-case-example-rewrite/12.1-01-SUMMARY.md

<interfaces>
<!-- Structural placeholder conventions established by Plan 01 in SKILL.md and step-discuss.md.
     The executor should verify Plan 01 is complete and cross-reference these conventions. -->

From skills/case/SKILL.md (after Plan 01):
- Global adaptation instruction exists in <philosophy> section
- Caller: [who/what can invoke this] (replaces Auth: field)
- [error outcome (ErrorName)] (failure branch placeholder in flow diagrams)
- [OperationName] (operation name placeholder)

From skills/case/step-discuss.md (after Plan 01):
- Error naming structural rule: `[error description] (ErrorName)` — omit parenthetical only when intentionally opaque
- Side effects: open format with `- SE_: [describe each observable side effect]` — no fixed categories
- `(ErrorName)` is always domain-level — names the error identity for code-level mapping
- Specificity criterion: "be specific enough that a test can assert against it"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neutralize step-finalize.md — output format, error table, Expected Outcome, Caller field, side effects</name>
  <files>skills/case/step-finalize.md</files>
  <read_first>
    - skills/case/step-finalize.md (current state — all biased sites to locate)
    - skills/case/step-discuss.md (verify Plan 01 changes: error naming rule wording and side effect open format — must be consistent)
    - skills/case/SKILL.md (verify Plan 01 changes: Caller: field name)
    - .planning/phases/12.1-case-example-rewrite/12.1-CONTEXT.md (locked decisions D-05, D-06, D-07, D-09, D-13, D-14)
    - .planning/phases/12.1-case-example-rewrite/12.1-RESEARCH.md (detailed change inventory + open questions 2, 3)
  </read_first>
  <action>
Apply changes to step-finalize.md. Read the file first to find exact current line positions.

**Change 1 — Line ~9: `Ceremony` in error category list (D-14):**
The error consistency table header guidance says: `derive error categories from the actual cases discussed (e.g., Auth, Validation, Infra, Ceremony, Conflict, NotFound, etc.)`
Replace `Ceremony` with a neutral category placeholder. Per RESEARCH open question 3: Auth, Validation, Infra, Conflict, NotFound are not protocol-specific — they are derived from actual discussed cases. Only `Ceremony` (WebAuthn-specific) needs replacement. Replace with a neutral example like `State` or simply remove it from the list.
Result: `(e.g., Auth, Validation, Infra, Conflict, NotFound, etc.)`

**Change 2 — Lines ~11-15: Error consistency table cell values (D-06):**
The example table has `400`, `401`, `500` as cell values. Replace with error name placeholders or checkmarks:
```
| Operation | [Category A] | [Category B] | ... | Infra |
|-----------|-------------|-------------|-----|-------|
| OpA       | (ErrorName) | (ErrorName) | ... | (ErrorName) |
| OpB       | --          | (ErrorName) | ... | (ErrorName) |
```
The point of the table is to compare error handling consistency across operations — checkmarks or error names serve this purpose without protocol assumptions.

**Change 3 — Lines ~258-260: `Auth required: [none / authenticated / role:X]` (D-13):**
In the output format Operation header section, replace:
`**Auth required:** [none / authenticated / role:X]`
with:
`**Caller:** [who/what can invoke this]`
This aligns with SKILL.md's Caller: field (D-12) established in Plan 01.

**Change 4 — Line ~271: Side Effects section in output format (RESEARCH open question 2):**
The output format template has:
```
### Side Effects

- Domain event: "[entity].[action]" emitted on success
- [category]: [description]
```
Replace with open format consistent with step-discuss.md's D-08 change:
```
### Side Effects

- [describe each observable side effect]
```
Keep the `> If the operation has no side effects, write: \`None (read-only)\` or \`None (query operation)\`.` line.

**Change 5 — Line ~383: `"Success; 'entity.created' event emitted"` (D-07):**
Replace with structural guidance about Expected Outcome for success cases. The replacement should convey the "result + state change + side effects" pattern at a level tests can assert (D-02). Example:
`Success cases: describe the primary result, any state changes, AND any side effects that occurred — all at a level a test can assert against.`

**Change 6 — Line ~384: `"400 bad request (ValidationError)"`, `"401 unauthorized (TokenExpired)"`, `"500 internal error (DatabaseUnavailable)"` (D-05, D-09):**
Replace all three protocol-specific examples with the structural rule, consistent with step-discuss.md:
`Failure cases: use the structural rule \`[error description] (ErrorName)\`. The error description uses terms natural to the operation's domain. \`(ErrorName)\` is always domain-level. Omit the parenthetical only when the error is intentionally opaque by design. Assert side effects DID NOT occur where relevant.`

This is an inline adaptation annotation site per D-19 — Expected Outcome guidance is one of the two non-obvious sites.

**Post-edit verification:** Read the full file. Verify consistency with step-discuss.md's error naming rule wording. Verify the Caller: field name matches SKILL.md. Check for any remaining biased terms per the bias definition (D-15).
  </action>
  <verify>
    <automated>grep -nE '(400 bad request|401 unauthorized|500 internal error|Ceremony|entity\.created|Auth required:)' skills/case/step-finalize.md | wc -l | grep -q '^0$' && echo "PASS: no biased terms" || echo "FAIL: biased terms remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Ceremony' skills/case/step-finalize.md` returns 0
    - `grep -c '400' skills/case/step-finalize.md` returns 0 (no HTTP status codes in table cells or examples)
    - `grep -c '401' skills/case/step-finalize.md` returns 0
    - `grep -c '500' skills/case/step-finalize.md` returns 0
    - `grep -c 'entity.created' skills/case/step-finalize.md` returns 0
    - `grep -c 'Auth required:' skills/case/step-finalize.md` returns 0 (replaced with Caller:)
    - `grep -c 'Caller:' skills/case/step-finalize.md` returns >= 1
    - `grep -c '\[error description\] (ErrorName)' skills/case/step-finalize.md` returns >= 1 (structural rule present)
    - `grep -c 'Domain event:' skills/case/step-finalize.md` returns 0 (fixed side effect category removed from output format)
    - `grep -c 'TokenExpired' skills/case/step-finalize.md` returns 0
    - `grep -c 'ValidationError' skills/case/step-finalize.md` returns 0 (as a concrete example — the structural rule `(ErrorName)` is fine)
    - `grep -c 'DatabaseUnavailable' skills/case/step-finalize.md` returns 0
  </acceptance_criteria>
  <done>step-finalize.md output format uses Caller: instead of Auth Required:, error consistency table uses error names instead of HTTP status codes, Expected Outcome guidance teaches structural rule instead of protocol-specific examples, side effects section uses open format. All 6 changes applied consistently with Plan 01's conventions.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize case-briefer.md — inline examples across 7 biased sites</name>
  <files>agents/case-briefer.md</files>
  <read_first>
    - agents/case-briefer.md (current state — all 7 biased sites to locate)
    - .planning/phases/12.1-case-example-rewrite/12.1-CONTEXT.md (locked decisions D-14, D-15, D-17)
    - .planning/phases/12.1-case-example-rewrite/12.1-RESEARCH.md (detailed change inventory for case-briefer.md)
  </read_first>
  <action>
Apply 7 changes to case-briefer.md. Read the file first to find exact current line positions.

**Change 1 — Line ~34: `"all endpoints require authentication"` (D-14, D-17):**
Replace `"all endpoints require authentication"` with `"[system-wide access policy]"`
Context: This is in Step 1 deep extraction guidance about authentication policy.

**Change 2 — Line ~37: `"API conventions -- URL structure, naming patterns, error handling conventions"` (D-14):**
Replace with: `"Interface conventions -- [project-specific patterns, naming conventions, error handling approach]"`
The heading itself (`API conventions`) is the biased part — "Interface conventions" is universal. The sub-items (URL structure is HTTP-specific) become structural placeholders.

**Change 3 — Line ~87: `"session/ceremony constraints"` (D-14):**
Replace with: `"[cross-operation interaction constraints]"`
Context: This is in Step 4.5 PR-candidate description — the example of what constitutes a phase-wide constraint.

**Change 4 — Line ~96: `"User.CreateUser"`, `"Auth.ValidateSession"` (D-14):**
Replace with: `"[Component.OperationName]"` (a single structural placeholder showing the pattern)
Context: Step 4.6 cross-reference example. The current two examples are web-app-specific operations. One structural placeholder communicates the format.

**Change 5 — Line ~117: `"all endpoints require authentication"` (D-14, D-17):**
Replace with: `"[system-wide access policy]"`
Context: Step 4.7 implicit dependency detection example (same text as Change 1 but different location).

**Change 6 — Line ~129: `"verify JWT claims freshness"` (D-14):**
Replace with: `"[specific behavioral concern from dependency phase]"`
Context: Inherited concerns classification example (behavioral type).

**Change 7 — Line ~130: `"ceremony logic should be reusable"` (D-14):**
Replace with: `"[implementation constraint from dependency phase]"`
Context: Inherited concerns classification example (constraint type).

**Post-edit verification:** Read the full file. Check for any remaining biased terms per D-15 bias definition. The rest of the file (agent contract, output format, quality gate) should be clean from Phase 12's term replacements.
  </action>
  <verify>
    <automated>grep -nE '(endpoint|JWT|ceremony|session/ceremony|API conventions|URL structure|CreateUser|ValidateSession)' agents/case-briefer.md | wc -l | grep -q '^0$' && echo "PASS: no biased terms" || echo "FAIL: biased terms remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'all endpoints' agents/case-briefer.md` returns 0 (both sites replaced)
    - `grep -c 'JWT' agents/case-briefer.md` returns 0
    - `grep -c 'ceremony' agents/case-briefer.md` returns 0
    - `grep -c 'API conventions' agents/case-briefer.md` returns 0 (replaced with Interface conventions)
    - `grep -c 'URL structure' agents/case-briefer.md` returns 0
    - `grep -c 'CreateUser' agents/case-briefer.md` returns 0
    - `grep -c 'ValidateSession' agents/case-briefer.md` returns 0
    - `grep -c 'Interface conventions' agents/case-briefer.md` returns >= 1 (replacement present)
    - `grep -c '\[Component.OperationName\]' agents/case-briefer.md` returns >= 1 (structural placeholder)
    - `grep -c '\[system-wide access policy\]' agents/case-briefer.md` returns >= 1 (structural placeholder)
  </acceptance_criteria>
  <done>case-briefer.md has all 7 biased inline examples replaced with structural placeholders. No JWT, ceremony, endpoint, API-specific, or web-app operation examples remain.</done>
</task>

<task type="auto">
  <name>Task 3: Neutralize case-validator.md + delete README.md</name>
  <files>agents/case-validator.md, skills/case/README.md</files>
  <read_first>
    - agents/case-validator.md (current state — all 4 biased areas to locate)
    - skills/case/README.md (confirm deletion target — 95 lines)
    - .planning/phases/12.1-case-example-rewrite/12.1-CONTEXT.md (locked decisions D-14, D-16, D-17, D-21)
    - .planning/phases/12.1-case-example-rewrite/12.1-RESEARCH.md (detailed change inventory for case-validator.md)
  </read_first>
  <action>
**Part A: Neutralize case-validator.md (4 changes)**

Read the file first to find exact current line positions.

**Change 1 — Lines ~74-75: `"cookie MUST be HttpOnly"` (D-14):**
Replace with: `"[property] MUST [constraint]"`
Context: This is in the classification decision tree, step 4 — architectural constraint example. The structural placeholder preserves the MUST-constraint pattern without technology specifics.

**Change 2 — Line ~81: `"Redis key pattern"`, `"JSONB column"`, `"PEM format keys"` (D-14):**
Replace with: `"[storage key pattern]"`, `"[data column format]"`, `"[key format]"`
Context: Classification decision tree step 5 — configuration/storage/transport examples.

**Change 3 — Line ~91: `"returns 401"`, `"responds with 204"` (D-17):**
Replace with: `"returns [status/error code]"`, `"responds with [status code]"`
Context: Language Pattern Signals — Behavioral signals category. Per D-17, the `"Explicit status codes"` category name is retained (status codes exist across protocols — CLI exit codes, HTTP status codes, gRPC codes). Only the illustrative examples change.

**Change 4 — Line ~98: `"all endpoints"` (D-17):**
Replace with: `"all operations"`
Context: Language Pattern Signals — Architectural constraint signals. Simple term replacement.

Per D-16: the Language Pattern Signals category structure (Behavioral signals, Architectural constraint signals) is kept intact. Only the inline examples within each category are neutralized.

**Post-edit verification:** Read the full file. Verify category structure unchanged. Check for any remaining biased terms per D-15.

**Part B: Delete README.md (D-21)**

Delete the file entirely:
```bash
rm skills/case/README.md
```

Per D-21: README.md is user-facing documentation, not a system prompt consumed by Claude. No functional impact on /case behavior. Full rewrite deferred to a future phase.
  </action>
  <verify>
    <automated>grep -nE '(HttpOnly|cookie MUST|Redis|JSONB|PEM format|returns 401|responds with 204|all endpoints)' agents/case-validator.md | wc -l | grep -q '^0$' && test ! -f skills/case/README.md && echo "PASS: validator clean + README deleted" || echo "FAIL: issues remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'HttpOnly' agents/case-validator.md` returns 0
    - `grep -c 'cookie MUST' agents/case-validator.md` returns 0
    - `grep -c 'Redis' agents/case-validator.md` returns 0
    - `grep -c 'JSONB' agents/case-validator.md` returns 0
    - `grep -c 'PEM format' agents/case-validator.md` returns 0
    - `grep -c 'returns 401' agents/case-validator.md` returns 0
    - `grep -c 'responds with 204' agents/case-validator.md` returns 0
    - `grep -c 'all endpoints' agents/case-validator.md` returns 0
    - `grep -c 'returns \[status/error code\]' agents/case-validator.md` returns >= 1 (structural placeholder)
    - `grep -c 'all operations' agents/case-validator.md` returns >= 1 (neutral replacement)
    - `grep -c '\[property\] MUST \[constraint\]' agents/case-validator.md` returns >= 1
    - `test ! -f skills/case/README.md` exits 0 (file deleted)
  </acceptance_criteria>
  <done>case-validator.md Language Pattern Signals and classification decision tree examples neutralized. Category structure intact per D-16. README.md deleted. All 4 validator changes + 1 deletion applied.</done>
</task>

</tasks>

<verification>
After all tasks complete, run the comprehensive bias audit across ALL /case files:

```bash
echo "=== Full bias audit ==="
grep -rnE '(400|401|500|JWT|SMTP|Redis|cookie|endpoint|middleware|ceremony|passkey|WebAuthn|gRPC|JSONB|PEM|HttpOnly)' skills/case/SKILL.md skills/case/step-discuss.md skills/case/step-finalize.md agents/case-briefer.md agents/case-validator.md 2>/dev/null
echo "=== README deletion ==="
test ! -f skills/case/README.md && echo "README.md deleted" || echo "README.md still exists"
echo "=== Caller field consistency ==="
grep -rn 'Auth:' skills/case/SKILL.md skills/case/step-finalize.md | grep -v 'Auth failure\|auth\|Auth.*section\|authentication\|Auth.*Required' || echo "No Auth: field labels remain"
echo "=== Structural rule consistency ==="
grep -rn '\[error description\] (ErrorName)' skills/case/step-discuss.md skills/case/step-finalize.md
echo "=== Global adaptation instruction ==="
grep -n 'adapt.*host project' skills/case/SKILL.md
```

Expected:
- Full bias audit: zero matches
- README deletion: confirmed
- No Auth: field labels remain (Auth: as Interface field is replaced with Caller:)
- Structural rule appears in both step-discuss.md and step-finalize.md
- Global adaptation instruction present in SKILL.md
</verification>

<success_criteria>
1. step-finalize.md output format uses Caller: field, error names in consistency table, structural Expected Outcome guidance, open side effects format
2. case-briefer.md contains zero JWT, ceremony, endpoint, API-specific, or web-app operation examples
3. case-validator.md Language Pattern Signals use structural placeholder examples while category structure remains intact
4. README.md is deleted
5. The comprehensive cross-file bias audit grep returns zero matches
6. Error naming structural rule `[error description] (ErrorName)` is consistent between step-discuss.md and step-finalize.md
7. Caller: field name is consistent between SKILL.md and step-finalize.md
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-case-example-rewrite/12.1-02-SUMMARY.md`
</output>
