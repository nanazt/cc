---
phase: 12.1-case-example-rewrite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/case/SKILL.md
  - skills/case/step-discuss.md
autonomous: true
requirements: [CASE-01]

must_haves:
  truths:
    - "SKILL.md contains a global adaptation instruction directing Claude to adapt all structural placeholders to the host project's actual interfaces, protocols, and terminology"
    - "SKILL.md canonical flow diagram uses structural placeholders instead of HTTP status codes"
    - "SKILL.md canonical example uses Caller: instead of Auth:"
    - "step-discuss.md error naming block teaches the structural rule, not protocol-specific examples"
    - "step-discuss.md side effect probing uses open format instead of 6 fixed categories"
    - "All biased inline examples in both files are replaced with structural placeholders"
  artifacts:
    - path: "skills/case/SKILL.md"
      provides: "Global adaptation instruction + neutralized canonical flow diagram"
      contains: "adapt.*host project"
    - path: "skills/case/step-discuss.md"
      provides: "Neutralized error naming block + open side effect format"
  key_links:
    - from: "skills/case/SKILL.md"
      to: "skills/case/step-discuss.md"
      via: "Consistent structural placeholder conventions"
      pattern: "\\[error outcome \\(ErrorName\\)\\]|\\[error description\\] \\(ErrorName\\)"
---

<objective>
Neutralize all technology-biased content in SKILL.md and step-discuss.md — the two files that establish the placeholder conventions and contain the most complex changes (side effect category overhaul, error naming block replacement).

Purpose: These two files set the canonical structural placeholder vocabulary that all other /case files must align with. SKILL.md's global adaptation instruction (D-19) is the authoritative statement; step-discuss.md contains the largest single change (D-08 side effect overhaul) and the critical error naming structural rule (D-10, D-11).

Output: Two neutralized Markdown files with all technology-biased examples replaced by structural placeholders.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12.1-case-example-rewrite/12.1-CONTEXT.md
@.planning/phases/12.1-case-example-rewrite/12.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Neutralize SKILL.md — global adaptation instruction + canonical flow + inline examples</name>
  <files>skills/case/SKILL.md</files>
  <read_first>
    - skills/case/SKILL.md (current state — all biased sites to locate)
    - .planning/phases/12.1-case-example-rewrite/12.1-CONTEXT.md (locked decisions D-01 through D-21)
    - .planning/phases/12.1-case-example-rewrite/12.1-RESEARCH.md (detailed change inventory for SKILL.md)
  </read_first>
  <action>
Apply 7 changes to SKILL.md. Read the file first to find exact current line positions (Phase 12 may have shifted line numbers from CONTEXT.md references).

**Change 1 — Global adaptation instruction (D-19):**
Add an authoritative statement inside `<philosophy>` (after the existing content, before the closing tag) or as a new paragraph within `<philosophy>`. The statement must convey:
- All examples in this skill use structural placeholders
- Claude must adapt content to the host project's actual interfaces, protocols, and terminology
- The skill teaches format and quality bar, not domain-specific content

Exact wording is Claude's discretion, but MUST contain the phrase "host project" and convey the adapt-to-project directive. This is the single global instruction per D-19.

**Change 2 — Line ~41: `"What if the token is expired?"`  (D-14):**
Replace with: `"What if the [credential/precondition] is [invalid state]?"`

**Change 3 — Line ~42: `"Should we use a middleware for this?"`  (D-14):**
Replace with: `"Should we use [implementation approach] for this?"`
(This is in the "Not allowed" list, so the structural placeholder is appropriate — it shows what NOT to ask regardless of technology.)

**Change 4 — Line ~50: `"use a parameterized function for two middleware variants"` (D-14):**
Replace with a structural placeholder like: `"[implementation approach for a specific design decision]"`

**Change 5 — Line ~57: `"Logout cases review. Missing anything?"` (D-14):**
Replace with: `"[OperationName] cases review. Missing anything?"`

**Change 6 — Line ~90: `Auth: any authenticated role (standard middleware)` (D-04, D-12):**
Replace with: `Caller: [who/what can invoke this]`

**Change 7 — Lines ~97-103: HTTP status codes in canonical flow diagram (D-04):**
Replace the three failure branch lines:
- `├──► F1: failure case → 400 bad request` becomes `├──► F1: failure case → [error outcome (ErrorName)]`
- `└──► F2: another failure → 401 unauthorized` becomes `└──► F2: another failure → [error outcome (ErrorName)]`
- `└──► F3: failure → 500 internal error` becomes `└──► F3: failure → [error outcome (ErrorName)]`

Maintain ASCII alignment of the flow diagram. The placeholder `[error outcome (ErrorName)]` is longer than the HTTP codes — verify visual alignment is acceptable (lines should not exceed ~90 chars).

**Post-edit verification:** Read the full file and check for any remaining biased terms that should have been caught. The bias definition (D-15): assumes a specific communication protocol, storage mechanism, auth mechanism, architecture pattern, or project type. Leave non-biased content as-is.
  </action>
  <verify>
    <automated>grep -nE '(400 bad request|401 unauthorized|500 internal error|standard middleware|token is expired|use a middleware|middleware variants|Logout cases)' skills/case/SKILL.md | wc -l | grep -q '^0$' && echo "PASS: no biased terms" || echo "FAIL: biased terms remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'adapt.*host project' skills/case/SKILL.md` returns >= 1 (global adaptation instruction exists)
    - `grep -c 'Caller:' skills/case/SKILL.md` returns >= 1 (Auth: replaced with Caller:)
    - `grep -c 'Auth:' skills/case/SKILL.md` returns 0 (no remaining Auth: field in canonical example — note: "Auth" may appear in other contexts like auth/authorization discussion text, which is fine; only the Interface field label must be Caller:)
    - `grep -c '\[error outcome (ErrorName)\]' skills/case/SKILL.md` returns >= 3 (flow diagram failure branches)
    - `grep -c '\[OperationName\] cases review' skills/case/SKILL.md` returns >= 1
    - `grep -c 'middleware' skills/case/SKILL.md` returns 0
    - `grep -c 'token is expired' skills/case/SKILL.md` returns 0
    - `grep -c 'Logout' skills/case/SKILL.md` returns 0
  </acceptance_criteria>
  <done>SKILL.md contains zero technology-biased examples in scope guardrail, canonical flow diagram, and formatting sections. Global adaptation instruction present in philosophy section. All 7 changes applied.</done>
</task>

<task type="auto">
  <name>Task 2: Neutralize step-discuss.md — error naming block, side effects, inline examples</name>
  <files>skills/case/step-discuss.md</files>
  <read_first>
    - skills/case/step-discuss.md (current state — all biased sites to locate)
    - .planning/phases/12.1-case-example-rewrite/12.1-CONTEXT.md (locked decisions D-08, D-09, D-10, D-11, D-14)
    - .planning/phases/12.1-case-example-rewrite/12.1-RESEARCH.md (detailed change inventory + code examples for error naming and side effects)
  </read_first>
  <action>
Apply 6 changes to step-discuss.md. Read the file first to find exact current line positions.

**Change 1 — Line ~27: `"use mock SMTP during development"` (D-14):**
Replace with a structural placeholder: `"[implementation approach that is explicitly temporary]"`

**Change 2 — Line ~47: `RefreshToken R4` (D-14):**
Replace with a structural placeholder: `[OperationName rule-ref]`
(This appears in the inherited concerns example. Replace only the operation name and rule reference, keep the surrounding sentence structure.)

**Change 3 — Lines ~196-214: 6 fixed side effect categories (D-08, most complex change):**
Replace the ENTIRE side effect probing template (all 6 categories: Domain events, Related entity updates, Cache mutations, Audit/logging, Notifications, External system calls) with an open format. The replacement should be:

```
When this operation succeeds, I expect these side effects:

- SE_: [describe each observable side effect]

List all effects beyond the primary return value: state changes in related data,
notifications, external calls, audit records, or any other observable consequence.
Adapt categories to the host project's actual architecture.

Which of these apply? Any I'm missing?
What if the operation partially succeeds then fails -- are side effects rolled back or left in place?
```

IMPORTANT: Keep the section header (3c-vi: Side effects and data integrity) and the text AFTER the categories block (the `Which of these apply?` question and partial success question are already in the current file — merge/deduplicate with the replacement so they appear once). Also keep the `SE_` working label explanation and the note about recording side effects in Expected Outcome column.

**Change 4 — Line ~220: `"Notifications"`, `"internal data migration operation"` (D-14):**
This line says: `Omit categories that clearly do not apply (e.g., skip "Notifications" for an internal data migration operation).`
With the open format replacing fixed categories, this guidance about omitting categories no longer applies. Remove or rephrase to align with the open format. Replacement: `Use SE_ as a working label during discussion; in the final case tables, side effects are recorded in the Expected Outcome column of the relevant S/F/E case, not as a separate category.`
(Note: check if this text already exists elsewhere in the section and deduplicate.)

**Change 5 — Lines ~271-275: Multi-protocol error example block (D-10, D-11):**
Replace the ENTIRE block (the `Examples (the error description...)` paragraph with its 4 bullet points showing HTTP/gRPC/domain/opaque examples) with the structural rule. The replacement:

```
The structural rule for error outcomes: `[error description] (ErrorName)`. Omit the parenthetical only when the error is intentionally opaque by design.

The error description uses terms natural to the operation's domain. `(ErrorName)` is always domain-level -- it names the error identity that maps to a code-level error variant. Be specific enough that a test can assert against it.
```

This is an inline adaptation annotation site per D-19 — the gap between placeholder and correct output is non-obvious here, so the structural rule explanation is warranted.

**Change 6 — Lines ~335-336: `->3B:AddPasskey` (D-14):**
Replace `->3B:AddPasskey` with `->XX:[OperationName]`
This appears in the Forward column values explanation. Replace only the operation name example, keep the structural explanation.

**Change 7 — Line ~312: Side Effects sub-section in CASE-SCRATCH.md template (RESEARCH open question 1):**
The scratch save format has:
```
### Side Effects
- Domain event: "[entity].[action]" with [key fields]
- [other side effects by category]
```
Replace with open format to match the D-08 change:
```
### Side Effects
- [describe each observable side effect]
```
Keep the `> If the operation has no side effects, write: \`None (read-only)\` or \`None (query operation)\`.` line.

**Post-edit verification:** Read the full file and check for any remaining biased terms per the bias definition (D-15). Verify ZOMBIES probing language is untouched per D-18.
  </action>
  <verify>
    <automated>grep -nE '(mock SMTP|Domain events:|Cache mutations:|Audit / logging:|External system calls:|400 Bad Request|PERMISSION_DENIED|AddPasskey)' skills/case/step-discuss.md | wc -l | grep -q '^0$' && echo "PASS: no biased terms" || echo "FAIL: biased terms remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Domain events:' skills/case/step-discuss.md` returns 0 (fixed categories removed)
    - `grep -c 'Related entity updates:' skills/case/step-discuss.md` returns 0
    - `grep -c 'Cache mutations:' skills/case/step-discuss.md` returns 0
    - `grep -c 'Audit / logging:' skills/case/step-discuss.md` returns 0
    - `grep -c 'Notifications:' skills/case/step-discuss.md` returns 0 (as category header — the word "notifications" in open-format guidance text is fine)
    - `grep -c 'External system calls:' skills/case/step-discuss.md` returns 0
    - `grep -c '400 Bad Request' skills/case/step-discuss.md` returns 0
    - `grep -c 'PERMISSION_DENIED' skills/case/step-discuss.md` returns 0
    - `grep -c 'gRPC' skills/case/step-discuss.md` returns 0
    - `grep -c 'mock SMTP' skills/case/step-discuss.md` returns 0
    - `grep -c 'AddPasskey' skills/case/step-discuss.md` returns 0
    - `grep -c '\[error description\] (ErrorName)' skills/case/step-discuss.md` returns >= 1 (structural rule present)
    - `grep -c 'ZOMBIES' skills/case/step-discuss.md` returns >= 1 (ZOMBIES probing untouched per D-18)
    - `grep -c 'Zero.*One.*Many' skills/case/step-discuss.md` returns >= 1 (ZOMBIES progression untouched)
  </acceptance_criteria>
  <done>step-discuss.md error naming block replaced with structural rule. All 6 fixed side effect categories replaced with open format. All biased inline examples replaced with structural placeholders. ZOMBIES probing language unchanged. CASE-SCRATCH.md template side effects aligned with open format.</done>
</task>

</tasks>

<verification>
After both tasks complete, run a comprehensive bias audit across both files:

```bash
grep -nE '(400|401|500|JWT|SMTP|Redis|cookie|endpoint|middleware|ceremony|passkey|WebAuthn|gRPC|JSONB|PEM)' skills/case/SKILL.md skills/case/step-discuss.md
```

Expected: zero matches. If any matches exist, verify they are in non-biased context (e.g., "endpoint" in a truly neutral usage) per D-15 bias definition.

Also verify the global adaptation instruction:
```bash
grep -n 'adapt.*host project' skills/case/SKILL.md
```
Expected: at least one match.
</verification>

<success_criteria>
1. SKILL.md has zero technology-biased examples in scope guardrail, canonical flow diagram, and formatting sections
2. SKILL.md contains a global adaptation instruction in the philosophy section
3. step-discuss.md error naming section teaches the structural rule `[error description] (ErrorName)` without protocol-specific examples
4. step-discuss.md side effect probing uses open format without fixed categories
5. All biased inline examples across both files replaced with structural placeholders
6. Non-biased content and ZOMBIES probing language are untouched
7. The comprehensive bias audit grep returns zero matches
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-case-example-rewrite/12.1-01-SUMMARY.md`
</output>
