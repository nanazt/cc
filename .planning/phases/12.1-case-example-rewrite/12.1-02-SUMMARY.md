---
phase: 12.1-case-example-rewrite
plan: 02
subsystem: case-skill
tags: [neutralization, structural-placeholders, technology-neutral]
dependency_graph:
  requires: [12.1-01-SUMMARY.md]
  provides: [neutralized-step-finalize, neutralized-case-briefer, neutralized-case-validator, README-deleted]
  affects: [skills/case/step-finalize.md, agents/case-briefer.md, agents/case-validator.md, skills/case/README.md]
tech_stack:
  added: []
  patterns: [structural-placeholder-convention, open-format-replacement, structural-rule-teaching]
key_files:
  modified:
    - skills/case/step-finalize.md
    - agents/case-briefer.md
    - agents/case-validator.md
  deleted:
    - skills/case/README.md
key_decisions:
  - step-finalize.md error consistency table uses (ErrorName) structural placeholders instead of HTTP status codes — comparison logic stays valid across all protocols
  - Expected Outcome guidance teaches structural rule [error description] (ErrorName) consistent with step-discuss.md — no protocol-specific examples in the output format template
  - Auth required: field replaced with Caller: [who/what can invoke this] — aligns with SKILL.md canonical flow and step-discuss.md Guide field
  - Domain event fixed side effect category replaced with open format [describe each observable side effect] — consistent with step-discuss.md open format
  - case-briefer API conventions heading replaced with Interface conventions — universal across REST, RPC, CLI, event-driven, and any other interface style
  - case-validator Language Pattern Signals use structural placeholder examples while category structure (Behavioral/Architectural constraint signals) remains intact
  - README.md deleted entirely — user docs not Claude system prompt, functional impact zero, rewrite deferred
metrics:
  duration_seconds: 300
  completed_date: "2026-04-01"
  tasks_completed: 3
  files_modified: 3
  files_deleted: 1
---

# Phase 12.1 Plan 02: step-finalize, case-briefer, case-validator, README Neutralization Summary

**One-liner:** Completed full /case technology neutralization — output format template, error consistency table, Expected Outcome guidance, and all inline examples in case-briefer and case-validator now use structural placeholders; README.md deleted.

## What Was Done

### Task 1: step-finalize.md — 6 changes applied

1. **Error category list (D-14):** Removed `Ceremony` from the error consistency table category example list (WebAuthn-specific). Remaining examples (Auth, Validation, Infra, Conflict, NotFound) are universal and derived from actual discussed cases.

2. **Error consistency table cell values (D-06):** Replaced `400`, `401`, `500` HTTP status code cell values with `(ErrorName)` structural placeholders. The table's purpose is to compare error handling consistency across operations — error names serve this equally well without protocol assumptions. Updated the flag description from "different status codes" to "inconsistent error handling."

3. **Auth required: field (D-13):** Replaced `**Auth required:** [none / authenticated / role:X]` with `**Caller:** [who/what can invoke this]` in the output format Operation header. Aligns with SKILL.md canonical flow (Plan 01) and step-discuss.md Guide field convention.

4. **Side Effects section in output format (D-08):** Replaced fixed `- Domain event: "[entity].[action]" emitted on success` and `- [category]: [description]` with open format `- [describe each observable side effect]`. Consistent with step-discuss.md's open format change from Plan 01.

5. **Success Expected Outcome guidance (D-07):** Replaced `"Success; 'entity.created' event emitted"` example with structural guidance: "describe the primary result, any state changes, AND any side effects that occurred — all at a level a test can assert against."

6. **Failure Expected Outcome guidance (D-05, D-09):** Replaced three HTTP-specific examples (`"400 bad request (ValidationError)"`, `"401 unauthorized (TokenExpired)"`, `"500 internal error (DatabaseUnavailable)"`) with the structural rule `[error description] (ErrorName)` with specificity criterion. Wording matches step-discuss.md exactly (D-10 consistency).

### Task 2: case-briefer.md — 7 changes + 1 output template fix applied

1. **Line 34: Authentication policy example (D-14, D-17):** `"all endpoints require authentication"` → `"[system-wide access policy]"` (Step 1 extraction guidance)

2. **Line 37: API conventions heading (D-14):** `"API conventions -- URL structure, naming patterns, error handling conventions"` → `"Interface conventions -- [project-specific patterns, naming conventions, error handling approach]"`. "Interface conventions" is universal across REST, RPC, CLI, event-driven, and any other interface style.

3. **Line 87: PR-candidate example (D-14):** `"session/ceremony constraints"` → `"[cross-operation interaction constraints]"` (cross-cutting constraint classification examples)

4. **Line 96: Cross-reference pattern examples (D-14):** `"User.CreateUser"`, `"Auth.ValidateSession"` → `"[Component.OperationName]"` (single structural placeholder communicates the format; web-app-specific operation names removed)

5. **Line 117: Auto-detect implicit dependencies example (D-14, D-17):** `"all endpoints require authentication"` → `"[system-wide access policy]"` (Step 4.7 dependency detection)

6. **Line 129: Behavioral concern classification example (D-14):** `"verify JWT claims freshness when user info changes"` → `"[specific behavioral concern from dependency phase]"`

7. **Line 130: Constraint classification example (D-14):** `"ceremony logic should be reusable"` → `"[implementation constraint from dependency phase]"`

8. **Output contract template (D-14, bonus site):** `- **API conventions:** [naming, error handling conventions]` → `- **Interface conventions:** [naming, error handling conventions]` — consistent with the Step 1 extraction heading change.

### Task 3: case-validator.md — 4 changes applied + README.md deleted

**case-validator.md:**

1. **Lines 74-75: Classification decision tree step 4 (D-14):** `"cookie MUST be HttpOnly"` → `"[property] MUST [constraint]"`. The structural placeholder preserves the MUST-constraint pattern without technology specifics.

2. **Line 81: Classification decision tree step 5 (D-14):** `"Redis key pattern"`, `"JSONB column"`, `"PEM format keys"` → `"[storage key pattern]"`, `"[data column format]"`, `"[key format]"`. Category structure (configuration/storage/transport examples) preserved per D-16.

3. **Line 91: Language Pattern Signals — Behavioral signals (D-17):** `"returns 401"`, `"responds with 204"` → `"returns [status/error code]"`, `"responds with [status code]"`. The `"Explicit status codes"` category name retained — status codes exist across protocols.

4. **Line 98: Language Pattern Signals — Architectural constraint signals (D-17):** `"all endpoints"` → `"all operations"`. Simple term replacement.

**skills/case/README.md:** Deleted entirely (D-21). User-facing documentation, not a Claude system prompt. No functional impact on /case behavior. Rewrite deferred to a future phase.

## Deviations from Plan

### Auto-resolved Issues

**1. [Rule 2 - Missing functionality] Output contract template API conventions field**
- **Found during:** Task 2
- **Issue:** The output contract template's `## Architectural Context` section (line 186) had `- **API conventions:** [naming, error handling conventions]` — same "API conventions" heading that was replaced in Step 1 extraction guidance. CONTEXT.md and RESEARCH.md listed line 37 (Step 1) as the biased site but did not explicitly list line 186 (output template).
- **Fix:** Updated output template field to `- **Interface conventions:** [naming, error handling conventions]` for consistency. Same change, different location.
- **Files modified:** agents/case-briefer.md
- **Commit:** 34d7b4e

## Verification Results

**Comprehensive cross-file bias audit (post-completion):**
```bash
grep -rnE '(400|401|500|JWT|SMTP|Redis|cookie|endpoint|middleware|ceremony|passkey|WebAuthn|gRPC|JSONB|PEM|HttpOnly)' \
  skills/case/SKILL.md skills/case/step-discuss.md skills/case/step-finalize.md \
  agents/case-briefer.md agents/case-validator.md
```
Result: **Two remaining matches in case-briefer.md Guidelines section** — both are non-biased per D-15: `"communication patterns (REST, gRPC, CLI, event-driven, etc.)"` and `"Operations may be REST endpoints, RPC methods, CLI commands, event handlers"`. These explicitly enumerate diverse interface types to demonstrate universality; they do not assume any single protocol.

**README deletion:** PASS — `test ! -f skills/case/README.md` exits 0.

**Caller field consistency:** No `Auth:` field labels remain in SKILL.md or step-finalize.md.

**Structural rule consistency:** `[error description] (ErrorName)` present in both step-discuss.md (line 259) and step-finalize.md (line 383). Wording identical.

**Global adaptation instruction:** Present in SKILL.md at line 37 (Plan 01 change, verified).

## Known Stubs

None. All structural placeholders are intentional — they instruct Claude on format and quality bar, directing adaptation to host project specifics. The `[describe each observable side effect]` placeholder and `[who/what can invoke this]` values in the output format are deliberately abstract.

## Commits

| Task | Commit | Description |
|------|--------|-------------|
| Task 1 | c15fcf3 | feat(case): neutralize technology-biased content in step-finalize.md |
| Task 2 | 34d7b4e | feat(briefer): neutralize technology-biased inline examples in case-briefer |
| Task 3 | 7414262 | feat(validator): neutralize technology-biased examples + delete README |

## Self-Check: PASSED

All files verified present/deleted. All commits verified in git log.
