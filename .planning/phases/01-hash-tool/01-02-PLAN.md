---
phase: 01-hash-tool
plan: 02
type: tdd
wave: 2
depends_on:
  - 01-01
files_modified:
  - tools/hash-sections_test.ts
autonomous: true
requirements:
  - HASH-07
  - TEST-04

must_haves:
  truths:
    - "All 10 test cases pass via deno test"
    - "Tests import functions directly from hash-sections.ts (no subprocess spawning)"
    - "Test 1 verifies basic H2 extraction with nested H3 and pre-H2 exclusion"
    - "Test 2 verifies fenced code block does not create false section boundary"
    - "Test 3 verifies tilde fence handled identically to backtick fence"
    - "Test 4 verifies trailing whitespace normalization produces identical hashes"
    - "Test 5 verifies blank line collapsing produces identical hashes"
    - "Test 6 verifies determinism over 10 consecutive runs"
    - "Test 7 verifies JSON output format matches IMPL-SPEC schema"
    - "Test 8 verifies header rename changes hash"
    - "Test 9 verifies empty section produces valid hash"
    - "Test 10 verifies multi-file produces files array with correct entries"
  artifacts:
    - path: "tools/hash-sections_test.ts"
      provides: "10 Deno test cases for hash-sections.ts"
      min_lines: 80
  key_links:
    - from: "tools/hash-sections_test.ts"
      to: "tools/hash-sections.ts"
      via: "direct function imports (normalize, hashSections)"
      pattern: 'from "./hash-sections.ts"'
    - from: "tools/hash-sections_test.ts"
      to: "tools/tests/fixtures/"
      via: "Deno.readTextFile for fixture loading"
      pattern: "fixtures/"
    - from: "tools/hash-sections_test.ts"
      to: "jsr:@std/assert"
      via: "assertEquals, assertNotEquals imports"
      pattern: "jsr:@std/assert"
---

<objective>
Create the test file with all 10 test cases per IMPL-SPEC and verify the full suite passes.

Purpose: TEST-04 requires `hash-sections_test.ts` with 10 test cases per IMPL-SPEC. HASH-07 requires all 10 pass. This plan validates every hash tool behavior through direct function imports.

Output: `tools/hash-sections_test.ts` with 10 passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-hash-tool/01-CONTEXT.md
@.planning/phases/01-hash-tool/01-RESEARCH.md
@docs/IMPL-SPEC.md (lines 93-111: test case definitions)
@.planning/phases/01-hash-tool/01-01-SUMMARY.md (prior plan output)

<interfaces>
<!-- From tools/hash-sections.ts (created in Plan 01) -->

```typescript
export function normalize(text: string): string;
export async function hashSections(source: string): Promise<Section[]>;
export async function hashFile(path: string): Promise<FileResult>;

interface Section {
  heading: string;
  hash: string;
}

interface FileResult {
  path: string;
  sections: Section[];
}
```

Fixture files in `tools/tests/fixtures/`:
basic.md, code-block.md, tilde-fence.md, trailing-ws.md, blank-lines.md,
determinism.md, json-format.md, header-in-hash.md, empty-section.md,
multi-file-a.md, multi-file-b.md
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create hash-sections_test.ts with all 10 test cases</name>
  <files>tools/hash-sections_test.ts</files>
  <read_first>
    tools/hash-sections.ts (understand exported function signatures and types)
    docs/IMPL-SPEC.md (lines 97-111 for exact test case definitions)
    .planning/phases/01-hash-tool/01-CONTEXT.md (D-11 test approach: direct imports, no subprocess)
    .planning/phases/01-hash-tool/01-RESEARCH.md (test file pattern in Code Examples section)
    tools/tests/fixtures/basic.md (verify fixture content for assertion values)
    tools/tests/fixtures/code-block.md
    tools/tests/fixtures/tilde-fence.md
    tools/tests/fixtures/trailing-ws.md
    tools/tests/fixtures/blank-lines.md
    tools/tests/fixtures/empty-section.md
    tools/tests/fixtures/multi-file-a.md
    tools/tests/fixtures/multi-file-b.md
    tools/tests/fixtures/header-in-hash.md
  </read_first>
  <behavior>
    - Test 1 (basic H2 extraction): hashSections(basic.md content) returns exactly 2 sections; headings are "Section One" and "Section Two"; each hash is 8 hex chars; pre-H1 content excluded
    - Test 2 (code block safety): hashSections(code-block.md content) returns exactly 1 section named "Real Section"; ## inside fenced block does NOT create a second section
    - Test 3 (tilde fence): hashSections(tilde-fence.md content) returns exactly 1 section named "Tilde Test"; ## inside ~~~ block does NOT create a second section
    - Test 4 (trailing whitespace normalization): normalize("text   \n") produces same result as normalize("text\n"); hashing section with trailing whitespace matches section without
    - Test 5 (blank line normalization): Content with 3+ consecutive blank lines produces same hash as content with single blank line separation
    - Test 6 (determinism): Hash same fixture 10 times in a loop; all 10 results are identical (assertEquals on each pair)
    - Test 7 (JSON format): hashFile(json-format.md) returns FileResult with correct path string and sections array matching { heading: string, hash: string } shape
    - Test 8 (header in hash): hashSections with "## Original Name\ncontent") vs hashSections with "## Changed Name\ncontent" produce different hashes (assertNotEquals)
    - Test 9 (empty section): hashSections(empty-section.md content) returns 2 sections; first section ("Empty") has a valid 8-char hex hash despite no content below heading
    - Test 10 (multi-file): Call hashFile on multi-file-a.md and multi-file-b.md separately; verify each returns correct path and section count
  </behavior>
  <action>
Create `tools/hash-sections_test.ts` per D-11 (direct function imports, no subprocess spawning).

**Imports:**
```typescript
import { assertEquals, assertNotEquals } from "jsr:@std/assert";
import { normalize, hashSections, hashFile } from "./hash-sections.ts";
```

**Fixture path resolution:**
```typescript
const FIXTURES = new URL("./tests/fixtures/", import.meta.url).pathname;
```
Use `Deno.readTextFile(`${FIXTURES}filename.md`)` to load each fixture.

**Test 1: "basic H2 extraction"**
- Load `basic.md`, call `hashSections(source)`
- Assert `sections.length === 2`
- Assert `sections[0].heading === "Section One"`
- Assert `sections[1].heading === "Section Two"`
- Assert each `hash.length === 8`
- Assert each hash matches `/^[0-9a-f]{8}$/`
- This implicitly validates HASH-04 (pre-first-H2 content excluded -- if H1/preamble were included, it would create unexpected sections or corrupt the first hash)

**Test 2: "code block safety"**
- Load `code-block.md`, call `hashSections(source)`
- Assert `sections.length === 1` (the `## Fake Heading` inside the code block must NOT create a boundary)
- Assert `sections[0].heading === "Real Section"`

**Test 3: "tilde fence handling"**
- Load `tilde-fence.md`, call `hashSections(source)`
- Assert `sections.length === 1`
- Assert `sections[0].heading === "Tilde Test"`

**Test 4: "normalization: trailing whitespace"**
- Use `normalize()` directly to verify:
  - `normalize("text   \n") === normalize("text\n")`
- Also verify with hashSections: create two strings that differ only in trailing whitespace, hash both, assert hashes are equal
  - String A: `"## Test\nSome text   \nMore text\t\n"`
  - String B: `"## Test\nSome text\nMore text\n"`
  - `hashSections(A)` hash === `hashSections(B)` hash

**Test 5: "normalization: consecutive blank lines"**
- Use `normalize()` directly:
  - `normalize("a\n\n\n\nb") === normalize("a\n\nb")`
- Also verify with hashSections: create two strings that differ only in blank line count
  - String A: `"## Test\nParagraph one.\n\n\n\n\nParagraph two.\n"`
  - String B: `"## Test\nParagraph one.\n\nParagraph two.\n"`
  - `hashSections(A)` hash === `hashSections(B)` hash

**Test 6: "determinism"**
- Load `determinism.md`, call `hashSections(source)` 10 times in a loop
- Collect all results, assert every run's first section hash equals the first run's hash
- `assertEquals(results[i][0].hash, results[0][0].hash)` for i = 1..9

**Test 7: "JSON output format"**
- Call `hashFile(`${FIXTURES}json-format.md`)` (note: uses hashFile, not hashSections)
- Assert result has `path` property that is a string
- Assert result has `sections` property that is an array
- Assert each section has `heading` (string) and `hash` (string) properties
- Assert `result.path` ends with `json-format.md` (or equals the exact path passed)
- Assert `result.sections.length >= 1`

**Test 8: "header included in hash"**
- Hash string A: `"## Original Name\n\nSome content here.\n"`
- Hash string B: `"## Changed Name\n\nSome content here.\n"`
- `assertNotEquals(sectionsA[0].hash, sectionsB[0].hash)` -- renaming the heading MUST change the hash

**Test 9: "empty section"**
- Load `empty-section.md`, call `hashSections(source)`
- Assert `sections.length === 2`
- Assert `sections[0].heading === "Empty"`
- Assert `sections[1].heading === "Has Content"`
- Assert `sections[0].hash.length === 8` (valid hash even with no content)
- Assert `sections[0].hash` matches `/^[0-9a-f]{8}$/`

**Test 10: "multiple files"**
- Call `hashFile(`${FIXTURES}multi-file-a.md`)`  and `hashFile(`${FIXTURES}multi-file-b.md`)` separately
- Assert fileA has `sections.length === 1` with heading "Alpha"
- Assert fileB has `sections.length === 1` with heading "Beta"
- Assert fileA.path contains "multi-file-a.md"
- Assert fileB.path contains "multi-file-b.md"
- Assert fileA sections[0].hash !== fileB sections[0].hash (different content = different hash)

**Run command:** `deno test --allow-read=tools/tests/fixtures tools/hash-sections_test.ts`

All 10 tests must pass. If any test fails, fix the test or the implementation (read the error, determine if the fixture content doesn't match expectations, and adjust accordingly -- but do NOT change locked decisions D-01 through D-14).
  </action>
  <verify>
    <automated>deno test --allow-read=tools/tests/fixtures tools/hash-sections_test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tools/hash-sections_test.ts` exists with 10 `Deno.test()` calls
    - File contains `import { normalize, hashSections, hashFile } from "./hash-sections.ts"`
    - File contains `import { assertEquals, assertNotEquals } from "jsr:@std/assert"`
    - File does NOT spawn subprocesses (no `Deno.Command`, no `new Deno.Process`)
    - Test names include: "basic", "code block", "tilde", "trailing whitespace" or "normalization", "blank line", "determinism", "JSON", "header", "empty", "multi"
    - Running `deno test --allow-read=tools/tests/fixtures tools/hash-sections_test.ts` exits with code 0 (all 10 tests pass)
    - Test output shows `10 passed` and `0 failed`
  </acceptance_criteria>
  <done>All 10 test cases pass via deno test, covering every IMPL-SPEC test case definition. HASH-07 and TEST-04 satisfied.</done>
</task>

</tasks>

<verification>
1. `deno test --allow-read=tools/tests/fixtures tools/hash-sections_test.ts` shows 10 passed, 0 failed
2. No subprocess spawning in test file (`grep -c "Deno.Command\|Deno.Process" tools/hash-sections_test.ts` returns 0)
3. Test file imports directly from `./hash-sections.ts`
</verification>

<success_criteria>
- All 10 test cases pass with `deno test`
- Tests cover: basic extraction, code block safety, tilde fence, trailing whitespace normalization, blank line normalization, determinism, JSON format, header-in-hash, empty section, multi-file
- Tests use direct function imports (no subprocess spawning per D-11)
- Test assertions use `jsr:@std/assert` (assertEquals, assertNotEquals)
</success_criteria>

<output>
After completion, create `.planning/phases/01-hash-tool/01-02-SUMMARY.md`
</output>
