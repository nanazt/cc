---
phase: 13-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - skills/consolidate/SKILL.md
  - tests/fixtures/verification/microservice/specs/auth/context.md
  - tests/fixtures/verification/microservice/specs/auth/cases.md
  - tests/fixtures/verification/microservice/specs/billing/context.md
  - tests/fixtures/verification/microservice/specs/billing/cases.md
  - tests/fixtures/verification/microservice/specs/INDEX.md
  - tests/fixtures/verification/cli/specs/init/context.md
  - tests/fixtures/verification/cli/specs/init/cases.md
  - tests/fixtures/verification/cli/specs/config/context.md
  - tests/fixtures/verification/cli/specs/config/cases.md
  - tests/fixtures/verification/cli/specs/INDEX.md
  - tests/fixtures/verification/library/specs/parser/context.md
  - tests/fixtures/verification/library/specs/parser/cases.md
  - tests/fixtures/verification/library/specs/emitter/context.md
  - tests/fixtures/verification/library/specs/emitter/cases.md
  - tests/fixtures/verification/library/specs/INDEX.md
autonomous: true
requirements:
  - VRFY-01
  - VRFY-03

must_haves:
  truths:
    - "SKILL.md Step 5 dispatches verifier with all 10 XML tags including schema_data and phase_context_file"
    - "SKILL.md Step 5 no longer has a skip branch or file-existence check"
    - "CLI fixture has no interface mapping in Public Interface sections -- V-11 would skip"
    - "Library fixture has no structured error identifiers -- V-15 would skip"
    - "Microservice fixture has interface mapping and error identifiers -- V-11 and V-15 would execute"
    - "All fixtures have correct provenance tags, section structure, and INDEX.md entries"
  artifacts:
    - path: "skills/consolidate/SKILL.md"
      provides: "Step 5 with full verifier dispatch"
      contains: "schema_data"
    - path: "tests/fixtures/verification/cli/specs/init/context.md"
      provides: "CLI component fixture for false-positive testing"
      min_lines: 10
    - path: "tests/fixtures/verification/library/specs/parser/context.md"
      provides: "Library component fixture for false-positive testing"
      min_lines: 10
    - path: "tests/fixtures/verification/microservice/specs/auth/context.md"
      provides: "Microservice component fixture (with interface mapping)"
      min_lines: 10
  key_links:
    - from: "skills/consolidate/SKILL.md"
      to: "agents/spec-verifier.md"
      via: "Agent dispatch in Step 5"
      pattern: "spec-verifier"
    - from: "tests/fixtures/verification/"
      to: "docs/examples/schema-*.md"
      via: "Fixtures match example schema component lists"
      pattern: "specs/"
---

<objective>
Wire the spec-verifier into the /consolidate pipeline by updating SKILL.md Step 5, and create minimal test fixtures for 3 project types to validate zero false positives (VRFY-03).

Purpose: The verifier agent (created in Plan 01) needs to be dispatched from the orchestrator with the correct XML tags. Test fixtures provide concrete specs/ structures for each project type so the verifier can be validated against non-service projects.

Output: Updated `skills/consolidate/SKILL.md` Step 5, test fixture directories for microservice/CLI/library project types
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-verification/13-CONTEXT.md
@.planning/phases/13-verification/13-RESEARCH.md
@.planning/phases/13-verification/13-01-SUMMARY.md
</context>

<interfaces>
<!-- Key contracts the executor needs. -->

From agents/spec-verifier.md (created in Plan 01) input contract -- 10 dispatch tags:
| Tag | Required |
|-----|----------|
| `<objective>` | Yes |
| `<schema_data>` | Yes |
| `<specs_dir>` | Yes |
| `<index_file>` | Yes |
| `<phase_id>` | Yes |
| `<phase_context_file>` | Yes |
| `<phase_cases_file>` | No |
| `<project_file>` | Yes |
| `<changed_components>` | Yes |
| `<e2e_dir>` | No |

From docs/examples/schema-microservice.md:
- Components: auth (type: api-gateway), user, notification, billing
- auth uses Sections: api-gateway override (Overview, Public Interface, Authentication, Rate Limiting, Error Handling, Dependencies, Configuration)
- Others use default sections

From docs/examples/schema-cli.md:
- Components: init, run, config, output
- All default type (no overrides)
- e2e-flows: false

From docs/examples/schema-library.md:
- Components: parser, transformer, emitter, plugin
- All default type (no overrides)
- e2e-flows: false

From skills/consolidate/SKILL.md Step 5 (current, lines 207-216):
```markdown
## Step 5: Dispatch Spec-Verifier Agent (Conditional)

<!-- Remove this skip branch once agents/spec-verifier.md exists in the agents/ directory -->

Check if `agents/spec-verifier.md` exists (use Glob).

- **Absent:** Log "Verification skipped (spec-verifier agent not yet created)". Mark output as "UNVERIFIED" in the confirmation summary.
- **Present:** Build dispatch prompt with full XML contract and dispatch the spec-verifier agent. Collect return: `## VERIFICATION COMPLETE` or `## VERIFICATION FAILED`.

**Error handling:** Verifier failure -> mark "UNVERIFIED", proceed. Verifier is read-only: no rollback.
```

From spec-consolidator.md output format -- context.md structure:
```markdown
# {Component} Spec

Last consolidated: Phase {id} ({YYYY-MM-DD})

> Component rules ({prefix}-XX) defined in cases.md. Referenced, not duplicated.

## Overview
{content}

## Public Interface
{content}
...
```

From spec-consolidator.md output format -- cases.md structure:
```markdown
# {Component} Cases

Last consolidated: Phase {id} ({YYYY-MM-DD})

## Component Rules
CR-1: {description} (Source: Phase {id})

## {Component}.{OperationName}
### Operation Rules
OR-1: {description} (Source: Phase {id})
### Side Effects
- {effect}
### Cases
| Type | # | When | Then | Expected Outcome |
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update SKILL.md Step 5 -- remove skip branch, wire verifier dispatch</name>
  <files>skills/consolidate/SKILL.md</files>
  <read_first>
    - skills/consolidate/SKILL.md (full file -- need to see Step 5 context and surrounding steps, especially Step 1 schema parsing and Step 6 summary format)
    - agents/spec-verifier.md (created in Plan 01 -- verify the actual input contract matches what we wire)
    - .planning/phases/13-verification/13-CONTEXT.md (D-07, D-08, D-26 decisions)
    - .planning/phases/13-verification/13-RESEARCH.md (Example 5: SKILL.md Step 5 After Update)
  </read_first>
  <action>
Replace SKILL.md Step 5 content (lines ~207-216) with the full dispatch specification. Remove the HTML comment about removing skip branch. Remove the Glob existence check and the Absent/Present branching. Per D-26, the skip branch is gone -- the verifier now always dispatches.

New Step 5 content (replace everything from `## Step 5:` through the error handling paragraph):

```markdown
## Step 5: Dispatch Spec-Verifier Agent

Build dispatch prompt for `agents/spec-verifier.md` with these XML tags:

| Tag | Required | Contents |
|-----|----------|----------|
| `<objective>` | Yes | "Verify consolidated specs for Phase {id}. Check all applicable verification items." |
| `<schema_data>` | Yes | Full JSON output from schema-parser.ts (parsed in Step 1) |
| `<specs_dir>` | Yes | Path to `specs/` directory root |
| `<index_file>` | Yes | Path to `specs/INDEX.md` |
| `<phase_id>` | Yes | Phase identifier being verified |
| `<phase_context_file>` | Yes | Path to phase CONTEXT.md |
| `<phase_cases_file>` | No | Path to source phase CASES.md. Omit for Phase 1 backfill. |
| `<project_file>` | Yes | Path to PROJECT.md |
| `<changed_components>` | Yes | JSON manifest from consolidator results |
| `<e2e_dir>` | No | Path to `specs/e2e/`. Omit if E2E flows disabled or no flows exist. |

Dispatch the spec-verifier agent. Collect return: `## VERIFICATION COMPLETE` or `## VERIFICATION FAILED`.

**Error handling:** Verifier failure -> mark "UNVERIFIED", proceed. Verifier is read-only: no rollback.
```

Key changes from the old Step 5:
1. Title changes from "Dispatch Spec-Verifier Agent (Conditional)" to "Dispatch Spec-Verifier Agent" (no longer conditional)
2. HTML comment `<!-- Remove this skip branch... -->` is deleted
3. Glob existence check is deleted
4. Absent/Present branching is deleted
5. Full XML tag table with all 10 tags is added (including `<schema_data>` from D-07 and `<phase_context_file>` from D-08)
6. Error handling paragraph retained as-is

IMPORTANT: Do NOT modify any other steps in SKILL.md. Only Step 5 changes.
  </action>
  <verify>
    <automated>grep "schema_data" skills/consolidate/SKILL.md && grep "phase_context_file" skills/consolidate/SKILL.md && grep -c "skip branch" skills/consolidate/SKILL.md | grep "0" && grep -c "Absent" skills/consolidate/SKILL.md | grep "0" && grep -c "(Conditional)" skills/consolidate/SKILL.md | grep "0" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md Step 5 heading is "## Step 5: Dispatch Spec-Verifier Agent" (no "(Conditional)")
    - SKILL.md contains `<schema_data>` in Step 5 tag table
    - SKILL.md contains `<phase_context_file>` in Step 5 tag table
    - SKILL.md contains "Check all applicable verification items" in objective row
    - SKILL.md does NOT contain "skip branch" (HTML comment removed)
    - SKILL.md does NOT contain "Absent:" or "spec-verifier agent not yet created"
    - SKILL.md does NOT contain "(Conditional)" after "Step 5"
    - SKILL.md Step 5 has exactly 10 rows in the tag table (objective, schema_data, specs_dir, index_file, phase_id, phase_context_file, phase_cases_file, project_file, changed_components, e2e_dir)
    - All other Steps (1-4, 6-7) are unchanged
    - Error handling paragraph still says "Verifier failure -> mark UNVERIFIED, proceed"
  </acceptance_criteria>
  <done>SKILL.md Step 5 dispatches verifier unconditionally with all 10 XML tags. Skip branch fully removed.</done>
</task>

<task type="auto">
  <name>Task 2: Create test fixtures for 3 project types</name>
  <files>
    tests/fixtures/verification/microservice/specs/auth/context.md
    tests/fixtures/verification/microservice/specs/auth/cases.md
    tests/fixtures/verification/microservice/specs/billing/context.md
    tests/fixtures/verification/microservice/specs/billing/cases.md
    tests/fixtures/verification/microservice/specs/INDEX.md
    tests/fixtures/verification/cli/specs/init/context.md
    tests/fixtures/verification/cli/specs/init/cases.md
    tests/fixtures/verification/cli/specs/config/context.md
    tests/fixtures/verification/cli/specs/config/cases.md
    tests/fixtures/verification/cli/specs/INDEX.md
    tests/fixtures/verification/library/specs/parser/context.md
    tests/fixtures/verification/library/specs/parser/cases.md
    tests/fixtures/verification/library/specs/emitter/context.md
    tests/fixtures/verification/library/specs/emitter/cases.md
    tests/fixtures/verification/library/specs/INDEX.md
  </files>
  <read_first>
    - agents/spec-verifier.md (created in Plan 01 -- understand what checks will be run against these fixtures)
    - agents/spec-consolidator.md (output format: context.md and cases.md structure)
    - docs/examples/schema-microservice.md (component names and types for microservice fixture)
    - docs/examples/schema-cli.md (component names for CLI fixture)
    - docs/examples/schema-library.md (component names for library fixture)
    - docs/MODEL.md (section structure 7+2, naming conventions)
    - .planning/phases/13-verification/13-CONTEXT.md (D-24, D-25: fixture scope and validation approach)
  </read_first>
  <action>
Per D-24 and D-25, create minimal test fixtures: 2 components per project type, each with context.md + cases.md + INDEX.md. Fixtures validate check skip/pass logic, not full consolidation output quality. Each file should be 10-20 lines -- enough to exercise section presence, provenance, naming format, and conditional check skip logic.

**Directory structure:**
```
tests/fixtures/verification/
  microservice/
    specs/
      auth/context.md
      auth/cases.md
      billing/context.md
      billing/cases.md
      INDEX.md
  cli/
    specs/
      init/context.md
      init/cases.md
      config/context.md
      config/cases.md
      INDEX.md
  library/
    specs/
      parser/context.md
      parser/cases.md
      emitter/context.md
      emitter/cases.md
      INDEX.md
```

**Microservice fixture (auth + billing):**

`auth` has type `api-gateway` in the schema, so its context.md uses the api-gateway section override (Overview, Public Interface, Authentication, Rate Limiting, Error Handling, Dependencies, Configuration -- NOT the default 7 sections). This tests V-04 section resolution against type overrides.

`auth/context.md` (~15 lines):
```markdown
# Auth Spec

Last consolidated: Phase 3 (2026-01-15)

> Component rules (CR-XX) defined in cases.md. Referenced, not duplicated.

## Overview
Authentication gateway handling token validation and session management.

## Public Interface
| Route | Operation | Auth |
|-------|-----------|------|
| POST /login | Auth.Login | public |
| POST /refresh | Auth.RefreshToken | authenticated |

## Authentication
Token validation via JWT with configurable expiry.

## Rate Limiting
10 requests per minute per IP on login endpoint.

## Error Handling
- invalid_credentials (InvalidCredentials) -- returned when login fails
- token_expired (TokenExpired) -- returned on expired JWT

## Dependencies
Requires User component for profile lookup.

## Configuration
JWT_SECRET, TOKEN_EXPIRY_SECONDS environment variables.
```

Note: `auth` has a route-to-operation mapping table in Public Interface -- V-11 should EXECUTE on this component. `auth` has structured error identifiers -- contributes to V-15 threshold.

`auth/cases.md` (~15 lines):
```markdown
# Auth Cases

Last consolidated: Phase 3 (2026-01-15)

## Component Rules
CR-1: All tokens expire after configured TTL (Source: Phase 3)

## Auth.Login
### Operation Rules
OR-1: Rate limited to 10 attempts per minute per IP (Source: Phase 3)
### Side Effects
- Emits login event on success
### Cases
| Type | # | When | Then | Expected Outcome |
|------|---|------|------|-----------------|
| S | 1 | Valid credentials | Login | Session token returned |
| F | 1 | Invalid credentials | Login | invalid_credentials (InvalidCredentials) |
```

`billing/context.md` (~12 lines):
```markdown
# Billing Spec

Last consolidated: Phase 3 (2026-01-15)

> Component rules (CR-XX) defined in cases.md. Referenced, not duplicated.

## Overview
Subscription management and payment processing.

## Public Interface
Billing.CreateSubscription, Billing.CancelSubscription operations.

## Domain Model
Subscription entity with status lifecycle.

## Behavior Rules
Cancellation takes effect at end of billing period.

## Error Handling
- payment_failed (PaymentFailed) -- returned when charge fails
- subscription_not_found (SubscriptionNotFound) -- returned on invalid subscription ID

## Dependencies
Requires Auth component for caller identity.

## Configuration
STRIPE_API_KEY environment variable.
```

Note: `billing` uses default sections (no type override). Has structured error identifiers -- with auth's identifiers, that's 2 components, so V-15 should EXECUTE on microservice fixture.

`billing/cases.md` (~12 lines):
```markdown
# Billing Cases

Last consolidated: Phase 3 (2026-01-15)

## Component Rules
CR-1: All subscription changes logged with timestamp (Source: Phase 3)

## Billing.CreateSubscription
### Operation Rules
OR-1: Payment method must be verified before subscription creation (Source: Phase 3)
### Cases
| Type | # | When | Then | Expected Outcome |
|------|---|------|------|-----------------|
| S | 1 | Valid payment method | Create subscription | Subscription active, first charge processed |
| F | 1 | Invalid payment method | Create subscription | payment_failed (PaymentFailed) |
```

`microservice INDEX.md` (~12 lines):
```markdown
# Spec Index

Last updated: 2026-01-15

## Components

| Component | Type | Description | Files | Last Consolidated |
|-----------|------|-------------|-------|-------------------|
| auth | api-gateway | Authentication and session management | [context](auth/context.md) [cases](auth/cases.md) | Phase 3 (2026-01-15) |
| billing | | Subscription and payment processing | [context](billing/context.md) [cases](billing/cases.md) | Phase 3 (2026-01-15) |

## E2E Flows

No E2E flows.

## Operation Index

| Operation | Component | Cases Source | Phase |
|-----------|-----------|-------------|-------|
| Auth.Login | auth | [cases.md](auth/cases.md) | 3 |
| Auth.RefreshToken | auth | [cases.md](auth/cases.md) | 3 |
| Billing.CreateSubscription | billing | [cases.md](billing/cases.md) | 3 |
```

**CLI fixture (init + config):**

Both components use default sections. No interface mapping tables in Public Interface (just prose listing commands). No structured error identifiers (just prose error descriptions).

`init/context.md` (~12 lines):
```markdown
# Init Spec

Last consolidated: Phase 2 (2026-02-10)

> Component rules (CR-XX) defined in cases.md. Referenced, not duplicated.

## Overview
Project scaffolding and configuration initialization from templates.

## Public Interface
Init.Create operation accepts project name and template selection.

## Domain Model
ProjectTemplate and ProjectConfig entities.

## Behavior Rules
Template selection defaults to "basic" when not specified.

## Error Handling
Reports descriptive errors on invalid template names or existing directories.

## Dependencies
Requires Config component for default settings.

## Configuration
TEMPLATE_DIR environment variable for custom template location.
```

Note: Public Interface has NO route/mapping table -- just prose. V-11 should SKIP on this component.

`init/cases.md` (~12 lines):
```markdown
# Init Cases

Last consolidated: Phase 2 (2026-02-10)

## Component Rules
CR-1: Created projects include standard .gitignore (Source: Phase 2)

## Init.Create
### Operation Rules
OR-1: Existing directory prevents creation without --force flag (Source: Phase 2)
### Cases
| Type | # | When | Then | Expected Outcome |
|------|---|------|------|-----------------|
| S | 1 | Valid template and new directory | Create project | Directory created with template files |
| F | 1 | Directory already exists | Create project | [directory exists error] (DirectoryExists) |
```

`config/context.md` (~12 lines):
```markdown
# Config Spec

Last consolidated: Phase 2 (2026-02-10)

> Component rules (CR-XX) defined in cases.md. Referenced, not duplicated.

## Overview
User configuration management and persistence to disk.

## Public Interface
Config.Load and Config.Save operations for reading and writing user preferences.

## Domain Model
ConfigFile entity with key-value pairs and optional nesting.

## Behavior Rules
Missing config file creates default configuration on first load.

## Error Handling
Reports errors on permission failures and malformed config files.

## Dependencies
None.

## Configuration
CONFIG_PATH for custom config file location.
```

Note: Error Handling has NO structured identifiers (no `(ErrorName)` patterns). Between init and config, zero components have structured error identifiers, so V-15 should SKIP on CLI fixture.

`config/cases.md` (~10 lines):
```markdown
# Config Cases

Last consolidated: Phase 2 (2026-02-10)

## Component Rules
CR-1: Config file format is TOML (Source: Phase 2)

## Config.Load
### Operation Rules
OR-1: Missing config file triggers default creation (Source: Phase 2)
### Cases
| Type | # | When | Then | Expected Outcome |
|------|---|------|------|-----------------|
| S | 1 | Config file exists | Load | Configuration returned |
| S | 2 | Config file missing | Load | Default config created and returned |
| F | 1 | Config file malformed | Load | Parse error reported with line number |
```

`cli INDEX.md` (~12 lines):
```markdown
# Spec Index

Last updated: 2026-02-10

## Components

| Component | Type | Description | Files | Last Consolidated |
|-----------|------|-------------|-------|-------------------|
| init | | Project scaffolding and initialization | [context](init/context.md) [cases](init/cases.md) | Phase 2 (2026-02-10) |
| config | | Configuration management and persistence | [context](config/context.md) [cases](config/cases.md) | Phase 2 (2026-02-10) |

## E2E Flows

No E2E flows.

## Operation Index

| Operation | Component | Cases Source | Phase |
|-----------|-----------|-------------|-------|
| Init.Create | init | [cases.md](init/cases.md) | 2 |
| Config.Load | config | [cases.md](config/cases.md) | 2 |
| Config.Save | config | [cases.md](config/cases.md) | 2 |
```

**Library fixture (parser + emitter):**

Both components use default sections. No interface mapping tables. No structured error identifiers. Similar to CLI but different domain.

`parser/context.md` (~12 lines):
```markdown
# Parser Spec

Last consolidated: Phase 1 (2026-03-01)

> Component rules (CR-XX) defined in cases.md. Referenced, not duplicated.

## Overview
Input parsing and AST construction from source text.

## Public Interface
Parser.Parse operation accepts source string and returns AST.

## Domain Model
AST nodes: Program, Statement, Expression, Literal.

## Behavior Rules
Parsing is deterministic -- same input always produces same AST.

## Error Handling
Collects all parse errors before returning, with line and column positions.

## Dependencies
None.

## Configuration
MAX_RECURSION_DEPTH for nested expression limit.
```

`parser/cases.md` (~12 lines):
```markdown
# Parser Cases

Last consolidated: Phase 1 (2026-03-01)

## Component Rules
CR-1: Parse errors include source location (line, column) (Source: Phase 1)

## Parser.Parse
### Operation Rules
OR-1: Maximum recursion depth enforced during expression parsing (Source: Phase 1)
### Cases
| Type | # | When | Then | Expected Outcome |
|------|---|------|------|-----------------|
| S | 1 | Valid source | Parse | AST returned |
| F | 1 | Syntax error in source | Parse | Error list with positions |
| E | 1 | Recursion depth exceeded | Parse | Depth limit error at offending expression |
```

`emitter/context.md` (~12 lines):
```markdown
# Emitter Spec

Last consolidated: Phase 1 (2026-03-01)

> Component rules (CR-XX) defined in cases.md. Referenced, not duplicated.

## Overview
Output code generation and serialization from AST.

## Public Interface
Emitter.Emit operation accepts AST and returns generated output string.

## Domain Model
EmitOptions for controlling output format (minified, pretty-printed).

## Behavior Rules
Output encoding is always UTF-8.

## Error Handling
Reports errors on unsupported AST node types encountered during emission.

## Dependencies
Requires Parser component for AST type definitions.

## Configuration
DEFAULT_EMIT_FORMAT for output style preference.
```

`emitter/cases.md` (~10 lines):
```markdown
# Emitter Cases

Last consolidated: Phase 1 (2026-03-01)

## Component Rules
CR-1: Generated output is valid UTF-8 (Source: Phase 1)

## Emitter.Emit
### Operation Rules
OR-1: Unsupported AST nodes produce descriptive error, not silent skip (Source: Phase 1)
### Cases
| Type | # | When | Then | Expected Outcome |
|------|---|------|------|-----------------|
| S | 1 | Valid AST | Emit | Generated output string |
| F | 1 | AST contains unsupported node | Emit | Unsupported node error with node type |
```

`library INDEX.md` (~12 lines):
```markdown
# Spec Index

Last updated: 2026-03-01

## Components

| Component | Type | Description | Files | Last Consolidated |
|-----------|------|-------------|-------|-------------------|
| parser | | Input parsing and AST construction | [context](parser/context.md) [cases](parser/cases.md) | Phase 1 (2026-03-01) |
| emitter | | Output code generation and serialization | [context](emitter/context.md) [cases](emitter/cases.md) | Phase 1 (2026-03-01) |

## E2E Flows

No E2E flows.

## Operation Index

| Operation | Component | Cases Source | Phase |
|-----------|-----------|-------------|-------|
| Parser.Parse | parser | [cases.md](parser/cases.md) | 1 |
| Emitter.Emit | emitter | [cases.md](emitter/cases.md) | 1 |
```

**Critical fixture design properties for VRFY-03 validation:**

| Property | Microservice | CLI | Library |
|----------|-------------|-----|---------|
| V-04 section resolution | api-gateway override (auth) + default (billing) | default only | default only |
| V-11 interface mapping | YES (auth has route table) | NO (prose only) | NO (prose only) |
| V-15 error identifiers | YES (2 components: auth, billing) | NO (0 components) | NO (0 components) |
| V-29 E2E | schema says true, but no e2e dir in fixture | schema says false | schema says false |
| Provenance tags | All entries have `(Source: Phase N)` | All entries have `(Source: Phase N)` | All entries have `(Source: Phase N)` |
| Operation naming | PascalCase `{Component}.{Op}` | PascalCase `{Component}.{Op}` | PascalCase `{Component}.{Op}` |

This ensures CLI and library fixtures would NOT trigger V-11 or V-15 findings (zero false positives), while microservice fixture WOULD trigger these checks (confirming they still work when applicable).
  </action>
  <verify>
    <automated>ls tests/fixtures/verification/microservice/specs/auth/context.md tests/fixtures/verification/cli/specs/init/context.md tests/fixtures/verification/library/specs/parser/context.md tests/fixtures/verification/microservice/specs/INDEX.md tests/fixtures/verification/cli/specs/INDEX.md tests/fixtures/verification/library/specs/INDEX.md 2>/dev/null && echo "All fixture files exist" && grep "Source: Phase" tests/fixtures/verification/cli/specs/init/cases.md && grep "Source: Phase" tests/fixtures/verification/library/specs/parser/cases.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 15 fixture files exist in the directory structure shown above
    - Every cases.md file contains `(Source: Phase N)` provenance tags
    - Every context.md file starts with `# {Component} Spec` and has `Last consolidated:` header
    - Every INDEX.md has Components table and Operation Index table
    - Microservice auth/context.md uses api-gateway sections (Authentication, Rate Limiting -- NOT Domain Model, Behavior Rules)
    - Microservice auth/context.md has a route-to-operation mapping table in Public Interface
    - Microservice auth/context.md and billing/context.md both have structured error identifiers in Error Handling
    - CLI init/context.md and config/context.md have NO route/mapping tables in Public Interface (prose only)
    - CLI init/context.md and config/context.md have NO structured `(ErrorName)` identifiers in Error Handling
    - Library parser/context.md and emitter/context.md have NO route/mapping tables (prose only)
    - Library parser/context.md and emitter/context.md have NO structured `(ErrorName)` identifiers
    - Each fixture file is 10-20 lines (minimal per D-25)
    - Operation names follow `{Component}.{Op}` PascalCase format in all fixtures
  </acceptance_criteria>
  <done>Test fixtures for 3 project types created with correct structural properties for V-11/V-15 skip/execute validation. SKILL.md Step 5 wires verifier dispatch with all 10 XML tags, skip branch removed.</done>
</task>

</tasks>

<verification>
1. `skills/consolidate/SKILL.md` Step 5 has full dispatch table with `<schema_data>` and `<phase_context_file>`
2. SKILL.md Step 5 has no skip branch, no existence check, no "(Conditional)" suffix
3. All 15 fixture files exist with correct content
4. CLI and library fixtures have NO interface mapping tables (V-11 skip targets)
5. CLI and library fixtures have NO structured error identifiers (V-15 skip targets)
6. Microservice fixture HAS both (V-11 and V-15 execute targets)
7. All fixtures have provenance tags and correct naming format
</verification>

<success_criteria>
- The /consolidate pipeline can dispatch the verifier with all required XML tags
- Test fixtures represent 3 distinct project types with structural properties that exercise conditional check logic
- Running the verifier against CLI or library fixtures would produce zero false positives from V-11 or V-15
</success_criteria>

<output>
After completion, create `.planning/phases/13-verification/13-02-SUMMARY.md`
</output>
