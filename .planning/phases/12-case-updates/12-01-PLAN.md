---
phase: 12-case-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/case/SKILL.md
  - skills/case/step-init.md
  - skills/case/step-discuss.md
  - skills/case/step-finalize.md
  - skills/case/README.md
  - agents/case-briefer.md
  - agents/case-validator.md
  - docs/MODEL.md
autonomous: true
requirements:
  - CASE-01
  - CASE-02
  - CASE-06
  - CASE-07

must_haves:
  truths:
    - "No /case skill file contains service-biased language (service topology, cross-service, {Service}, SR-01, System Rules)"
    - "case-briefer uses 'component topology' and 'cross-component' instead of service equivalents"
    - "All rule prefixes use dash+number format with no padding (OR-1, PR-1, GR-1, CR-1, TR-1)"
    - "case-validator recognizes TR-N and OR-N as valid rule prefixes"
    - "MODEL.md Rule System table uses GR-N format (no zero-padding)"
  artifacts:
    - path: "skills/case/SKILL.md"
      provides: "Updated rule tier conventions and canonical example with OR-N, GR-N, PR-N format"
      contains: "OR-1, OR-2, OR-3"
    - path: "agents/case-briefer.md"
      provides: "Component topology language, GR-candidate classification"
      contains: "component topology"
    - path: "agents/case-validator.md"
      provides: "V2 prefix acceptance (TR-N, OR-N)"
      contains: "TR"
    - path: "docs/MODEL.md"
      provides: "Updated Rule System table with GR-N format"
      contains: "GR-N"
  key_links:
    - from: "skills/case/SKILL.md"
      to: "skills/case/step-discuss.md"
      via: "Rule prefix conventions referenced in both"
      pattern: "OR-\\d|PR-\\d|GR-\\d"
    - from: "skills/case/step-discuss.md"
      to: "skills/case/step-finalize.md"
      via: "Phase Rules and GR Candidates referenced consistently"
      pattern: "GR Candidates|Global Rules"
---

<objective>
Migrate all /case skill files, both agents, and MODEL.md from v1 vocabulary to v2 universal terminology. This is mechanical 1:1 term replacement plus rule prefix unification.

Purpose: Eliminate all service-biased language (CASE-01, CASE-02) and establish native OR-N prefix format (CASE-06) across the entire /case ecosystem. Also update case-validator prefix acceptance (CASE-07 partial -- prefix regex only, section recognition in Plan 02). Update MODEL.md GR-XX to GR-N (D-28).

Output: 8 updated files with consistent v2 vocabulary and unified prefix format. Zero matches for old-format terms in any modified file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-case-updates/12-CONTEXT.md
@.planning/phases/12-case-updates/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vocabulary migration and prefix unification in /case skill files and README</name>
  <files>skills/case/SKILL.md, skills/case/step-init.md, skills/case/step-discuss.md, skills/case/step-finalize.md, skills/case/README.md</files>

  <read_first>
    skills/case/SKILL.md
    skills/case/step-init.md
    skills/case/step-discuss.md
    skills/case/step-finalize.md
    skills/case/README.md
    .planning/phases/12-case-updates/12-CONTEXT.md
    .planning/phases/12-case-updates/12-RESEARCH.md
  </read_first>

  <action>
Apply these exact replacements across all 5 skill files. Use the term replacement inventory from 12-RESEARCH.md (verified line numbers) as the authoritative list.

**Rule prefix replacements (per D-09, D-10, D-11, D-12):**
- `R1, R2, R3` (bare operation rules) -> `OR-1, OR-2, OR-3` (with dash)
- `PR1, PR2, PR3` (bare phase rules) -> `PR-1, PR-2, PR-3` (with dash)
- `SR-01, SR-02` (zero-padded system rules) -> `GR-1, GR-2` (no padding, renamed)
- `Inherits: PR1, PR2, SR-01` -> `Inherits: PR-1, PR-2, GR-1`
- `Per SR-XX` -> `Per GR-N`
- `Overrides SR-XX:` -> `Overrides GR-N:`
- `PR[N]` dynamic references in step-discuss.md -> `PR-[N]`

**Label replacements (per D-24, D-25):**
- `System Rules` (label/heading) -> `Global Rules`
- `SR Candidates` (section name) -> `GR Candidates`
- `SR-candidate` (classification term) -> `GR-candidate`
- `system-wide rule (all services, all phases)` -> `global rule (all components, all phases)` (step-discuss.md L61)

**Vocabulary replacements (per D-20, no example rewrite):**
- `service` references in context descriptions -> `component` (where it refers to the target entity, not in generic English usage)
- step-init.md: `service topology` reference in briefer dispatch -> `component topology` (if present)

**SKILL.md specific updates:**
Replace the entire Rule tier conventions block (starting at approximately L144) with this exact content:

```
**Rule tier conventions:**
- `OR-1, OR-2, OR-3` -- operation-specific rules (dash, no padding)
- `PR-1, PR-2, PR-3` -- phase-wide permanent rules (dash, no padding)
- `TR-1, TR-2, TR-3` -- phase-wide temporary rules (excluded from specs at consolidation)
- `GR-1, GR-2` -- global rules in PROJECT.md (dash, no padding)
- `CR-1, CR-2` -- component rules in specs/ (dash, no padding; promoted from PR at consolidation)
- Operation Rules reference Phase/Global Rules via `Inherits: PR-1, PR-2, GR-1` line
- Override format: `Overrides GR-N: [what changes] ([justification])`
- `Per GR-N` prefix when a Phase Rule derives from a Global Rule
```

Update the canonical example in SKILL.md (approximately L117-120):
```
Rules:
  - OR-1: constraint not in diagram
  - OR-2: another constraint. Design intent: developer's proposed approach
  - Inherits: PR-1, PR-2, GR-1
```

**step-discuss.md specific updates:**
- Step 2.5 Phase Rules presentation: change `SR-01:` to `GR-1:` in the example block
- `System Rules (from PROJECT.md, apply to this phase):` heading -> `Global Rules (from PROJECT.md, apply to this phase):`
- Mid-discussion PR promotion: `PR[N]` -> `PR-[N]` in the promotion proposal text
- SR-candidate discovery: replace entire block -- change "system-wide rule (all services, all phases)" to "global rule (all components, all phases)", change "SR-candidate for PROJECT.md promotion" to "GR-candidate for PROJECT.md promotion", change "Phase Rule PR[N]" to "Phase Rule PR-[N]"
- `## SR Candidates` reference at L65 -> `## GR Candidates`

**step-finalize.md specific updates:**
- Phase Rules section in output format: `PR1:` -> `PR-1:`, `SR-01:` -> `GR-1:`, `Per SR-01:` -> `Per GR-1:`
- `**System Rules (from PROJECT.md) applicable to this phase:**` -> `**Global Rules (from PROJECT.md) applicable to this phase:**`
- `Inherits: PR1, PR2, SR-01` -> `Inherits: PR-1, PR-2, GR-1`
- `## SR Candidates` heading -> `## GR Candidates`
- All `R1, R2, R3` in operation Rules examples -> `OR-1, OR-2, OR-3`

**README.md specific updates:**
- 3-tier rule hierarchy description: update SR->GR, R->OR, add dash to PR
- `System Rules (SR)` -> `Global Rules (GR)`
- `SR Candidates` -> `GR Candidates`
- `SR-01` examples -> `GR-1`
- `R1` examples -> `OR-1`

**Do NOT change:**
- Example scenario text content (per D-21, full example rewrite is deferred)
- Structural organization of any file
- Any non-terminology content
  </action>

  <verify>
    <automated>cd /Users/syr/Developments/cckit && grep -rn 'SR-0[0-9]\|SR Candidates\|System Rules\| R[0-9]\b\|PR[0-9]\b\|Overrides SR\|Per SR' skills/case/ | grep -v 'cross-component' | grep -v '\.md~' ; echo "EXIT:$?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -rn 'SR-0[0-9]' skills/case/` returns zero matches
    - `grep -rn 'SR Candidates' skills/case/` returns zero matches
    - `grep -rn 'System Rules' skills/case/` returns zero matches (heading/label context only; generic English use of "system" is fine)
    - `grep -rn ' R[0-9]:' skills/case/SKILL.md` returns zero matches (old bare R1 format)
    - `grep -n 'OR-1' skills/case/SKILL.md` returns at least 2 matches (conventions + canonical example)
    - `grep -n 'GR Candidates' skills/case/step-discuss.md` returns at least 1 match
    - `grep -n 'GR Candidates' skills/case/step-finalize.md` returns at least 1 match
    - `grep -n 'Global Rules' skills/case/step-discuss.md` returns at least 1 match
    - `grep -n 'Global Rules' skills/case/step-finalize.md` returns at least 1 match
    - `grep -n 'Inherits: PR-1, PR-2, GR-1' skills/case/SKILL.md` returns at least 1 match
  </acceptance_criteria>

  <done>All 5 skill files use v2 vocabulary: OR-N for operation rules, PR-N (with dash) for phase rules, GR-N for global rules, "Global Rules" label, "GR Candidates" section name. No SR-XX, bare R1, bare PR1, or "System Rules" label remains.</done>
</task>

<task type="auto">
  <name>Task 2: Vocabulary migration in case-briefer, case-validator, and MODEL.md</name>
  <files>agents/case-briefer.md, agents/case-validator.md, docs/MODEL.md</files>

  <read_first>
    agents/case-briefer.md
    agents/case-validator.md
    docs/MODEL.md
    .planning/phases/12-case-updates/12-CONTEXT.md
    .planning/phases/12-case-updates/12-RESEARCH.md
  </read_first>

  <action>
**case-briefer.md replacements (per D-20, D-24, D-27, verified line numbers from RESEARCH.md):**

Service->Component terminology (11 references):
- L33: `Service topology` -> `Component topology`
- L36: `Cross-service interaction patterns` -> `Cross-component interaction patterns`
- L45: `service contract` -> `component contract`
- L79: `{Service}.{OperationName}` -> `{Component}.{OperationName}`
- L130 (decision tree): `cross-service` -> `cross-component`
- L130 (decision tree): `service contract` -> `component contract`
- L178 (table): `Service.Operation` -> `Component.Operation`

Rule prefix changes:
- L71, L75, L167, L211-215, L282: all `SR-` references -> `GR-` with no-padding
- L69, L71: `SR-candidate` -> `GR-candidate`
- L71, L215: `All services/phases` -> `All components/phases`

Step 4.5 classification rename (per D-27):
- `SR-candidate` classification label -> `GR-candidate`
- `SR` in classification headers/descriptions -> `GR`
- `System-Wide Candidates` heading in output contract -> `Global-Wide Candidates` or `Global Candidates` (keep the table structure, just rename)

Section heading updates in output contract:
- `### System-Wide Candidates (may belong in PROJECT.md SR)` -> `### Global Candidates (may belong in PROJECT.md GR)`
- `Existing SR?` column header -> `Existing GR?`
- `SR-XX (yes) or New` example -> `GR-N (yes) or New`

Briefer prompt text in step-init.md dispatch:
- `service topology, patterns` in `<files_to_read>` comment -> `component topology, patterns`

**case-validator.md updates (per D-17, D-19 -- prefix regex only, section recognition is Plan 02):**

The validator is already technology-neutral (confirmed by RESEARCH.md). Only prefix acceptance needs updating:

- Wherever the validator references rule ID formats (in Check A, Check B, etc.), ensure it recognizes the unified format: `(GR|CR|OR|PR|TR)-\d+`
- If there are any implicit assumptions about `R\d` or `SR-\d+` format, update to the unified regex
- The validator currently checks for rules in Phase Rules (PR section), Operation Rules (R section) -- update any references from `R section` to `OR section` if they exist

Note: case-validator.md has no explicit regex patterns in its text (it's a prompt, not code). The update here is ensuring the methodology text and examples use v2 prefix format. If examples reference `PR1` or `R1`, update to `PR-1` or `OR-1`.

**MODEL.md updates (per D-28, D-11):**

Update ALL GR-XX references (not just the Rule System table):
- L136: `GR-XX` -> `GR-N` in Prefix column; `GR-01: All timestamps use UTC` -> `GR-1: All timestamps use UTC` in Example column
- L139: `PR1:` example -> `PR-1:` in Example column
- L140: `TR1:` example -> `TR-1:` in Example column
- L392: `GR-XX` -> `GR-N` in Merge Rules section text; `See GR-XX` -> `See GR-N`
  </action>

  <verify>
    <automated>cd /Users/syr/Developments/cckit && echo "=== case-briefer ===" && grep -n 'service topology\|cross-service\|{Service}\|SR-\|SR-candidate\|All services' agents/case-briefer.md ; echo "=== case-validator ===" && grep -n 'SR-\| R[0-9]\|PR[0-9][^-]' agents/case-validator.md ; echo "=== MODEL.md ===" && grep -n 'GR-XX\|GR-01\|PR1:\|TR1:' docs/MODEL.md ; echo "EXIT:$?"</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'service topology' agents/case-briefer.md` returns zero matches
    - `grep -n 'component topology' agents/case-briefer.md` returns at least 1 match
    - `grep -n 'cross-service' agents/case-briefer.md` returns zero matches
    - `grep -n '{Service}' agents/case-briefer.md` returns zero matches
    - `grep -n '{Component}' agents/case-briefer.md` returns at least 1 match
    - `grep -n 'SR-candidate' agents/case-briefer.md` returns zero matches
    - `grep -n 'GR-candidate' agents/case-briefer.md` returns at least 1 match
    - `grep -n 'GR-XX' docs/MODEL.md` returns zero matches
    - `grep -n 'GR-01' docs/MODEL.md` returns zero matches
    - `grep -n 'GR-N' docs/MODEL.md` returns at least 2 matches (Rule System table + Merge Rules)
    - `grep -n 'GR-1' docs/MODEL.md` returns at least 1 match (example in table)
    - `grep -n 'PR1:' docs/MODEL.md` returns zero matches (should be PR-1:)
    - `grep -n 'TR1:' docs/MODEL.md` returns zero matches (should be TR-1:)
  </acceptance_criteria>

  <done>case-briefer uses component terminology throughout. case-validator text uses v2 prefix format. MODEL.md Rule System table and Merge Rules section use GR-N (no zero-padding) with consistent dash+number format for PR-1, TR-1 examples.</done>
</task>

</tasks>

<verification>
Run comprehensive old-format audit across all modified files:

```bash
# Should return ZERO matches for all old-format terms
grep -rn 'SR-0[0-9]\|SR Candidates\|System Rules\|service topology\|cross-service\|{Service}\|GR-XX\|GR-01' \
  skills/case/ agents/case-briefer.md agents/case-validator.md docs/MODEL.md

# Should return ZERO matches for bare (no-dash) rule IDs in conventions/examples context
grep -rn ' R[0-9]:\| PR[0-9]:\| TR[0-9]:' \
  skills/case/ agents/case-briefer.md agents/case-validator.md docs/MODEL.md
```

Both commands should produce no output. Any match indicates an incomplete migration.
</verification>

<success_criteria>
- All 8 files updated with v2 vocabulary
- Zero grep matches for any old-format term (SR-01, SR Candidates, System Rules, service topology, cross-service, {Service}, GR-XX, GR-01, bare R1/PR1/TR1)
- All rule prefixes use unified dash+number format: OR-1, PR-1, GR-1, CR-1, TR-1
- CASE-01 satisfied: no service-biased language in /case skill
- CASE-02 satisfied: case-briefer uses "component topology"
- CASE-06 satisfied: OR-N prefix native throughout
- CASE-07 partial: validator text uses v2 format (section recognition in Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/12-case-updates/12-01-SUMMARY.md`
</output>
