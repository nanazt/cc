---
phase: 19
slug: first-convention-commit
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-06
---

# Phase 19 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | deno test (built-in) + shell script inline verification |
| **Config file** | none — Deno test needs no config |
| **Quick run command** | `deno test --allow-read --allow-write tools/` |
| **Full suite command** | `deno test --allow-read --allow-write tools/ && echo '{"tool_name":"Bash","tool_input":{"command":"echo hello"},"cwd":"/tmp"}' \| conventions/commit/hooks/validate.sh; echo "exit:$?"` |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run task-level `<automated>` verify command from plan
- **After every plan wave:** Run full suite command
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 19-01-01 | 01 | 1 | COMMIT-01 | — | N/A | automated | `grep -q "GSD workflow" .planning/PROJECT.md && ! grep -q "### Phase 19.1" .planning/ROADMAP.md && echo "PASS"` | N/A (grep) | ⬜ pending |
| 19-01-02 | 01 | 1 | COMMIT-01 | — | N/A | automated + manual | `test -f conventions/commit/CONVENTION.md && head -3 conventions/commit/CONVENTION.md \| grep -c "description:" && tail -3 conventions/commit/CONVENTION.md \| grep -c "Generated by /convention"` | N/A (file check) | ⬜ pending |
| 19-02-01 | 02 | 2 | COMMIT-01 | T-19-02, T-19-05 | Hook rejects GSD refs; safe JSON construction | automated | Three-path verify: passthrough silent + deny GSD ref + allow with injection (see plan 02 task 1 `<automated>`) | N/A (inline) | ⬜ pending |
| 19-02-02 | 02 | 2 | COMMIT-01 | — | N/A | automated | `grep -c "conventions/commit/hooks/validate.sh" .claude/settings.json && test ! -f .claude/hooks/validate-commit-scope.sh && echo "old-hook-removed"` | N/A (grep) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

No Wave 0 test file needed. Rationale:

- Plan 01 tasks use inline file checks (grep, test -f) as automated verification — no test framework needed
- Plan 02 tasks use inline shell pipe tests that exercise the hook directly — three paths tested (passthrough, deny, allow)
- The structural integrity tests in RESEARCH.md (`tools/convention-commit_test.ts`) are post-hoc verification targets for `/gsd-verify-work`, not execution-time prerequisites
- All task `<automated>` verify commands are self-contained and run in < 5 seconds

*Existing deno test infrastructure covers tool tests. No new framework or test file needed for plan execution.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Convention passes delta test | COMMIT-01 | Requires human judgment on LLM knowledge | Read convention, verify no rules that restate basic Conventional Commits |
| Convention is technology-neutral | COMMIT-01 | Semantic check | Read convention, verify no framework/language-specific assumptions |
| Convention provides real value | COMMIT-01 | Subjective quality | Apply convention mentally to recent cckit commits |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify commands
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] No Wave 0 gaps — all verification is inline
- [x] No watch-mode flags
- [x] Feedback latency < 5s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** ready
