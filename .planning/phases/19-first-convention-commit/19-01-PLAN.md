---
phase: 19-first-convention-commit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
  - conventions/commit/CONVENTION.md
autonomous: false
requirements:
  - COMMIT-01
must_haves:
  truths:
    - "PROJECT.md reflects that cckit targets GSD workflow users"
    - "Phase 19.1 is removed from ROADMAP as a separate phase"
    - "A commit message convention file exists at conventions/commit/CONVENTION.md"
    - "The convention was authored via the /convention skill pipeline, not manually"
    - "The convention passes the delta test -- no rules that restate basic Conventional Commits"
    - "The convention includes a GSD reference prohibition rule stated directly"
    - "The convention is technology-neutral"
  artifacts:
    - path: "conventions/commit/CONVENTION.md"
      provides: "Commit message convention rules"
      contains: "description:"
    - path: ".planning/PROJECT.md"
      provides: "Updated project identity"
      contains: "GSD workflow"
    - path: ".planning/ROADMAP.md"
      provides: "Cleaned roadmap without Phase 19.1 as separate phase"
  key_links:
    - from: "conventions/commit/CONVENTION.md"
      to: "conventions/commit/hooks/validate.sh"
      via: "hook reads convention file at runtime for additionalContext injection (Plan 02)"
      pattern: "conventions/commit/CONVENTION.md"
---

<objective>
Update PROJECT.md to reflect that cckit targets GSD workflow users (per D-01), merge Phase 19.1 into Phase 19 in ROADMAP (per D-11), and author the first commit message convention via the `/convention commit` skill.

Purpose: Establishes cckit's refined identity and validates the entire convention authoring pipeline end-to-end by producing a real convention deliverable.
Output: Updated PROJECT.md, updated ROADMAP.md, and `conventions/commit/CONVENTION.md` authored through the /convention skill.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-first-convention-commit/19-CONTEXT.md
@.planning/phases/19-first-convention-commit/19-RESEARCH.md
@docs/CONVENTIONS.md
@skills/convention/SKILL.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update PROJECT.md identity and ROADMAP.md phase structure</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/PROJECT.md (current state -- will be modified)
    - .planning/ROADMAP.md (current state -- will be modified)
    - .planning/phases/19-first-convention-commit/19-CONTEXT.md (D-01 and D-11 decisions)
  </read_first>
  <action>
Two changes:

**1. PROJECT.md identity update (per D-01):**

In the "What This Is" section, change the phrase "for installation into arbitrary host projects" to "for installation into GSD-workflow host projects". The key change: cckit's audience is projects using the GSD workflow. Conventions work without GSD, but skills (/case, /consolidate, /convention) require GSD. Update the paragraph to reflect this.

Specifically in "What This Is", the first paragraph should read:
"A personal Claude Code toolkit that packages skills, agents, and behavioral directives for installation into host projects using the GSD workflow. Ships `/case` (behavioral case discovery), `/consolidate` (spec consolidation after phase ship), and `/convention` (research-driven convention authoring) as GSD workflow extensions: `discuss -> /case -> plan -> execute -> verify -> ship -> /consolidate`."

In the "Core Value" section, change "All artifacts must be project-type agnostic" to "All artifacts must be project-type agnostic. Skills require the GSD workflow; conventions work standalone."

In the Constraints section, update the "GSD conventions" constraint from "Depends on CONTEXT.md, CASES.md, ROADMAP.md, PROJECT.md phase directory structure" to "Skills require GSD workflow (CONTEXT.md, CASES.md, ROADMAP.md, PROJECT.md phase directory structure). Conventions are standalone Claude Code rules."

Do NOT change the "Technology Neutrality" section -- technology neutrality still applies to convention content (project-type agnostic), the change is about audience (GSD users), not about technology scope.

**2. ROADMAP.md Phase 19.1 merge (per D-11):**

Remove the entire "### Phase 19.1: GSD Commit Convention Injection (INSERTED)" section from the ROADMAP. The hook work is now part of Phase 19 Plan 02.

Update the Phase 19 description to reflect it now includes the injection hook:
- Goal should read: "A complete commit message convention exists (base + no tech pack needed), authored via `/convention`, validating the entire authoring pipeline end-to-end. A PreToolUse hook enforces convention rules on commit messages."
- Success Criteria: add a 5th item: "5. A PreToolUse hook exists that injects convention content and mechanically validates commit messages"

In the Execution Order line, remove any reference to 19.1. The order should be: 17 -> 18 -> 19 -> 20 -> 21 -> 22 -> 23

In the Progress table, remove the Phase 19.1 row if it exists.

Also update the "Roadmap Evolution" note in STATE.md if you find reference to Phase 19.1 insertion there -- but only if it's straightforward. The primary target is ROADMAP.md.
  </action>
  <verify>
    <automated>grep -q "GSD workflow" .planning/PROJECT.md && ! grep -q "### Phase 19.1" .planning/ROADMAP.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/PROJECT.md "What This Is" contains "host projects using the GSD workflow"
    - .planning/PROJECT.md "Core Value" contains "Skills require the GSD workflow; conventions work standalone"
    - .planning/PROJECT.md Constraints "GSD conventions" line contains "Skills require GSD workflow"
    - .planning/ROADMAP.md does NOT contain "### Phase 19.1" as a section heading
    - .planning/ROADMAP.md Phase 19 Goal contains "PreToolUse hook"
    - .planning/ROADMAP.md Phase 19 Success Criteria has 5 items
    - .planning/ROADMAP.md Execution Order line does not contain "19.1"
  </acceptance_criteria>
  <done>PROJECT.md reflects GSD-user audience. ROADMAP no longer has Phase 19.1 as a separate phase -- hook work is part of Phase 19.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Author commit convention via /convention skill</name>
  <files>conventions/commit/CONVENTION.md</files>
  <read_first>
    - skills/convention/SKILL.md (skill orchestrator -- understand the invocation flow)
    - .claude/cckit.json (publisher mode config -- confirms output path)
    - docs/CONVENTIONS.md (convention file format spec -- frontmatter, footer, delta test)
    - CLAUDE.md (existing commit rules -- informs what the convention should teach)
    - .planning/phases/19-first-convention-commit/19-CONTEXT.md (D-02 through D-06 content decisions)
  </read_first>
  <action>
Invoke the `/convention commit` skill to author the commit message convention file. Do NOT manually write convention content -- the skill handles research, delta test, user preferences, and generation.

Run: `/convention commit`

The skill will:
1. Detect publisher mode from `.claude/cckit.json` (publisher: true)
2. Resolve output path to `conventions/commit/CONVENTION.md`
3. Detect CREATE mode (file does not exist yet)
4. Present flow choice (research-first vs preferences-first)
5. Dispatch convention-researcher agent to research commit best practices
6. Collect user preferences via adaptive AskUserQuestion loop
7. Dispatch convention-generator with research results + preferences
8. Apply delta test -- only rules that genuinely change Claude's behavior survive
9. Present preview for user approval

**Key content direction (per CONTEXT.md decisions):**
- Scope naming strategy (codebase nouns only) -- HIGH-DELTA candidate
- GSD reference prohibition (phase numbers, plan IDs, requirement IDs, decision IDs) -- HIGH-DELTA candidate, stated directly not abstracted
- Commit granularity/atomicity guidance
- Subject/body quality rules ("why not what")
- Strict delta test: basic Conventional Commits format, imperative mood, 72-char subjects, standard type list are NO-DELTA and should be filtered out

**Quality criteria:** Practicality (helpful for cckit's own commits), conciseness (not too many rules), example quality (concrete and genuinely helpful).

**Empty convention is valid:** If delta test eliminates all rules, that is a legitimate outcome.

**What NOT to do:** Do NOT modify CLAUDE.md. Do NOT copy the convention to .claude/rules/. Both are Phase 20 scope.

**CHECKPOINT:** After the skill completes and the convention file is written, verify with the user:
1. The skill invocation `/convention commit` completed successfully
2. A file exists at `conventions/commit/CONVENTION.md`
3. The file has `description:` frontmatter (no `paths` field -- base convention)
4. The file has a footer comment with timestamp: `<!-- Generated by /convention, ... -->`
5. The file includes GSD reference prohibition stated directly
6. No rules that restate basic Conventional Commits (imperative mood, standard types, 72-char line)
7. The convention is technology-neutral -- no language or framework references
8. Rules are concise and practical

User types "approved" to continue, or describes issues for re-generation.
  </action>
  <verify>
    <automated>test -f conventions/commit/CONVENTION.md && head -3 conventions/commit/CONVENTION.md | grep -c "description:" && tail -3 conventions/commit/CONVENTION.md | grep -c "Generated by /convention"</automated>
  </verify>
  <acceptance_criteria>
    - conventions/commit/CONVENTION.md exists
    - File frontmatter contains `description:` field
    - File frontmatter does NOT contain `paths:` field (base convention -- always loads)
    - File ends with a footer comment containing "Generated by /convention" and an ISO 8601 timestamp
    - File contains a GSD reference prohibition section mentioning phase numbers, plan IDs, requirement IDs, decision IDs
    - File does NOT contain rules restating basic Conventional Commits format (imperative mood, standard types list, 72-char subject line)
    - File contains no language-specific or framework-specific references (technology-neutral)
  </acceptance_criteria>
  <done>Convention file authored via /convention skill, approved by user, exists at conventions/commit/CONVENTION.md with proper frontmatter, delta-tested content, and GSD reference prohibition.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries in this plan -- all work is local documentation and markdown content generation via existing skill pipeline.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Information Disclosure | Convention content | accept | Convention file is public repository content; no secrets involved |
</threat_model>

<verification>
- `conventions/commit/CONVENTION.md` exists with proper frontmatter
- Convention has `description:` field, no `paths` field
- Convention has footer comment with ISO 8601 timestamp
- Convention content includes GSD reference prohibition
- PROJECT.md has GSD-user audience language
- ROADMAP.md has no Phase 19.1 section
</verification>

<success_criteria>
1. PROJECT.md updated with GSD-user audience (per D-01)
2. ROADMAP.md cleaned of Phase 19.1 (per D-11)
3. `conventions/commit/CONVENTION.md` exists, authored via /convention skill
4. Convention passes delta test -- no NO-DELTA content
5. Convention includes GSD reference prohibition stated directly (per D-03)
6. Convention is technology-neutral
</success_criteria>

<output>
After completion, create `.planning/phases/19-first-convention-commit/19-01-SUMMARY.md`
</output>
