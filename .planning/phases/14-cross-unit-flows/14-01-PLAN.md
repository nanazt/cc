---
phase: 14-cross-unit-flows
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MODEL.md
  - agents/spec-consolidator.md
  - skills/consolidate/SKILL.md
  - agents/e2e-flows.md
  - docs/STACK.md
  - CLAUDE.md
autonomous: true
requirements:
  - FLOW-03
  - FLOW-04

must_haves:
  truths:
    - "Dependencies section in MODEL.md specifies a structured format with bold component names"
    - "spec-consolidator agent is instructed to write Dependencies using the structured format"
    - "SKILL.md Step 3.5 parses structured dependency entries by bold component name instead of scanning natural language"
    - "e2e-flows.md Step Table template uses {operation} without HTTP call bias"
    - "No external file references IMPL-SPEC as a current document"
  artifacts:
    - path: "docs/MODEL.md"
      provides: "Structured Dependencies format specification"
      contains: "**{component-name}** --"
    - path: "agents/spec-consolidator.md"
      provides: "Updated Dependencies output instruction"
      contains: "bold component name"
    - path: "skills/consolidate/SKILL.md"
      provides: "Updated Step 3.5 structured dependency parsing"
      contains: "bold"
    - path: "agents/e2e-flows.md"
      provides: "Bias-free Step Table template"
    - path: "docs/STACK.md"
      provides: "IMPL-SPEC references replaced"
    - path: "CLAUDE.md"
      provides: "IMPL-SPEC commit example replaced"
  key_links:
    - from: "docs/MODEL.md"
      to: "agents/spec-consolidator.md"
      via: "Dependencies section format specification consumed by consolidator"
      pattern: "bold component name"
    - from: "docs/MODEL.md"
      to: "skills/consolidate/SKILL.md"
      via: "Step 3.5 parses Dependencies using MODEL.md-defined format"
      pattern: "bold"
---

<objective>
Update agent definitions, model specification, and documentation to complete the universal E2E flow pipeline: structured Dependencies format for deterministic flow discovery, bias removal in e2e-flows agent, and IMPL-SPEC reference cleanup.

Purpose: Make cross-component flow discovery deterministic (structured format replaces NLP-dependent prose scanning) and remove the last protocol-specific bias from the E2E flows agent.
Output: 6 modified files with structured Dependencies format, bias-free agent template, and current documentation references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-cross-unit-flows/14-CONTEXT.md
@.planning/phases/14-cross-unit-flows/14-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define structured Dependencies format and update agent/skill parsing</name>
  <files>docs/MODEL.md, agents/spec-consolidator.md, skills/consolidate/SKILL.md, agents/e2e-flows.md</files>
  <read_first>
    - docs/MODEL.md (full file -- Dependencies is section #6 at line 56, guide text definition)
    - agents/spec-consolidator.md (full file -- find Dependencies output instructions)
    - skills/consolidate/SKILL.md (lines 170-181 -- Step 3.5 flow discovery with dependency scanning)
    - agents/e2e-flows.md (line 52 -- Step Table template with bias)
  </read_first>
  <action>
**docs/MODEL.md -- Dependencies section format (section #6, around line 56):**

After the existing line:
```
6. **Dependencies** -- What this component requires from other components or external systems
```

Add a new subsection under the Context Sections block (after the guide text style paragraph, before "### Conditional Sections"). Insert a "### Dependencies Format" subsection with this exact content:

```markdown
### Dependencies Format

The Dependencies section uses a structured format for cross-component references. Each dependency is a bullet with a bold component name followed by a description:

```markdown
## Dependencies
- **{component-name}** -- {what this component requires from it}
```

Example:
```markdown
## Dependencies
- **auth** -- caller identity and session validation
- **notification** -- sends confirmation after account changes
```

The bold component name (`**{name}**`) is the parsing anchor. SKILL.md Step 3.5 uses this format to discover cross-component flows deterministically. Free-text prose dependencies (external systems, libraries) do not use bold formatting and are ignored by flow discovery.
```

Place this subsection after the "Neutrality Validation" subsection and before "## Section Overrides". This keeps all section-format details in the Sections area of MODEL.md.

**agents/spec-consolidator.md -- Dependencies output instruction:**

Find the Output Format section's context.md template (around line 42-58). After the template block showing `## {Section Name from sections dispatch tag}`, add a new paragraph:

```markdown
**Dependencies section format:** When writing the Dependencies section, use structured entries for cross-component references: `- **{component-name}** -- {description}`. The bold component name is the parsing anchor for flow discovery. External dependencies (libraries, databases, third-party services) use plain text without bold formatting.
```

**skills/consolidate/SKILL.md -- Step 3.5 structured parsing (lines 176-180):**

Replace the current step 2:
```
2. For each changed component, check Dependencies section for cross-component references.
```

With:
```
2. For each changed component, parse Dependencies section for structured cross-component entries. Look for bold component names matching the pattern `- **{name}** --`. Each bold name that matches a known component from the schema is a cross-component dependency. Ignore non-bold entries (external dependencies).
```

**agents/e2e-flows.md -- bias fix (line 52):**

Change line 52 from:
```
| 1 | {caller} | {component} | {operation or HTTP call} | {key fields} | {Component.Op} |
```
To:
```
| 1 | {caller} | {component} | {operation} | {key fields} | {Component.Op} |
```

This removes "or HTTP call" which is protocol-specific bias per the Phase 12.1 neutralization principle (per D-11).
  </action>
  <verify>
    <automated>grep -n "bold component name" docs/MODEL.md && grep -n "bold component name" agents/spec-consolidator.md && grep -c "HTTP call" agents/e2e-flows.md | grep "^0$" && grep -n "bold component names" skills/consolidate/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/MODEL.md contains "### Dependencies Format" subsection
    - docs/MODEL.md contains the pattern `- **{component-name}** -- {what this component requires from it}`
    - docs/MODEL.md contains "bold component name" as parsing anchor description
    - agents/spec-consolidator.md contains "Dependencies section format:" instruction paragraph
    - agents/spec-consolidator.md contains `- **{component-name}** -- {description}` in the instruction
    - skills/consolidate/SKILL.md Step 3.5 step 2 contains "bold component names" and `**{name}**` pattern
    - skills/consolidate/SKILL.md Step 3.5 step 2 does NOT contain "check Dependencies section for cross-component references" (old text)
    - agents/e2e-flows.md line 52 contains `{operation}` and does NOT contain `HTTP call`
    - agents/e2e-flows.md grep for "HTTP call" returns 0 matches
  </acceptance_criteria>
  <done>Dependencies section in MODEL.md has structured format spec with bold anchor. spec-consolidator knows to write bold entries. SKILL.md Step 3.5 parses bold entries deterministically. e2e-flows.md Step Table template is bias-free.</done>
</task>

<task type="auto">
  <name>Task 2: Replace external IMPL-SPEC references with current authoritative sources</name>
  <files>docs/STACK.md, CLAUDE.md</files>
  <read_first>
    - docs/STACK.md (lines 55-135 -- the 3 IMPL-SPEC references at lines 62, 74, 130)
    - CLAUDE.md (lines 48-67 -- commit examples section with IMPL-SPEC reference at line 58)
  </read_first>
  <action>
**docs/STACK.md line 62** -- Current:
```
| `model` | No | Yes (`sonnet` or `opus` per IMPL-SPEC) |
```
Replace with:
```
| `model` | No | Yes (`sonnet` or `opus` per MODEL.md and agent frontmatter) |
```

**docs/STACK.md line 74** -- Current:
```
### Agent Model Assignments (from IMPL-SPEC)
```
Replace with:
```
### Agent Model Assignments (from MODEL.md)
```

**docs/STACK.md line 130** -- Current:
```
| Markdown parser | unified + remark-parse | Manual regex | IMPL-SPEC explicitly rejected. 40+ lines of fragile code vs. 10 lines of AST traversal. |
```
Replace with:
```
| Markdown parser | unified + remark-parse | Manual regex | Rejected early in project design. 40+ lines of fragile code vs. 10 lines of AST traversal. |
```

**CLAUDE.md line 58** -- Current:
```
docs: update IMPL-SPEC with review findings
```
Replace with:
```
docs: update MODEL.md with section override syntax
```

This replacement uses a realistic commit example referencing a current file (MODEL.md) instead of a deleted one (per D-05).
  </action>
  <verify>
    <automated>grep -c "IMPL-SPEC" docs/STACK.md | grep "^0$" && grep -c "IMPL-SPEC" CLAUDE.md | grep "^0$" && grep "per MODEL.md and agent frontmatter" docs/STACK.md && grep "from MODEL.md" docs/STACK.md && grep "update MODEL.md with section override syntax" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/STACK.md contains 0 occurrences of "IMPL-SPEC"
    - docs/STACK.md line 62 area contains "per MODEL.md and agent frontmatter"
    - docs/STACK.md line 74 area contains "Agent Model Assignments (from MODEL.md)"
    - docs/STACK.md line 130 area contains "Rejected early in project design"
    - CLAUDE.md contains 0 occurrences of "IMPL-SPEC"
    - CLAUDE.md line 58 area contains "docs: update MODEL.md with section override syntax"
  </acceptance_criteria>
  <done>All external IMPL-SPEC references point to current authoritative sources. No file outside .planning/ references IMPL-SPEC as a current document.</done>
</task>

</tasks>

<verification>
- `grep -rc "IMPL-SPEC" docs/STACK.md CLAUDE.md` returns 0 for both files
- `grep -c "HTTP call" agents/e2e-flows.md` returns 0
- `grep "Dependencies Format" docs/MODEL.md` finds the new subsection
- `grep "bold component name" agents/spec-consolidator.md` finds the instruction
- `grep "bold component names" skills/consolidate/SKILL.md` finds the updated Step 3.5
- `deno test tools/ --allow-read` -- all 19 existing tests still pass (no tool changes)
</verification>

<success_criteria>
1. MODEL.md defines structured Dependencies format with bold component name as parsing anchor
2. spec-consolidator.md instructs agents to write Dependencies in structured format
3. SKILL.md Step 3.5 parses bold entries deterministically instead of scanning natural language
4. e2e-flows.md Step Table template uses `{operation}` without protocol-specific bias
5. Zero external IMPL-SPEC references remain in docs/STACK.md and CLAUDE.md
6. All 19 existing deno tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/14-cross-unit-flows/14-01-SUMMARY.md`
</output>
