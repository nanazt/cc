---
phase: 15-artifact-paths-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/consolidate/SKILL.md
  - docs/IMPL-SPEC.md
  - .gitignore
autonomous: true
requirements:
  - PIPE-01
  - PIPE-05
  - PIPE-06
  - CASE-08
gap_closure: true
must_haves:
  truths:
    - "SKILL.md references specs/ (project-root relative) in all path occurrences -- zero .planning/specs/ remain"
    - "IMPL-SPEC.md does not exist in the repository"
    - ".gitignore contains no orphaned IMPL-SPEC.md entry"
    - "Memory file reflects the deletion is complete, not pending"
  artifacts:
    - path: "skills/consolidate/SKILL.md"
      provides: "Corrected specs/ paths in orchestrator prompt"
      contains: "specs/{component}/"
    - path: ".gitignore"
      provides: "Clean ignore file without orphaned entries"
  key_links:
    - from: "skills/consolidate/SKILL.md"
      to: "agents/spec-consolidator.md"
      via: "specs/ path convention"
      pattern: "specs/.*context\\.md|specs/.*cases\\.md"
    - from: "skills/consolidate/SKILL.md"
      to: "docs/MODEL.md"
      via: "specs/ path convention"
      pattern: "specs/\\{component\\}/"
---

<objective>
Fix 7 incorrect `.planning/specs/` path references in the consolidate SKILL.md orchestrator and delete the stale IMPL-SPEC.md document with its orphaned .gitignore entry.

Purpose: Close integration gaps INT-02 (path mismatch between SKILL.md and all other production artifacts) and INT-03 (stale IMPL-SPEC.md contradicts current pipeline behavior). After this, all production artifacts agree on `specs/` as the canonical output path.

Output: Corrected SKILL.md, deleted IMPL-SPEC.md, cleaned .gitignore, updated memory file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-artifact-paths-cleanup/15-CONTEXT.md
@.planning/phases/15-artifact-paths-cleanup/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix specs/ path references in SKILL.md</name>
  <files>skills/consolidate/SKILL.md</files>
  <read_first>
    - skills/consolidate/SKILL.md (the file being modified -- executor must see current state)
    - .planning/phases/15-artifact-paths-cleanup/15-CONTEXT.md (D-01 locked decision: fix all 7 occurrences)
  </read_first>
  <action>
Replace all 7 occurrences of `.planning/specs/` with `specs/` in skills/consolidate/SKILL.md. The replacements are identical mechanical substitutions -- the `.planning/` prefix is removed, leaving `specs/` intact.

**Exact replacements (line numbers as of current file):**

1. **Line 21** (purpose section): Change `at \`.planning/specs/{component}/\`` to `at \`specs/{component}/\``
2. **Line 120** (Step 2 dispatch table, output_context): Change `\`.planning/specs/{component}/context.md\`` to `\`specs/{component}/context.md\``
3. **Line 121** (Step 2 dispatch table, output_cases): Change `\`.planning/specs/{component}/cases.md\`` to `\`specs/{component}/cases.md\``
4. **Line 133** (Step 2 error handling, abort command): Change `git checkout -- .planning/specs/` to `git checkout -- specs/`
5. **Line 141** (Step 3, INDEX.md write path): Change `Write \`.planning/specs/INDEX.md\`` to `Write \`specs/INDEX.md\``
6. **Line 264** (Step 7 commit, git stage path): Change `Stage \`.planning/specs/\`` to `Stage \`specs/\``
7. **Line 266** (Step 7 rollback, git checkout path): Change `git checkout -- .planning/specs/` to `git checkout -- specs/`

Per D-01: all 7 occurrences, no more, no less. Do NOT modify any files in `.planning/` -- those are frozen historical artifacts.

After editing, verify with: `grep -c '\.planning/specs/' skills/consolidate/SKILL.md` (must return 0).
Also verify replacements exist: `grep -c 'specs/' skills/consolidate/SKILL.md` (must return 7 or more, since existing correct references like `<existing_spec>` also contain `specs/`).
  </action>
  <verify>
    <automated>grep -c '\.planning/specs/' skills/consolidate/SKILL.md | grep '^0$' && grep -c 'specs/' skills/consolidate/SKILL.md | xargs test 7 -le</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\.planning/specs/' skills/consolidate/SKILL.md` returns exactly `0`
    - `grep -c 'specs/' skills/consolidate/SKILL.md` returns 7 or greater
    - Line 21 contains `at \`specs/{component}/\`` (no `.planning/` prefix)
    - Line 120 contains `specs/{component}/context.md` (no `.planning/` prefix)
    - Line 121 contains `specs/{component}/cases.md` (no `.planning/` prefix)
    - Line 133 contains `git checkout -- specs/` (no `.planning/` prefix)
    - Line 141 contains `specs/INDEX.md` (no `.planning/` prefix)
    - Line 264 contains `Stage \`specs/\`` (no `.planning/` prefix)
    - Line 266 contains `git checkout -- specs/` (no `.planning/` prefix)
    - No files in `.planning/` directory are modified (check with `git diff --name-only | grep -c '\.planning/'` returns 0)
  </acceptance_criteria>
  <done>All 7 .planning/specs/ references in SKILL.md replaced with specs/. Zero occurrences of .planning/specs/ remain. SKILL.md now matches the path convention used by all agents, MODEL.md, and test fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Delete IMPL-SPEC.md, clean .gitignore, update memory</name>
  <files>docs/IMPL-SPEC.md, .gitignore, ~/.claude/projects/-Users-syr-Developments-cckit/memory/project_impl_spec_stale.md</files>
  <read_first>
    - .gitignore (the file being modified -- executor must see current state to locate line 230)
    - ~/.claude/projects/-Users-syr-Developments-cckit/memory/project_impl_spec_stale.md (the memory file being updated)
    - .planning/phases/15-artifact-paths-cleanup/15-CONTEXT.md (D-02, D-03, D-04 locked decisions)
  </read_first>
  <action>
Three sub-steps, all per locked decisions D-02, D-03, D-04:

**Step A: Delete IMPL-SPEC.md (per D-03)**

Run `git rm docs/IMPL-SPEC.md` to remove the file from both git index and working tree. This is a hard delete -- no redirect stub, no CLAUDE.md memo. MODEL.md and agent frontmatter are already the authoritative sources.

Verify: `test ! -f docs/IMPL-SPEC.md` (file must not exist).

**Step B: Remove orphaned .gitignore entry (per D-02)**

Remove the line `docs/IMPL-SPEC.md` from `.gitignore`. This is line 230 in the current file. The surrounding context is:

```
# End of https://www.toptal.com/developers/gitignore/api/macos,windows,node,linux,deno

docs/IMPL-SPEC.md    <-- REMOVE THIS LINE
.claude/worktrees
```

After removal, the file should read:
```
# End of https://www.toptal.com/developers/gitignore/api/macos,windows,node,linux,deno

.claude/worktrees
```

Verify: `grep -c 'IMPL-SPEC' .gitignore` returns 0.

**Step C: Update memory file (per D-04)**

Overwrite `~/.claude/projects/-Users-syr-Developments-cckit/memory/project_impl_spec_stale.md` with:

```markdown
---
name: IMPL-SPEC.md deleted
description: docs/IMPL-SPEC.md was deleted as a gap closure item. The file was stale and contained incorrect paths. MODEL.md and agent frontmatter are the authoritative sources.
type: project
---

docs/IMPL-SPEC.md has been deleted. It was a stale transition document for Phases 11-14 that contained incorrect paths (`.planning/specs/` instead of the correct `specs/`).

**Current state:** Deleted. MODEL.md and agent frontmatter are the authoritative sources for consolidation pipeline documentation.
```

Also update `~/.claude/projects/-Users-syr-Developments-cckit/memory/MEMORY.md` to change the IMPL-SPEC line item from the current text to reflect deletion is complete (change `[IMPL-SPEC.md is stale]` to `[IMPL-SPEC.md deleted]` and update the description).
  </action>
  <verify>
    <automated>test ! -f docs/IMPL-SPEC.md && grep -c 'IMPL-SPEC' .gitignore | grep '^0$' && grep -q 'deleted' ~/.claude/projects/-Users-syr-Developments-cckit/memory/project_impl_spec_stale.md</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f docs/IMPL-SPEC.md` succeeds (file does not exist)
    - `git status docs/IMPL-SPEC.md` shows deletion staged or committed
    - `grep -c 'IMPL-SPEC' .gitignore` returns exactly `0`
    - `.gitignore` line after "End of toptal" comment block is `.claude/worktrees` (no blank line with orphaned entry between)
    - `~/.claude/projects/-Users-syr-Developments-cckit/memory/project_impl_spec_stale.md` contains the string `has been deleted`
    - `~/.claude/projects/-Users-syr-Developments-cckit/memory/project_impl_spec_stale.md` frontmatter name field is `IMPL-SPEC.md deleted`
    - `~/.claude/projects/-Users-syr-Developments-cckit/memory/MEMORY.md` references `[IMPL-SPEC.md deleted]` (not `[IMPL-SPEC.md is stale]`)
  </acceptance_criteria>
  <done>IMPL-SPEC.md removed from repository. .gitignore cleaned of orphaned entry. Memory file updated to reflect deletion is complete. No production files reference IMPL-SPEC.md.</done>
</task>

</tasks>

<verification>
All phase success criteria checked after both tasks complete:

1. `grep -c '\.planning/specs/' skills/consolidate/SKILL.md` returns `0` -- INT-02 closed
2. `test ! -f docs/IMPL-SPEC.md` succeeds -- INT-03 closed
3. `grep -c 'IMPL-SPEC' .gitignore` returns `0` -- orphaned entry removed
4. `deno test` passes -- no behavioral regressions from mechanical cleanup
5. `git diff --name-only` shows only: skills/consolidate/SKILL.md, .gitignore (plus staged deletion of docs/IMPL-SPEC.md)
</verification>

<success_criteria>
- Zero occurrences of `.planning/specs/` in skills/consolidate/SKILL.md
- docs/IMPL-SPEC.md does not exist in repository
- .gitignore has no IMPL-SPEC.md entry
- Memory file reflects completed deletion
- Existing deno test suite passes (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/15-artifact-paths-cleanup/15-01-SUMMARY.md`
</output>
