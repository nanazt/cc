---
phase: 13-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/spec-verifier.md
  - docs/IMPL-SPEC.md
autonomous: true
requirements:
  - VRFY-01
  - VRFY-02
must_haves:
  truths:
    - "Verifier checks section presence against schema-declared sections, not a hardcoded list"
    - "V-10 check description uses structural placeholders, not service-biased examples"
    - "V-11 skips with explicit reason when no interface mapping exists in Public Interface"
    - "V-15 skips with explicit reason when fewer than 2 components have structured error identifiers"
    - "V-29 remains conditional on E2E opt-in"
    - "Verifier performs conditional section re-evaluation and reports contradictions as T2"
    - "V-20 and V-21 are automated T2 checks, not human-only"
    - "Only V-24 remains in the human-only checklist"
  artifacts:
    - path: "agents/spec-verifier.md"
      provides: "Complete verification agent with 27 checks"
      min_lines: 200
      contains: "model: opus"
    - path: "docs/IMPL-SPEC.md"
      provides: "Transferred annotation on spec-verifier section"
      contains: "Transferred to agents/spec-verifier.md"
  key_links:
    - from: "agents/spec-verifier.md"
      to: "schema-parser.ts JSON"
      via: "schema_data dispatch tag in input contract"
      pattern: "schema_data"
    - from: "agents/spec-verifier.md"
      to: "phase CONTEXT.md"
      via: "phase_context_file dispatch tag"
      pattern: "phase_context_file"
    - from: "agents/spec-verifier.md"
      to: "specs/ directory"
      via: "specs_dir dispatch tag"
      pattern: "specs_dir"
---

<objective>
Create the spec-verifier agent -- an opus-model, read-only verification agent that checks consolidated spec files for correctness, consistency, and compliance with consolidation rules. All 27 checks are parameterized against the active schema and follow technology-neutral conventions.

Purpose: The verifier is the quality gate between consolidation and commit. It catches consolidator (sonnet) mistakes before specs become authoritative. This plan creates the agent definition and annotates IMPL-SPEC as transferred.

Output: `agents/spec-verifier.md` (new), `docs/IMPL-SPEC.md` (annotated)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-verification/13-CONTEXT.md
@.planning/phases/13-verification/13-RESEARCH.md
</context>

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From agents/case-validator.md (structural reference for opus read-only agent):
- YAML frontmatter: name, description, tools (Read, Grep), model: opus
- Body structure: Mission paragraph, Methodology, Input Contract (XML tag table), Output Contract, Quality Gate checklist
- Return protocol: ## VALIDATION COMPLETE / ## VALIDATION FAILED with structured findings

From agents/spec-consolidator.md (dispatch tag pattern reference):
- XML dispatch tags: <objective>, <component>, <sections>, <conditional_sections>, <rule_prefix>, <files_to_read>, etc.
- Tags documented in Input Contract table with Required/How You Use It columns

From tools/schema-parser.ts JSON output structure:
```json
{
  "meta": { "version": 1, "rulePrefix": "CR", "e2eFlows": false },
  "components": [{ "name": "init", "description": "...", "type": "" }],
  "sections": {
    "default": {
      "context": [{ "name": "Overview", "guide": "..." }],
      "conditional": [{ "name": "State Lifecycle", "condition": "..." }]
    }
  }
}
```

From docs/IMPL-SPEC.md line 343: current spec-verifier section header is "## Agent: spec-verifier"
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create agents/spec-verifier.md</name>
  <files>agents/spec-verifier.md</files>
  <read_first>
    - agents/case-validator.md (structural reference: opus read-only agent pattern, frontmatter format, methodology structure, quality gate pattern)
    - agents/spec-consolidator.md (dispatch tag pattern: XML tag table format, input contract layout)
    - agents/e2e-flows.md (first 50 lines: frontmatter + input contract pattern for cross-reference)
    - docs/IMPL-SPEC.md lines 343-418 (current spec-verifier contract: input tags, return protocol, quality gate)
    - docs/IMPL-SPEC.md lines 690-740 (verification checks inventory: all 28 checks with tiers and descriptions)
    - .planning/phases/13-verification/13-CONTEXT.md (ALL 27 locked decisions D-01 through D-27 -- these OVERRIDE IMPL-SPEC)
    - .planning/phases/13-verification/13-RESEARCH.md (reconciled check inventory, code examples, anti-patterns)
  </read_first>
  <action>
Create `agents/spec-verifier.md` following the established agent pattern from case-validator.md. The agent has these structural sections:

**YAML Frontmatter (per D-20, D-21, D-22):**

The frontmatter block must contain exactly these fields (see RESEARCH.md Example 1 for the exact YAML):
- name: spec-verifier
- description: multi-line describing read-only verification, syntactic correctness, cross-component consistency, consolidation rule compliance
- tools: Read, Grep, Glob (three items)
- model: opus

CRITICAL: No `maxTurns` field (D-21 explicitly rejected it). Tools are Read, Grep, Glob only -- no Write, no Bash (D-22).

**Body structure (following case-validator.md pattern):**

1. **Title and mission paragraph:** "# Spec Verifier" followed by mission description. Emphasize read-only nature, schema-parameterized checks, structured findings output.

2. **Input Contract table (10 tags, per D-07, D-08, D-09, D-19):**

| Tag | Required | How You Use It |
|-----|----------|----------------|
| `<objective>` | Yes | Mission statement. Wording: "Verify consolidated specs for Phase {id}. Check all applicable verification items." (per D-19 -- never hardcode a number) |
| `<schema_data>` | Yes | Pre-parsed schema JSON from schema-parser.ts. Contains components, sections (with type overrides), meta config. Use to resolve section lists per component. (D-07: NEW tag) |
| `<specs_dir>` | Yes | Path to `specs/` directory root. Read all component spec files from here. |
| `<index_file>` | Yes | Path to `specs/INDEX.md`. For V-07, V-08, V-12, V-13 checks. |
| `<phase_id>` | Yes | Phase identifier being verified. For provenance tag validation. |
| `<phase_context_file>` | Yes | Path to phase CONTEXT.md. For V-20, V-21 automation and conditional section re-evaluation. (D-08: NEW tag) |
| `<phase_cases_file>` | No | Path to source phase CASES.md. For V-14 PR count verification. Omit for Phase 1 backfill. (D-09: retained) |
| `<project_file>` | Yes | Path to PROJECT.md. For V-25, V-26, V-27. |
| `<changed_components>` | Yes | JSON manifest from consolidator results. Same structure as E2E agent input. |
| `<e2e_dir>` | No | Path to `specs/e2e/`. Omit if E2E flows disabled or no flows exist. For V-29. |

3. **Schema Resolution section:** Explain how to resolve a component's section list from `<schema_data>`:
   - Read `components[].type` for each component
   - If type is non-empty, use `sections[type]` block
   - If type is empty, use `sections["default"]` block
   - NEVER use hardcoded section names -- always read from resolved schema block

4. **Methodology section with all 27 checks organized by tier.** For each check, include: ID, Tier, Description, Check logic. Apply these CONTEXT.md transforms:

   **Tier 1 (10 checks) -- V-01, V-02, V-07, V-08, V-09, V-16, V-17, V-25, V-26, V-29:**
   - V-01: Provenance tag check. Grep for `(Source: Phase` in all specs/ files. Unchanged.
   - V-02: No empty sections. Parse H2 sections, check for content. Unchanged.
   - V-07: INDEX.md Components table matches specs/ directory. Unchanged.
   - V-08: INDEX.md links resolve to existing files. Unchanged.
   - V-09: No fabricated cases. For components whose source phase has no CASES.md, verify cases.md does not exist or contains only CR references. Unchanged.
   - V-16: Latest-wins. No duplicate operations across phases. Unchanged.
   - V-17: Forward Concerns not in specs/. Unchanged.
   - V-25: GR references resolve to PROJECT.md entries. Unchanged.
   - V-26: No GR content duplication. Unchanged.
   - V-29: E2E Spec References validity. (D-06: retain, already conditional on `<e2e_dir>` being provided)

   **Tier 2 (11 checks) -- V-05, V-10, V-11, V-14, V-15, V-18, V-20, V-21, V-22, V-27, V-28:**
   - V-05: CR format consistency. Unchanged.
   - V-10: Cross-component operation references resolve. (D-02: UNIVERSALIZE -- replace any service-biased examples with structural placeholders like `[Component].[OperationName]`). Check logic unchanged.
   - V-11: Interface-operation mapping. (D-03: CONDITIONALIZE -- execute ONLY when a component's Public Interface section contains operation-to-entry mapping. Skip with reason: "Skipped: V-11 (no interface mapping in [component] Public Interface)")
   - V-14: All PRs promoted to CR (delta comparison). Unchanged.
   - V-15: Error identifier consistency. (D-04: CONDITIONALIZE -- execute cross-component comparison ONLY when structured error identifiers exist in 2+ component Error Handling sections. Skip with reason: "Skipped: V-15 (structured error identifiers found in fewer than 2 components)")
   - V-18: Backfill provenance correctness. Unchanged.
   - V-20: Semantic correctness. (D-12: PROMOTED from Human-Only to automated T2. Compare CONTEXT.md decisions against spec content.)
   - V-21: Completeness. (D-13: PROMOTED from Human-Only to automated T2. Extract decision list from CONTEXT.md, verify each behavioral decision is present in specs.)
   - V-22: Classification accuracy. (D-14: PARTIALLY AUTOMATED. Compare CASES.md operation-component assignments against spec placement. Mismatches as T2, remaining ambiguities as T3.)
   - V-27: Schema registry match. (D-05: retain, already universal)
   - V-28: CR keyword overlap detection. Unchanged.

   **Tier 3 (6 checks) -- V-03, V-04, V-06, V-12, V-13, V-19:**
   - V-03: Operation name format `{Component}.{Op}` PascalCase. Unchanged.
   - V-04: Schema-defined sections present. (D-01: retain, already universal. MUST read section names from resolved `<schema_data>` block per component type -- NEVER enumerate section names in check description)
   - V-06: No phase-contextual content. Unchanged.
   - V-12: INDEX.md no stale entries. Unchanged.
   - V-13: INDEX.md attribution matches provenance. Unchanged.
   - V-19: Shared type consistency. Unchanged.

   **Human-Only (1 check):**
   - V-24: E2E accuracy. (D-16: remains human-only. Present as checklist item.)

   **Dropped:** V-23 (D-15: dropped, covered by V-01, V-02, V-03, V-05, V-07, V-17).

5. **Conditional Section Re-evaluation section (D-11):** Integrate as an extended behavior within the methodology (research recommends folding into V-04 logic area). Steps:
   - Read conditional section list from `<schema_data>` (the `conditional[]` arrays)
   - Read phase CONTEXT.md (from `<phase_context_file>`) and CASES.md (from `<phase_cases_file>`) for evidence
   - Read each component's context.md to find HTML comments logging inclusion/exclusion decisions
   - Independently assess whether each conditional section should be included or excluded
   - If assessment contradicts consolidator's logged decision (the HTML comment), report as T2 finding
   - This cross-checks sonnet (consolidator) with opus (verifier)

6. **Return Protocol (per D-17, D-18):** Structure the return with these sections (see RESEARCH.md Example 4 for exact format):
   - `## VERIFICATION COMPLETE` heading
   - `### Tier 1 Findings (block confirmation)` -- numbered list or "None."
   - `### Tier 2 Findings (developer decides)` -- numbered list or "None."
   - `### Tier 3 Findings (informational)` -- numbered list or "None."
   - `### Human-Only Checks` -- checklist with V-24 only
   - `### Skipped Checks` -- list with reasons per D-18
   - `Summary: Checked=N/27 Skipped=N | T1=N T2=N T3=N | Verdict: {PASS / FINDINGS}`

   On failure: `## VERIFICATION FAILED` with Reason.

   Note: Check count is dynamic per D-17. Summary shows `Checked=N/Total` where Total is discovered at runtime.

7. **Quality Gate Checklist:** Adapt from IMPL-SPEC quality gate but apply D-15 (no V-23), D-17 (no "28"), D-19 (no hardcoded count):
   - [ ] All applicable verification checks executed (or explicitly noted as skipped with reason)
   - [ ] Each finding references specific file path and section
   - [ ] Findings correctly classified into tiers (T1/T2/T3/Human)
   - [ ] No false positives from Phase 1 backfill (context.md only, no cases.md expected)
   - [ ] V-09 (no fabricated cases) explicitly checked and reported
   - [ ] V-14 (PR to CR count) uses delta comparison against source CASES.md
   - [ ] V-25/V-26/V-27 checked against PROJECT.md
   - [ ] V-29 checked for E2E Spec References validity (if E2E flows exist)
   - [ ] Skipped checks listed with explicit reasons (per D-18)
   - [ ] Conditional section re-evaluation performed (per D-11)
   - [ ] V-20 and V-21 reported in Tier 2 findings (not Human-Only)

**Technology neutralization (D-23, aligning with Phase 12.1):**
ALL check descriptions use structural placeholders:
- `[Component]` not specific component names
- `[OperationName]` not specific operation names
- `[error description] (ErrorName)` not HTTP status codes
- No route paths, no API-specific terminology, no service-biased language

**Anti-patterns to verify against:**
- No `maxTurns` in frontmatter
- No "Check all 28" or any hardcoded count in objective or body
- No hardcoded section names in V-04 description
- No service-biased examples anywhere
- V-20 and V-21 are NOT in Human-Only section
  </action>
  <verify>
    <automated>grep -c "model: opus" agents/spec-verifier.md && grep -c "maxTurns" agents/spec-verifier.md | grep "0" && grep -c "schema_data" agents/spec-verifier.md && grep -c "phase_context_file" agents/spec-verifier.md && grep "Check all applicable" agents/spec-verifier.md && grep -c "V-23" agents/spec-verifier.md | grep "0" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - agents/spec-verifier.md exists and has 200+ lines
    - Frontmatter contains `model: opus` (exact string)
    - Frontmatter contains `- Read`, `- Grep`, `- Glob` in tools list
    - Frontmatter does NOT contain `maxTurns` (grep returns 0 matches)
    - Frontmatter does NOT contain `- Write` or `- Bash`
    - Body contains `schema_data` tag in input contract table
    - Body contains `phase_context_file` tag in input contract table
    - Body contains the string "Check all applicable verification items" (D-19)
    - Body does NOT mention V-23 anywhere (D-15: dropped)
    - V-11 description contains "Skip" and "no interface mapping" (D-03 conditionalization)
    - V-15 description contains "Skip" and "fewer than 2 components" (D-04 conditionalization)
    - V-20 appears under Tier 2, NOT under Human-Only
    - V-21 appears under Tier 2, NOT under Human-Only
    - V-24 appears under Human-Only section
    - Body contains "conditional section re-evaluation" or equivalent for D-11
    - Return protocol contains `Checked=` and `Skipped=` format (D-17, D-18)
    - No occurrences of "Check all 28" or "all 28" (D-19)
    - grep for "service" in check descriptions returns 0 matches (excluding "external services" in Dependencies context which is neutral)
  </acceptance_criteria>
  <done>Complete spec-verifier agent definition with 27 checks, schema-parameterized input contract, tiered findings output, conditional section re-evaluation, and technology-neutral language throughout. All 27 CONTEXT.md decisions applied.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate IMPL-SPEC spec-verifier section as transferred</name>
  <files>docs/IMPL-SPEC.md</files>
  <read_first>
    - docs/IMPL-SPEC.md lines 342-345 (spec-verifier section header area)
  </read_first>
  <action>
Per D-27: Add "Transferred to agents/spec-verifier.md" annotation to the IMPL-SPEC spec-verifier section header. The section header is at approximately line 343: `## Agent: spec-verifier`.

Change the line immediately after the section header (the blockquote starting with "> **Phase 13 deliverable.**") to include the transfer note. Replace the blockquote content with:

```markdown
> **Transferred to `agents/spec-verifier.md`.** Content below retained as reference until Phase 14 deletion. See agents/spec-verifier.md for the authoritative agent definition.
```

Do NOT delete the IMPL-SPEC section content -- retain it as reference per D-27. Only update the annotation text.
  </action>
  <verify>
    <automated>grep "Transferred to" docs/IMPL-SPEC.md && grep "agents/spec-verifier.md" docs/IMPL-SPEC.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - docs/IMPL-SPEC.md contains "Transferred to" followed by "agents/spec-verifier.md"
    - The original section content (input contract table, return protocol, quality gate) is still present below the annotation
    - No other sections of IMPL-SPEC are modified
  </acceptance_criteria>
  <done>IMPL-SPEC spec-verifier section annotated as transferred. Original content retained as reference.</done>
</task>

</tasks>

<verification>
1. `agents/spec-verifier.md` exists with correct frontmatter (opus, Read+Grep+Glob, no maxTurns)
2. All 27 checks present in the agent (V-01 through V-29, minus V-23)
3. V-10 universalized, V-11 and V-15 conditionalized, V-20/V-21 promoted to T2
4. `schema_data` and `phase_context_file` in input contract
5. No service-biased language in check descriptions
6. IMPL-SPEC annotated with transfer note
</verification>

<success_criteria>
- The spec-verifier agent definition is complete and could be dispatched by the /consolidate orchestrator
- Every check from the reconciled inventory (RESEARCH.md) is present with correct tier assignment
- All 27 CONTEXT.md decisions are reflected in the agent
- IMPL-SPEC marks the section as transferred
</success_criteria>

<output>
After completion, create `.planning/phases/13-verification/13-01-SUMMARY.md`
</output>
