---
phase: 09-universal-model-design
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - docs/examples/schema-microservice.md
  - docs/examples/schema-cli.md
  - docs/examples/schema-library.md
autonomous: false
requirements:
  - MODEL-01
  - MODEL-03
  - MODEL-05
must_haves:
  truths:
    - "Each starter schema example declares components freely without selecting from predefined archetypes"
    - "All 7 mandatory sections produce meaningful content in the microservice example"
    - "All 7 mandatory sections produce meaningful content in the CLI tool example"
    - "All 7 mandatory sections produce meaningful content in the library example"
    - "Conditional sections are appropriately included or excluded per project type"
    - "Each example follows the schema format specified in MODEL.md exactly"
  artifacts:
    - path: "docs/examples/schema-microservice.md"
      provides: "Starter schema for backend service projects"
      contains: "# Consolidation Schema"
      min_lines: 30
    - path: "docs/examples/schema-cli.md"
      provides: "Starter schema for CLI tool projects"
      contains: "# Consolidation Schema"
      min_lines: 30
    - path: "docs/examples/schema-library.md"
      provides: "Starter schema for library projects"
      contains: "# Consolidation Schema"
      min_lines: 30
  key_links:
    - from: "docs/examples/schema-microservice.md"
      to: "docs/MODEL.md ## Schema Format"
      via: "Instantiates the schema format from MODEL.md"
      pattern: "## Meta"
    - from: "docs/examples/schema-cli.md"
      to: "docs/MODEL.md ## Default Section Structure"
      via: "Uses the 7+2 section list from MODEL.md"
      pattern: "## Sections: default"
    - from: "docs/examples/schema-library.md"
      to: "docs/MODEL.md ## Default Section Structure"
      via: "Uses the 7+2 section list from MODEL.md"
      pattern: "## Sections: default"
---

<objective>
Create 3 starter schema examples that demonstrate the universal model works for different project types. These examples serve as both reference material and neutrality test evidence -- each must show that all 7 mandatory sections produce meaningful content regardless of project type.

Purpose: Validates MODEL-05 (neutrality test) with concrete proof. Ships as reference for Phase 10 bootstrapping (SCHEMA-04). Demonstrates MODEL-01 (components declared freely).

Output: docs/examples/schema-microservice.md, docs/examples/schema-cli.md, docs/examples/schema-library.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-universal-model-design/09-CONTEXT.md
@.planning/phases/09-universal-model-design/09-RESEARCH.md
@.planning/phases/09-universal-model-design/09-01-SUMMARY.md
@docs/MODEL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 3 starter schema examples</name>
  <files>docs/examples/schema-microservice.md, docs/examples/schema-cli.md, docs/examples/schema-library.md</files>
  <read_first>
    - docs/MODEL.md (the authoritative spec -- examples must follow the schema format exactly as defined here)
    - .planning/phases/09-universal-model-design/09-CONTEXT.md (D-16: no per-component overrides; D-17: meta fields; D-20: 3 examples; D-29: deliverable list)
    - .planning/phases/09-universal-model-design/09-RESEARCH.md (Pattern 2: "Schema Examples as Validation" -- examples instantiate the model, not repeat it; Pitfall 3: write CLI and library FIRST; guide text specificity recommendation: use default guide text in all examples)
  </read_first>
  <action>
Create `docs/examples/` directory and 3 starter schema files. Write the CLI and library examples FIRST (per research Pitfall 3 -- these are the real neutrality test). Then write the microservice example.

**IMPORTANT:** Use the EXACT default guide text from MODEL.md `## Default Section Structure` for all 7 mandatory sections and 2 conditional sections. Do NOT adapt guide text per project type (per research recommendation: the default is descriptive enough to be interpreted per project type).

**All 3 examples follow identical structure** (the schema format from MODEL.md `## Schema Format`):

```markdown
# Consolidation Schema

A component is the smallest independently specifiable unit in your project.

## Meta

| Key | Value |
|-----|-------|
| version | 1 |
| rule-prefix | CR |
| e2e-flows | {true for microservice, false for cli and library} |

## Components

| Component | Description |
|-----------|-------------|
| {name} | {description} |
...

## Sections: default

### Context Sections
1. **Overview** -- What this component does and why it exists
2. **Public Interface** -- Operations, commands, endpoints, or API surface this component exposes to consumers
3. **Domain Model** -- Entities, types, and data structures this component owns
4. **Behavior Rules** -- Business rules, constraints, and invariants governing this component's behavior
5. **Error Handling** -- Error categories, failure modes, and recovery strategies
6. **Dependencies** -- What this component requires from other components or external systems
7. **Configuration** -- Environment variables, feature flags, and tunable parameters

### Conditional Sections
- **State Lifecycle** -- Include when: component manages stateful entities with lifecycle transitions
- **Event Contracts** -- Include when: component produces or consumes events/messages
```

**Example 1: schema-cli.md** (write FIRST)
- Meta: e2e-flows = false (CLI tools typically don't have cross-component communication flows)
- Components (3-4 realistic CLI components, e.g.):
  - init | Project scaffolding and configuration initialization
  - run | Core command execution and pipeline orchestration
  - config | User configuration management and persistence
  - output | Output formatting, colors, and display rendering
- Add a brief comment at top (below the title): `<!-- Example: CLI application (e.g., build tool, dev CLI, code generator) -->`

**Example 2: schema-library.md** (write SECOND)
- Meta: e2e-flows = false (libraries are consumed, not orchestrated)
- Components (3-4 realistic library components, e.g.):
  - parser | Input parsing and AST construction
  - transformer | AST transformation and optimization passes
  - emitter | Output code generation and serialization
  - plugin | Plugin discovery, loading, and lifecycle management
- Add a brief comment at top: `<!-- Example: Reusable library (e.g., compiler library, data processing SDK) -->`

**Example 3: schema-microservice.md** (write LAST)
- Meta: e2e-flows = true (microservices typically have cross-component flows)
- Components (3-4 realistic service components, e.g.):
  - auth | Authentication, session management, and token lifecycle
  - user | User profiles, account settings, and preferences
  - notification | Message delivery across email, push, and in-app channels
  - billing | Subscription management, payment processing, and invoicing
- Add a brief comment at top: `<!-- Example: Microservice backend (e.g., SaaS platform, API service) -->`

**Critical checks before finishing:**
- All 3 files use IDENTICAL section structure (same 7+2 sections, same guide text)
- The ONLY differences between files are: the comment line, e2e-flows value, and the Components table entries
- No file contains "archetype", "template_type", "SR-N", "Service Rule"
- Component descriptions are concise (one line) and clearly convey what the component does
  </action>
  <verify>
    <automated>test -f docs/examples/schema-microservice.md && test -f docs/examples/schema-cli.md && test -f docs/examples/schema-library.md && grep -c "e2e-flows" docs/examples/schema-microservice.md | xargs test 1 -eq && grep -c "e2e-flows" docs/examples/schema-cli.md | xargs test 1 -eq</automated>
  </verify>
  <acceptance_criteria>
    - docs/examples/schema-microservice.md exists, starts with `# Consolidation Schema`, contains `e2e-flows | true`
    - docs/examples/schema-cli.md exists, starts with `# Consolidation Schema`, contains `e2e-flows | false`
    - docs/examples/schema-library.md exists, starts with `# Consolidation Schema`, contains `e2e-flows | false`
    - All 3 files contain exactly 7 numbered context sections (1. Overview through 7. Configuration)
    - All 3 files contain 2 conditional sections (State Lifecycle, Event Contracts)
    - All 3 files contain `## Meta` table with version, rule-prefix, e2e-flows
    - All 3 files contain `## Components` table with at least 3 components each
    - All 3 files use identical guide text for sections (no project-type adaptation)
    - Zero occurrences of "archetype", "template_type", "SR-N", "Service Rule" across all 3 files
    - schema-microservice.md Components table contains service-oriented names (e.g., auth, user, notification)
    - schema-cli.md Components table contains CLI-oriented names (e.g., init, run, config)
    - schema-library.md Components table contains library-oriented names (e.g., parser, transformer, emitter)
  </acceptance_criteria>
  <done>3 starter schema examples created. Each follows MODEL.md schema format exactly. Each declares 3-4 realistic components for its project type. All use identical 7+2 section structure with default guide text. Microservice has e2e-flows=true; CLI and library have e2e-flows=false.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify model specification and examples</name>
  <files>docs/MODEL.md, docs/examples/schema-microservice.md, docs/examples/schema-cli.md, docs/examples/schema-library.md</files>
  <what-built>
Complete universal consolidation model specification (docs/MODEL.md) and 3 starter schema examples (docs/examples/).

MODEL.md defines: terminology, 7+2 default section structure, rule prefix system (GR/CR/OR/PR/TR), cases format, schema file format with parsing rules, component discovery algorithm, conditional section evaluation mechanism, 11 merge rules in v2 terminology, bootstrapping behavior, and output structure.

Examples demonstrate the schema format for microservice, CLI tool, and library project types.
  </what-built>
  <action>
Present the deliverables for human review. Run the v1 terminology audit command to confirm zero leaks. Summarize the review checklist for the user.

Automated pre-check before presenting to user:
1. Run: `grep -rinE "SR-N|Service Rule|archetype|template_type|SvcR|domain-service|gateway-bff" docs/MODEL.md docs/examples/` -- expect zero matches
2. Run: `grep -c "^## " docs/MODEL.md` -- expect at least 8 H2 sections
3. Run: `wc -l docs/MODEL.md` -- expect 250+ lines
4. Count components in each example: `grep -c "^|" docs/examples/schema-*.md` -- expect 5+ lines per file (header + separator + 3+ data rows)

Present results and the how-to-verify checklist to the user.
  </action>
  <how-to-verify>
1. Read `docs/MODEL.md` -- confirm it reads as a formal specification, not narrative. Key checks:
   - Section definitions in `## Default Section Structure` -- are the 7 mandatory sections meaningful for YOUR projects?
   - Rule system in `## Rule System` -- does CR-N (Component Rule) feel right as the replacement for SR-N?
   - Schema format in `## Schema Format` -- could you author a schema for a real project from this spec?
   - Merge rules in `## Merge Rules` -- are all 11 rules clear and v1-compatible?
   - Component discovery in `## Component Discovery` -- does the 2-stage algorithm make sense?

2. Read `docs/examples/schema-cli.md` -- does every mandatory section produce meaningful content for a CLI tool? Or do any feel forced?

3. Read `docs/examples/schema-library.md` -- same check for a library project.

4. Read `docs/examples/schema-microservice.md` -- sanity check for a service project (this one should be easy).

5. Terminology audit: search for "service" (outside component names), "SR-", "archetype" in MODEL.md. Should be zero hits.
  </how-to-verify>
  <verify>
    <automated>grep -rincE "SR-N|Service Rule|archetype|template_type" docs/MODEL.md docs/examples/ | grep -v ":0$" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <done>Human reviewer has approved MODEL.md as a complete, implementable specification and confirmed all 3 examples pass the neutrality test (no forced or empty sections for any project type).</done>
  <resume-signal>Type "approved" to complete Phase 9, or describe issues to address</resume-signal>
</task>

</tasks>

<verification>
After all tasks complete:

1. **MODEL-01 (declare units without archetypes):**
   - MODEL.md `## Terminology`: states no predefined archetypes
   - All 3 examples: Components table has free-form names, no type column constraints

2. **MODEL-02 (custom section structures per type):**
   - MODEL.md `## Default Section Structure`: documents that all components use 7+2 (D-16), per-component overrides deferred
   - MODEL.md `## Schema Format`: documents where overrides would go when implemented

3. **MODEL-03 (default sections for any project type):**
   - MODEL.md lists all 7 mandatory sections with descriptive guide text
   - 3 examples show the sections applied to different project types

4. **MODEL-04 (meta configuration):**
   - MODEL.md `## Schema Format` > `### Meta Fields`: 3 fields (version, rule-prefix, e2e-flows)

5. **MODEL-05 (neutrality test):**
   - 3 examples serve as concrete proof
   - Checkpoint (Task 2) gets human confirmation
</verification>

<success_criteria>
- 3 starter schema examples exist in docs/examples/
- Each example declares 3-4 realistic components for its project type
- All examples use identical section structure (7+2) with default guide text
- Human reviewer confirms sections are meaningful for non-service project types (CLI, library)
- Human reviewer approves MODEL.md as a complete, implementable specification
</success_criteria>

<output>
After completion, create `.planning/phases/09-universal-model-design/09-02-SUMMARY.md`
</output>
