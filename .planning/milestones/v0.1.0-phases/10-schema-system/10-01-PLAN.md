---
phase: 10-schema-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MODEL.md
  - docs/examples/schema-microservice.md
  - docs/examples/schema-cli.md
  - docs/examples/schema-library.md
  - tools/schema-bootstrap.ts
  - tools/schema-bootstrap_test.ts
autonomous: true
requirements:
  - SCHEMA-01
  - SCHEMA-04

must_haves:
  truths:
    - "MODEL.md documents section override syntax with Sections: {type-name} blocks and Type column in Components table"
    - "All 3 example schemas have a Type column in their Components table"
    - "Microservice example has at least one component with a custom type and a matching Sections: {type} block"
    - "Bootstrap tool creates a valid starter schema at the specified path"
    - "Bootstrap tool refuses to overwrite an existing file (exit 1)"
    - "Bootstrap output includes Meta defaults, empty Components table with Type column, and full 7+2 Sections: default block"
  artifacts:
    - path: "docs/MODEL.md"
      provides: "Section override specification"
      contains: "## Sections:"
    - path: "docs/examples/schema-microservice.md"
      provides: "Microservice example with section override"
      contains: "| Type |"
    - path: "docs/examples/schema-cli.md"
      provides: "CLI example with Type column"
      contains: "| Type |"
    - path: "docs/examples/schema-library.md"
      provides: "Library example with Type column"
      contains: "| Type |"
    - path: "tools/schema-bootstrap.ts"
      provides: "Schema bootstrap CLI tool"
      exports: ["generateStarterSchema", "main entrypoint via import.meta.main"]
    - path: "tools/schema-bootstrap_test.ts"
      provides: "Bootstrap tool tests"
      min_lines: 40
  key_links:
    - from: "tools/schema-bootstrap.ts"
      to: "Deno.writeTextFile"
      via: "createNew: true option for atomic no-overwrite"
      pattern: "createNew:\\s*true"
    - from: "docs/MODEL.md"
      to: "docs/examples/"
      via: "cross-reference to examples"
      pattern: "docs/examples/"
---

<objective>
Update MODEL.md with section override syntax, update all 3 schema examples with Type column (microservice gets a section override block), and create the bootstrap tool that generates starter schemas.

Purpose: Establish the documentation and fixtures that the schema parser (Plan 02) will parse and test against, plus the bootstrap tool for new projects.
Output: Updated MODEL.md, updated 3 example files, schema-bootstrap.ts with tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-schema-system/10-CONTEXT.md
@.planning/phases/10-schema-system/10-RESEARCH.md

<interfaces>
<!-- Reference pattern from existing tool: hash-sections.ts -->
<!-- Bootstrap tool must follow this exact CLI pattern -->

From tools/hash-sections.ts:
```typescript
import { unified } from "npm:unified@^11.0.0";
import remarkParse from "npm:remark-parse@^11.0.0";
import { toString } from "npm:mdast-util-to-string@^4.0.0";
import type { Heading, Root } from "npm:@types/mdast@^4.0.0";

// Export core functions for testability
export function normalize(text: string): string { ... }
export async function hashSections(source: string): Promise<Section[]> { ... }
export async function hashFile(path: string): Promise<FileResult> { ... }

// CLI entrypoint behind import.meta.main guard
if (import.meta.main) {
  if (Deno.args.length === 0) {
    console.error("Usage: hash-sections.ts <file1.md> [file2.md ...]");
    Deno.exit(1);
  }
  // ... process args, JSON to stdout, errors to stderr
}
```

From tools/hash-sections_test.ts:
```typescript
import { assertEquals, assertNotEquals, assertMatch } from "jsr:@std/assert";
import { normalize, hashSections, hashFile } from "./hash-sections.ts";

const FIXTURES = new URL("./tests/fixtures/", import.meta.url).pathname;

Deno.test("basic H2 extraction", async () => { ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MODEL.md with section override specification</name>
  <files>docs/MODEL.md</files>
  <read_first>
    - docs/MODEL.md (current state -- 398 lines, has "Per-component section overrides are deferred" language)
    - .planning/phases/10-schema-system/10-CONTEXT.md (decisions D-01 through D-05, D-20, D-21)
  </read_first>
  <action>
Update docs/MODEL.md to add the section override mechanism. Specific changes:

**1. Replace deferred language (per D-21):**
Find the sentence "Per-component section overrides are deferred; implement the 7+2 default first." in the "Default Section Structure" section. Replace it with:

"Components use the default section structure unless a type-specific override is declared in the schema. See [Section Overrides](#section-overrides) for the override mechanism."

**2. Add Section Overrides section** (new H2 section, place it after "Default Section Structure" and before "Rule System"):

```markdown
## Section Overrides

Type-based section overrides allow groups of components to share a custom section structure. Overrides are declared in the schema file using named section blocks.

### Override Syntax

The schema file supports multiple section blocks:

- `## Sections: default` -- applies to components with no Type or an empty Type column
- `## Sections: {type-name}` -- applies to components whose Type column matches `{type-name}`

Each section block contains `### Context Sections` (numbered list) and optionally `### Conditional Sections` (bullet list). An override completely replaces the default -- both mandatory and conditional sections. If an override block omits `### Conditional Sections`, components of that type have no conditional sections.

### Type Names

Type names must be kebab-case: lowercase ASCII letters, digits, and hyphens. Examples: `api-service`, `worker`, `data-store`. Validated by the schema parser.

Regex: `^[a-z][a-z0-9]*(-[a-z0-9]+)*$`

### Components Table Type Column

The `## Components` table includes a `Type` column that maps each component to its section block:

| Component | Description | Type |
|-----------|-------------|------|
| auth | Authentication and session management | api-service |
| worker | Background job processor | worker |
| config | Configuration management | |

- Components with an empty Type column use `## Sections: default`.
- Every Type value in the Components table must have a corresponding `## Sections: {type}` block in the schema.

### Override Example

```markdown
## Sections: default

### Context Sections
1. **Overview** -- What this component does and why it exists
2. **Public Interface** -- Operations, commands, endpoints, or API surface
...

## Sections: worker

### Context Sections
1. **Overview** -- What this worker does and why it exists
2. **Job Interface** -- Job types, payloads, and scheduling triggers
3. **Processing Rules** -- Retry policies, timeout handling, and idempotency guarantees
4. **Error Handling** -- Dead letter handling, failure escalation, and alerting
5. **Dependencies** -- Queues, databases, and external services this worker requires
```

In this example, `worker`-typed components get 5 custom sections with no conditional sections. All other components use the 7+2 default.
```

**3. Update Schema Format section:**
In the existing "Schema Structure" code block, change the `## Components` table from 2-column to 3-column:

```markdown
## Components

| Component | Description | Type |
|-----------|-------------|------|
| auth | Authentication and session management | |
| user | User profile and account operations | |
```

**4. Update Parsing Rules:**
Add these new parsing rules after the existing rule 7:

```markdown
8. **Section override blocks:** Headings matching `## Sections: {name}` (where `{name}` is not "default") define type-specific section overrides. The content structure is identical to `## Sections: default`. The `{name}` must match a Type value in the Components table.

9. **Type column in Components table:** When present, the third column maps components to section blocks. Empty values mean "use default". Every non-empty Type value must have a corresponding `## Sections: {type}` block.

10. **Type name validation:** Type names must match the kebab-case pattern `^[a-z][a-z0-9]*(-[a-z0-9]+)*$`. The parser rejects non-conforming type names with an error.
```

**5. Update INDEX.md Format:**
The INDEX.md table already has a Type column -- verify it is present. If the existing table shows `| Component | Type | Description |`, leave it. Confirm the note about Type being optional is present.

Do NOT change any other sections (Rule System, Cases Format, Merge Rules, etc.). The override mechanism is additive.
  </action>
  <verify>
    <automated>grep -c "Section Overrides" docs/MODEL.md | grep -q "^[1-9]" && grep -c "Sections: {type-name}" docs/MODEL.md | grep -q "^[1-9]" && ! grep -q "Per-component section overrides are deferred" docs/MODEL.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/MODEL.md contains `## Section Overrides` as an H2 heading
    - docs/MODEL.md contains `## Sections: {type-name}` in the override syntax explanation
    - docs/MODEL.md does NOT contain "Per-component section overrides are deferred"
    - docs/MODEL.md Components table in Schema Structure shows 3 columns: Component, Description, Type
    - docs/MODEL.md Parsing Rules section includes rules 8, 9, and 10 covering override blocks, Type column, and kebab-case validation
    - docs/MODEL.md contains the kebab-case regex `^[a-z][a-z0-9]*(-[a-z0-9]+)*$`
  </acceptance_criteria>
  <done>MODEL.md documents section override syntax, Type column, parsing rules 8-10, and kebab-case constraint. No deferred language remains.</done>
</task>

<task type="auto">
  <name>Task 2: Update 3 schema examples with Type column and microservice override</name>
  <files>docs/examples/schema-microservice.md, docs/examples/schema-cli.md, docs/examples/schema-library.md</files>
  <read_first>
    - docs/examples/schema-microservice.md (current: 2-column Components table, no Type, no override block)
    - docs/examples/schema-cli.md (current: 2-column Components table)
    - docs/examples/schema-library.md (current: 2-column Components table)
    - .planning/phases/10-schema-system/10-CONTEXT.md (D-17, D-18, D-19)
    - docs/MODEL.md (after Task 1 updates -- for override syntax reference)
  </read_first>
  <action>
Update all 3 example files. These files also serve as test fixtures for schema-parser_test.ts (Plan 02).

**schema-cli.md -- Add Type column, default types only (per D-18):**
Replace the Components table with:

```markdown
## Components

| Component | Description | Type |
|-----------|-------------|------|
| init | Project scaffolding and configuration initialization | |
| run | Core command execution and pipeline orchestration | |
| config | User configuration management and persistence | |
| output | Output formatting, colors, and display rendering | |
```

No other changes. Keep the existing `## Sections: default` block as-is.

**schema-library.md -- Add Type column, default types only (per D-18):**
Replace the Components table with:

```markdown
## Components

| Component | Description | Type |
|-----------|-------------|------|
| parser | Input parsing and AST construction | |
| transformer | AST transformation and optimization passes | |
| emitter | Output code generation and serialization | |
| plugin | Plugin discovery, loading, and lifecycle management | |
```

No other changes. Keep the existing `## Sections: default` block as-is.

**schema-microservice.md -- Add Type column AND section override (per D-18):**
Replace the Components table with (at least one component has a custom type):

```markdown
## Components

| Component | Description | Type |
|-----------|-------------|------|
| auth | Authentication, session management, and token lifecycle | api-gateway |
| user | User profiles, account settings, and preferences | |
| notification | Message delivery across email, push, and in-app channels | |
| billing | Subscription management, payment processing, and invoicing | |
```

After the existing `## Sections: default` block, add a new section override block:

```markdown
## Sections: api-gateway

### Context Sections
1. **Overview** -- What this gateway component does and why it exists
2. **Public Interface** -- Routes, middleware chains, and API surface exposed to external consumers
3. **Authentication** -- Token validation, session management, and identity resolution
4. **Rate Limiting** -- Throttling rules, quota management, and abuse prevention
5. **Error Handling** -- Error response formats, status code mapping, and client-facing error contracts
6. **Dependencies** -- Upstream services, identity providers, and external integrations
7. **Configuration** -- Environment variables, feature flags, and tunable parameters
```

Note: This override has 7 context sections (different from the default) and NO conditional sections (per D-02, omitting `### Conditional Sections` means the type has no conditional sections). This demonstrates a real-world override where a gateway component needs different section topics than a generic component.
  </action>
  <verify>
    <automated>grep -c "| Type |" docs/examples/schema-cli.md docs/examples/schema-library.md docs/examples/schema-microservice.md | grep -v ":0$" | wc -l | tr -d ' ' | grep -q "^3$" && grep -c "## Sections: api-gateway" docs/examples/schema-microservice.md | grep -q "^1$" && grep -c "| api-gateway |" docs/examples/schema-microservice.md | grep -q "^1$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - docs/examples/schema-cli.md Components table has 3 columns: Component, Description, Type
    - docs/examples/schema-library.md Components table has 3 columns: Component, Description, Type
    - docs/examples/schema-microservice.md Components table has 3 columns: Component, Description, Type
    - docs/examples/schema-microservice.md contains `## Sections: api-gateway` heading
    - docs/examples/schema-microservice.md contains at least one row with `| api-gateway |` in the Type column
    - docs/examples/schema-microservice.md `## Sections: api-gateway` block has numbered Context Sections and no `### Conditional Sections`
    - docs/examples/schema-cli.md has NO `## Sections:` blocks other than `## Sections: default`
    - docs/examples/schema-library.md has NO `## Sections:` blocks other than `## Sections: default`
    - All 3 files still contain `## Sections: default` with the full 7+2 structure unchanged
  </acceptance_criteria>
  <done>All 3 examples have Type column. Microservice example demonstrates a complete section override. CLI and library use default only. Files are ready to serve as parser test fixtures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create schema-bootstrap.ts tool and tests</name>
  <files>tools/schema-bootstrap.ts, tools/schema-bootstrap_test.ts</files>
  <read_first>
    - tools/hash-sections.ts (reference Deno CLI tool pattern: import.meta.main guard, export core functions, CLI with JSON stdout + stderr errors)
    - tools/hash-sections_test.ts (reference test pattern: jsr:@std/assert, Deno.test, fixture-based)
    - .planning/phases/10-schema-system/10-CONTEXT.md (D-06 through D-10)
    - .planning/phases/10-schema-system/10-RESEARCH.md (Pattern 5: Atomic No-Overwrite, Bootstrap Template Generation)
  </read_first>
  <behavior>
    - Test 1: generateStarterSchema() returns a string containing `# Consolidation Schema`
    - Test 2: generateStarterSchema() output contains `## Meta` table with version=1, rule-prefix=CR, e2e-flows=false
    - Test 3: generateStarterSchema() output contains `## Components` table with 3 columns (Component, Description, Type) and no data rows
    - Test 4: generateStarterSchema() output contains `## Sections: default` with all 7 context sections and 2 conditional sections
    - Test 5: generateStarterSchema() output contains a comment referencing docs/examples/
    - Test 6: writeSchema() creates a file at the specified path with starter schema content
    - Test 7: writeSchema() throws/returns error when file already exists (refuses to overwrite)
    - Test 8: CLI exits 1 with usage message when no args provided
  </behavior>
  <action>
**schema-bootstrap.ts:**

Create `tools/schema-bootstrap.ts` following the hash-sections.ts CLI pattern. The tool generates a starter schema file.

**Exported function:**
```typescript
export function generateStarterSchema(): string
```
Returns the complete starter schema as a string. Content (per D-07):

```
# Consolidation Schema

A component is the smallest independently specifiable unit in your project.

<!-- See docs/examples/ for complete schema examples -->

## Meta

| Key | Value |
|-----|-------|
| version | 1 |
| rule-prefix | CR |
| e2e-flows | false |

## Components

| Component | Description | Type |
|-----------|-------------|------|

## Sections: default

### Context Sections
1. **Overview** -- What this component does and why it exists
2. **Public Interface** -- Operations, commands, endpoints, or API surface this component exposes to consumers
3. **Domain Model** -- Entities, types, and data structures this component owns
4. **Behavior Rules** -- Business rules, constraints, and invariants governing this component's behavior
5. **Error Handling** -- Error categories, failure modes, and recovery strategies
6. **Dependencies** -- What this component requires from other components or external systems
7. **Configuration** -- Environment variables, feature flags, and tunable parameters

### Conditional Sections
- **State Lifecycle** -- Include when: component manages stateful entities with lifecycle transitions
- **Event Contracts** -- Include when: component produces or consumes events/messages
```

**Exported async function:**
```typescript
export async function writeSchema(outputPath: string): Promise<void>
```
Writes the starter schema to `outputPath`. Uses `Deno.writeTextFile(outputPath, content, { createNew: true })` for atomic no-overwrite. On `Deno.errors.AlreadyExists`, throw a descriptive error or let it propagate.

**CLI entrypoint** (behind `import.meta.main` guard per D-08):
- No args: `console.error("Usage: schema-bootstrap.ts <output-path>")` then `Deno.exit(1)`
- With arg: call `writeSchema(Deno.args[0])`, print `Created: {path}` on success
- On `AlreadyExists`: `console.error("Error: {path} already exists. Will not overwrite.")` then `Deno.exit(1)`

**No npm dependencies needed.** Bootstrap generates a fixed string and writes a file. No AST parsing, no unified, no remark.

**schema-bootstrap_test.ts:**

Use `jsr:@std/assert` and import from `./schema-bootstrap.ts`. Use a temp directory (`Deno.makeTempDir()`) for file write tests. Clean up temp files after each test.

Test cases:
1. `generateStarterSchema()` returns string containing `# Consolidation Schema`
2. Output contains `| version | 1 |`
3. Output contains `| rule-prefix | CR |`
4. Output contains `| e2e-flows | false |`
5. Output contains `| Component | Description | Type |` (3-column Components table)
6. Output contains `## Sections: default`
7. Output contains all 7 mandatory section names (Overview, Public Interface, Domain Model, Behavior Rules, Error Handling, Dependencies, Configuration)
8. Output contains both conditional sections (State Lifecycle, Event Contracts)
9. Output contains `docs/examples/` reference comment
10. `writeSchema()` creates file successfully in temp dir, verify content matches `generateStarterSchema()`
11. `writeSchema()` throws `Deno.errors.AlreadyExists` when file exists (write a file first, then call writeSchema on same path)
  </action>
  <verify>
    <automated>deno test tools/schema-bootstrap_test.ts --allow-read --allow-write -x</automated>
  </verify>
  <acceptance_criteria>
    - tools/schema-bootstrap.ts exists and exports `generateStarterSchema` and `writeSchema`
    - tools/schema-bootstrap_test.ts exists with at least 8 test cases
    - `deno test tools/schema-bootstrap_test.ts --allow-read --allow-write` exits 0
    - tools/schema-bootstrap.ts contains `import.meta.main` guard
    - tools/schema-bootstrap.ts contains `createNew: true` in writeTextFile call
    - generateStarterSchema() output contains `| Component | Description | Type |` (3-column table per D-07)
    - generateStarterSchema() output contains `<!-- See docs/examples/` comment
    - generateStarterSchema() output contains `## Sections: default` with 7 numbered items and 2 bullet items
  </acceptance_criteria>
  <done>Bootstrap tool generates correct starter schema with Meta defaults, empty 3-column Components table, and full 7+2 Sections: default. Refuses to overwrite existing files. All tests pass.</done>
</task>

</tasks>

<verification>
After all 3 tasks:
1. `grep -c "Section Overrides" docs/MODEL.md` returns >= 1
2. `grep -c "| Type |" docs/examples/schema-microservice.md` returns >= 1
3. `grep -c "## Sections: api-gateway" docs/examples/schema-microservice.md` returns 1
4. `deno test tools/schema-bootstrap_test.ts --allow-read --allow-write` exits 0
5. `deno run --allow-read --allow-write tools/schema-bootstrap.ts /tmp/test-schema.md && cat /tmp/test-schema.md | head -5` shows `# Consolidation Schema`
</verification>

<success_criteria>
- MODEL.md fully documents the section override mechanism with no deferred language
- All 3 example files have Type column; microservice has a working override block
- Bootstrap tool creates valid starter schemas and refuses to overwrite existing files
- All bootstrap tests pass
- Examples are ready to serve as test fixtures for the schema parser (Plan 02)
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema-system/10-01-SUMMARY.md`
</output>
