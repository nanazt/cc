---
phase: 12-case-updates
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - skills/case/SKILL.md
  - skills/case/step-discuss.md
  - skills/case/step-finalize.md
  - agents/case-briefer.md
  - agents/case-validator.md
autonomous: true
requirements:
  - CASE-03
  - CASE-04
  - CASE-05
  - CASE-07
  - CASE-08

must_haves:
  truths:
    - "step-discuss.md contains TR classification interaction at Step 2.5 and mid-discussion"
    - "step-finalize.md contains finalize TR review pass at step 4g (skipped when no TR rules exist)"
    - "step-finalize.md contains Superseded Operations and Superseded Rules conditional sections in CASES.md output format"
    - "case-briefer reads specs/{component}/cases.md first for current phase components, falling back silently to phase directories"
    - "case-validator recognizes Superseded Operations and Superseded Rules as valid CASES.md sections"
    - "CASES.md output format includes TR-N rules in Phase Rules section (distinguished by prefix only, no subsections)"
  artifacts:
    - path: "skills/case/step-discuss.md"
      provides: "TR classification at Step 2.5 and mid-discussion; supersession inline detection"
      contains: "TR-"
    - path: "skills/case/step-finalize.md"
      provides: "TR review pass, Supersession Review step, updated CASES.md output format"
      contains: "Superseded Operations"
    - path: "agents/case-briefer.md"
      provides: "specs/ priority lookup substep, existing spec state output"
      contains: "specs/"
    - path: "agents/case-validator.md"
      provides: "Superseded Operations and Superseded Rules section recognition"
      contains: "Superseded"
  key_links:
    - from: "skills/case/step-discuss.md"
      to: "skills/case/step-finalize.md"
      via: "TR-classified rules flow from discussion to finalize TR review pass"
      pattern: "TR review|TR-classified"
    - from: "skills/case/step-discuss.md"
      to: "skills/case/step-finalize.md"
      via: "Supersession signals detected inline flow to finalize Supersession Review"
      pattern: "Supersession Review|restructuring"
    - from: "agents/case-briefer.md"
      to: "skills/case/step-init.md"
      via: "Briefer output includes Existing Spec State consumed by orchestrator"
      pattern: "Existing Spec State|specs/"
---

<objective>
Add three new features to the /case ecosystem: (1) PR/TR rule classification during discussion, (2) supersession metadata support in CASES.md output, and (3) specs/ priority lookup for the case-briefer. Also complete case-validator v2 acceptance with section recognition.

Purpose: Enable /case to classify rules as permanent vs temporary (CASE-03), track operation/rule supersession across phases (CASE-04, CASE-05), give the briefer access to consolidated spec history (CASE-08), and make the validator aware of all v2 sections (CASE-07 complete).

Output: 5 updated files with TR classification flow, supersession detection/review, specs/ lookup, and validator section recognition. All new features follow existing CB (Configuration Behaviors) pattern for consistency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-case-updates/12-CONTEXT.md
@.planning/phases/12-case-updates/12-RESEARCH.md
@.planning/phases/12-case-updates/12-01-SUMMARY.md
</context>

<interfaces>
<!-- Patterns from IMPL-SPEC.md that define the supersession table contracts -->
<!-- The CASES.md output must produce tables matching these formats for downstream consumption -->

Superseded Operations table format (from IMPL-SPEC spec-consolidator input contract):
| Old Operation | Replacement | Reason |
Types: Renamed, Split, Merged, Moved, Removed

Superseded Rules table format (from IMPL-SPEC spec-consolidator input contract):
| Phase | Rule ID | Reason |

<!-- Existing CB pattern in step-discuss.md (3c-vii) and step-finalize.md (4d) is the template -->
<!-- TR classification and supersession detection both follow: inline detect -> capture -> finalize review -> conditional section -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add TR classification and supersession detection to step-discuss.md and SKILL.md</name>
  <files>skills/case/step-discuss.md, skills/case/SKILL.md</files>

  <read_first>
    skills/case/step-discuss.md
    skills/case/step-finalize.md
    skills/case/SKILL.md
    .planning/phases/12-case-updates/12-CONTEXT.md
    .planning/phases/12-case-updates/12-RESEARCH.md
    docs/IMPL-SPEC.md
  </read_first>

  <action>
**step-discuss.md: TR classification at Step 2.5 (per D-01, D-02, D-03, D-04)**

After the existing Phase Rules confirmation block in Step 2.5 (after "Confirm these phase rules? Any to add, remove, or adjust?"), add a new paragraph for TR classification:

```markdown
After presenting Phase Rules for confirmation, review each rule's content for temporary signals. If a rule describes behavior that is explicitly temporary, migration-only, or development-only (e.g., "use mock SMTP during development", "maintain v1 compatibility until migration completes"), propose TR classification:

```
This rule sounds temporary: PR-3 "[rule text]"
Reclassify as TR-3? TR rules are excluded from consolidated specs.
```

Developer can confirm (reclassifies to TR-3) or reject (stays as PR-3). All rules default to PR (permanent) -- TR is the exception. The developer can also initiate TR classification directly ("this is temporary").
```

**step-discuss.md: TR classification mid-discussion (per D-04)**

In step 3e (Save to scratch file), after the existing Rules example block that shows `OR-1:` rules and `Inherits:` line, update the format to note that TR-classified rules are saved with their TR- prefix:

Add to the Rules subsection of the scratch format: rules discovered during per-operation discussion that the developer classifies as temporary use TR-N prefix instead of OR-N.

Also add a brief note near the mid-discussion PR promotion block (around the "That sounds like a phase-wide rule" section). After that existing block, add a parallel block for TR:

```markdown
**Mid-discussion TR classification:** When a new rule is discovered during per-operation probing and appears temporary, propose TR classification:
```
This sounds temporary -- classify as TR-N instead of OR-N? TR rules are excluded from consolidated specs.
```
Developer confirms (becomes TR-N) or rejects (stays OR-N). Same interaction pattern as PR promotion above.
```

**step-discuss.md: Supersession inline detection (per D-06)**

Add a new section 3c-ix after 3c-viii (Infrastructure failures), following the same pattern as 3c-vii (Configuration-dependent behaviors):

```markdown
**3c-ix: Supersession detection**

During per-operation discussion, watch for restructuring signals: an operation being renamed, split into multiple operations, merged from multiple operations, moved to a different component, or removed entirely. Also watch for rules being replaced by new rules.

When detected, capture internally (do not surface mid-discussion -- same as CB inline detection in 3c-vii). These are consolidated during finalize (Step 4f) as a Supersession Review.

Restructuring signals to watch for:
- "This replaces [old operation]" or "We renamed X to Y"
- "We split X into Y and Z" or "We merged X and Y into Z"
- "This moved from [component A] to [component B]"
- "X is no longer needed" or "We removed X"
- "PR-2 replaces the old PR-1" or "This rule supersedes [old rule]"
```

**SKILL.md: Add TR to success criteria (minor)**

In the `<success_criteria>` section, the existing line about "Configuration behaviors (CB) captured" is a peer item. No change needed here -- TR and supersession are captured through the same discussion flow. However, update the line in `<formatting>` that lists rule tier conventions to include TR (this should already be done in Plan 01; verify and confirm).

Also in the SKILL.md `<success_criteria>` section, add after the CB line:
```markdown
- TR-classified rules (if any) confirmed by developer during discussion and finalize review
- Supersession metadata captured for any restructured operations or replaced rules
```
  </action>

  <verify>
    <automated>cd /Users/syr/Developments/cckit && echo "=== TR in step-discuss ===" && grep -c 'TR-' skills/case/step-discuss.md && echo "=== Supersession in step-discuss ===" && grep -c 'supersession\|restructuring\|Supersed' skills/case/step-discuss.md && echo "=== TR in SKILL.md criteria ===" && grep -c 'TR-classified' skills/case/SKILL.md && echo "=== Supersession in SKILL.md criteria ===" && grep -c 'Supersession\|supersession' skills/case/SKILL.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'TR-3\|TR-N\|TR classification' skills/case/step-discuss.md` returns at least 3 matches (Step 2.5 proposal, mid-discussion proposal, classification description)
    - `grep -n 'Supersession\|supersession\|restructuring signals' skills/case/step-discuss.md` returns at least 2 matches (3c-ix section)
    - `grep -n 'TR-classified' skills/case/SKILL.md` returns at least 1 match (success criteria)
    - `grep -n 'Supersession metadata' skills/case/SKILL.md` returns at least 1 match (success criteria)
    - step-discuss.md contains text "TR rules are excluded from consolidated specs"
    - step-discuss.md section 3c-ix exists with heading containing "Supersession" or "supersession"
    - step-discuss.md Step 2.5 contains TR proposal example with "Reclassify as TR-"
    - The TR classification follows the same "Claude proposes, developer confirms" pattern as PR promotion
    - Supersession detection follows the same "capture internally, consolidate at finalize" pattern as CB in 3c-vii
  </acceptance_criteria>

  <done>step-discuss.md has TR classification at Step 2.5 and mid-discussion, plus supersession inline detection in 3c-ix. SKILL.md success criteria mention TR and supersession. All follow existing CB pattern -- no new interaction paradigms.</done>
</task>

<task type="auto">
  <name>Task 2: Add TR review pass, Supersession Review, and updated CASES.md output format to step-finalize.md</name>
  <files>skills/case/step-finalize.md</files>

  <read_first>
    skills/case/step-finalize.md
    skills/case/step-discuss.md
    .planning/phases/12-case-updates/12-CONTEXT.md
    .planning/phases/12-case-updates/12-RESEARCH.md
    docs/IMPL-SPEC.md
  </read_first>

  <action>
**New Step 4f: Supersession Review (per D-06, D-07, placed after 4e Forward concerns)**

Add between existing Step 4e and Step 5 (Validate):

```markdown
### 4f: Supersession review

If restructuring was detected during per-operation discussion (operations renamed, split, merged, moved, or removed; rules replaced by newer rules), present a supersession summary for confirmation:

```
I detected these structural changes:

Superseded Operations:
| Old Operation | Replacement | Reason |
|---|---|---|
| Component.OldOp | Component.NewOp | Renamed |

Superseded Rules:
| Phase | Rule ID | Reason |
|---|---|---|
| 8 | PR-2 | Replaced by PR-1 in this phase |

Confirm these supersession entries? Any to add, remove, or adjust?
```

Reason types for Superseded Operations: Renamed, Split, Merged, Moved, Removed.

If no restructuring was detected during discussion, skip this step silently (same conditional pattern as CB in 4d -- omit when empty).
```

**New Step 4g: TR review pass (per D-04, D-05, placed after 4f)**

Add after the new Step 4f:

```markdown
### 4g: TR review pass

If any rules were classified as TR (temporary) during Phase Rules confirmation (Step 2.5) or per-operation discussion, present them all for final confirmation:

```
These rules are classified as temporary (TR) and will be excluded from consolidated specs:

- TR-1: [rule text] (from Phase Rules)
- TR-2: [rule text] (from Operation.Name)

Confirm TR classification? Any to reclassify back to PR/OR?
```

If no TR rules exist, skip this step silently. This is a safety net -- the developer may want to reclassify rules they marked TR during discussion now that they see the full picture.
```

**Updated CASES.md output format in `<output_format>` section (per D-22, D-23, D-08):**

In the output format template, make these specific changes:

1. Phase Rules section -- update to show TR alongside PR (per D-22, list structure maintained):

Replace the existing Phase Rules block:
```markdown
## Phase Rules

> Constraints that apply to ALL operations in this phase.
> Referenced by ID (PR-1, PR-2...) in operation Rules sections.
> TR rules are temporary and excluded from consolidated specs.

- PR-1: [constraint description] (D-XX)
- PR-2: [constraint description] (D-XX, D-YY)
- TR-1: [temporary constraint] (D-XX) -- excluded from specs
- PR-3: [constraint description]. Per GR-1: [global rule it derives from]
```

2. Add two new conditional sections AFTER Global Rules, BEFORE the first `## Operation:` section (per D-23):

```markdown
## Superseded Operations

> Operations from previous phases that this phase restructures.
> Omit this section if no operations were superseded.

| Old Operation | Replacement | Reason |
|---|---|---|
| Component.OldName | Component.NewName | Renamed |
| Component.SplitOp | Component.PartA, Component.PartB | Split |

---

## Superseded Rules

> Rules from previous phases that this phase replaces.
> Omit this section if no rules were superseded.

| Phase | Rule ID | Reason |
|---|---|---|
| 8 | PR-2 | Replaced by PR-1 in this phase |
| 5 | OR-3 | Operation removed |
```

3. Rename `**System Rules (from PROJECT.md) applicable to this phase:**` to `**Global Rules (from PROJECT.md) applicable to this phase:**` (this should already be done by Plan 01; verify).

4. Add conditional omission notes (per D-08) -- add `Omit this section if no operations/rules were superseded.` as blockquote inside each supersession section (shown above).

**Step 6 summary update:**

In the Step 6 pre-write summary block, add TR count and supersession count:

```markdown
Ready to write CASES.md:

[Operation 1]: S:[n] F:[n] E:[n] Q:[n] -- [ready/needs-answers]
[Operation 2]: S:[n] F:[n] E:[n] Q:[n] -- [ready/needs-answers]
Configuration Behaviors: [n] items
TR Rules: [n] items
Superseded Operations: [n] | Superseded Rules: [n]

Total: [N] success, [M] failure, [K] edge, [P] questions, [C] config behaviors, [T] TR rules across [Q] operations.
```
  </action>

  <verify>
    <automated>cd /Users/syr/Developments/cckit && echo "=== 4f Supersession Review ===" && grep -n '4f.*[Ss]upersession\|Supersession [Rr]eview\|Superseded Operations\|Superseded Rules' skills/case/step-finalize.md && echo "=== 4g TR review ===" && grep -n '4g.*TR\|TR review' skills/case/step-finalize.md && echo "=== Output format supersession ===" && grep -n 'Omit this section if no.*supersed' skills/case/step-finalize.md && echo "=== TR in Phase Rules ===" && grep -n 'TR-1\|excluded from specs' skills/case/step-finalize.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n '### 4f' skills/case/step-finalize.md` returns at least 1 match (Supersession Review step exists)
    - `grep -n '### 4g' skills/case/step-finalize.md` returns at least 1 match (TR review pass step exists)
    - `grep -n 'Superseded Operations' skills/case/step-finalize.md` returns at least 2 matches (one in 4f review, one in output format)
    - `grep -n 'Superseded Rules' skills/case/step-finalize.md` returns at least 2 matches (one in 4f review, one in output format)
    - `grep -n 'TR-1' skills/case/step-finalize.md` returns at least 1 match (in Phase Rules output format)
    - `grep -n 'excluded from specs' skills/case/step-finalize.md` returns at least 1 match
    - `grep -n 'Omit this section if no' skills/case/step-finalize.md` returns at least 2 matches (superseded ops + superseded rules conditional omission)
    - `grep -n 'Old Operation.*Replacement.*Reason' skills/case/step-finalize.md` returns at least 1 match (table header in output format)
    - `grep -n 'Phase.*Rule ID.*Reason' skills/case/step-finalize.md` returns at least 1 match (table header in output format)
    - Step 4f appears after 4e and before Step 5
    - Step 4g appears after 4f and before Step 5
    - Both 4f and 4g have "skip silently" conditional (same as 4d CB pattern)
    - Step 6 summary includes TR Rules and Superseded counts
  </acceptance_criteria>

  <done>step-finalize.md has Supersession Review (4f) and TR review pass (4g) as new finalize steps. CASES.md output format includes TR-N in Phase Rules section, Superseded Operations table, and Superseded Rules table -- all conditional (omitted when empty). Step 6 summary includes TR and supersession counts.</done>
</task>

<task type="auto">
  <name>Task 3: Add specs/ priority lookup to case-briefer and complete case-validator v2 acceptance</name>
  <files>agents/case-briefer.md, agents/case-validator.md</files>

  <read_first>
    agents/case-briefer.md
    agents/case-validator.md
    .planning/phases/12-case-updates/12-CONTEXT.md
    .planning/phases/12-case-updates/12-RESEARCH.md
  </read_first>

  <action>
**case-briefer.md: specs/ priority lookup (per D-13, D-14, D-15, D-16)**

Add a new Step 1.5 between existing Step 1 (Understand phase scope) and Step 2 (Discover operations):

```markdown
### Step 1.5: Check consolidated specs (current phase only)

For each component referenced in this phase's CONTEXT.md:
1. Check if `specs/{component}/cases.md` exists using Glob
2. If exists: read it and note existing operations, their rules and case counts
3. If not exists: proceed with phase directories only (no warning, no fallback message)

This lookup applies ONLY to the phase being discussed. Do NOT read specs/ for dependency phases -- those are handled by Step 4.7 (Inherited Concerns).

When a spec exists:
- Include an "Existing Spec State" subsection in the briefing for that component
- Note: "Operation {Component.Op} exists in spec with {N} rules, {M} cases"
- If CONTEXT.md has decisions affecting an operation that already exists in the spec, flag: "Operation exists in spec but current phase has new decisions affecting it"
- Do NOT merge spec content with phase content -- they are distinct sources (spec = historical consolidated state; phase CONTEXT.md = current phase changes)
```

Update the output contract to include the new Existing Spec State section. After the `## Architectural Context` section and before the category groups, add:

```markdown
## Existing Spec State

> For components in this phase that have consolidated specs.
> Omit this section if no specs/ exist for any component in this phase.

### [ComponentName]

- **Spec location:** `specs/{component}/cases.md`
- **Existing operations:** [count]
  - {Component.Op1}: {N} rules, {M} cases
  - {Component.Op2}: {N} rules, {M} cases
- **Operations with new decisions in this phase:** [list or "none"]
```

Update the Quality Gate checklist to add:
```markdown
- [ ] specs/{component}/cases.md checked for each component in this phase (silent skip if absent)
- [ ] Existing Spec State section included when specs exist (omitted when none exist)
- [ ] No spec content merged with phase content (distinct sources)
```

**TR boundary enforcement in case-briefer (per D-26):**

In Step 4.5 (Classify cross-cutting constraints), add an explicit constraint after the classification list:

```markdown
**TR exclusion:** This agent does NOT classify rules as TR (temporary). TR judgment is the Protester's (orchestrator's) responsibility during discussion. Classify constraints as GR-candidate, PR-candidate, or operation-specific (OR) only.
```

**case-validator.md: Section recognition (per D-17, D-18, D-19)**

Add to the validator's methodology, in a note after the five checks or as a preamble to Check A:

```markdown
### Valid CASES.md Sections

The validator recognizes these as valid CASES.md sections (present or absent):
- Phase Rules (with PR-N and TR-N entries)
- Global Rules (with GR-N entries)
- Superseded Operations (conditional -- valid when present, no gap when absent)
- Superseded Rules (conditional -- valid when present, no gap when absent)
- Operation sections with OR-N rules
- GR Candidates (renamed from SR Candidates)
- Configuration Behaviors
- Forward Concerns
- Cross-Operation Concerns

**Valid rule prefix format:** `(GR|CR|OR|PR|TR)-\d+` (all prefixes use dash + number, no padding).
```

Update Check B (Decision and Constraint Coverage) coverage scope section to explicitly include Superseded Operations and Superseded Rules:

```markdown
A decision is covered if it appears in ANY of these locations in CASE-SCRATCH.md:
- Phase Rules (PR/TR section)
- Operation Rules (OR section)
- Side Effects
- Case table Expected Outcome
- Superseded Operations table
- Superseded Rules table
```

Update Check E (Briefing Coverage) to note that briefed operations appearing only in the Superseded Operations table (as "Old Operation") should not be flagged as missing -- they were intentionally superseded.
  </action>

  <verify>
    <automated>cd /Users/syr/Developments/cckit && echo "=== Briefer specs/ lookup ===" && grep -n 'specs/.*cases.md\|Step 1.5\|Existing Spec State' agents/case-briefer.md && echo "=== Briefer TR exclusion ===" && grep -n 'TR exclusion\|does NOT classify.*TR\|Protester' agents/case-briefer.md && echo "=== Validator sections ===" && grep -n 'Superseded Operations\|Superseded Rules\|TR-.*valid\|Valid.*prefix' agents/case-validator.md</automated>
  </verify>

  <acceptance_criteria>
    - `grep -n 'Step 1.5' agents/case-briefer.md` returns at least 1 match
    - `grep -n 'specs/{component}/cases.md' agents/case-briefer.md` returns at least 1 match (could be slightly different quoting, but the path pattern must appear)
    - `grep -n 'Existing Spec State' agents/case-briefer.md` returns at least 2 matches (methodology + output contract)
    - `grep -n 'does NOT classify.*TR\|TR exclusion' agents/case-briefer.md` returns at least 1 match
    - `grep -n 'ONLY.*phase being discussed\|Do NOT read specs.*dependency' agents/case-briefer.md` returns at least 1 match (scope limitation per D-13)
    - `grep -n 'Superseded Operations' agents/case-validator.md` returns at least 1 match
    - `grep -n 'Superseded Rules' agents/case-validator.md` returns at least 1 match
    - `grep -n 'GR|CR|OR|PR|TR' agents/case-validator.md` returns at least 1 match (unified prefix regex)
    - case-briefer.md quality gate includes specs/ check item
    - case-validator.md coverage scope includes Superseded tables
  </acceptance_criteria>

  <done>case-briefer has Step 1.5 specs/ lookup scoped to current phase only, with Existing Spec State output section and TR exclusion constraint. case-validator recognizes Superseded Operations/Rules as valid sections, uses v2 prefix regex, and includes supersession tables in coverage scope.</done>
</task>

</tasks>

<verification>
After all tasks complete, verify the full feature set:

```bash
# TR classification exists in step-discuss
grep -c 'TR-' skills/case/step-discuss.md
# Expected: >= 3

# Supersession exists in step-discuss and step-finalize
grep -c 'Supersed' skills/case/step-discuss.md skills/case/step-finalize.md
# Expected: >= 2 in each file

# specs/ lookup in briefer
grep -c 'specs/' agents/case-briefer.md
# Expected: >= 3

# Validator knows about supersession sections
grep -c 'Supersed' agents/case-validator.md
# Expected: >= 2

# CASES.md output format has both supersession tables
grep -c 'Old Operation.*Replacement.*Reason' skills/case/step-finalize.md
grep -c 'Phase.*Rule ID.*Reason' skills/case/step-finalize.md
# Expected: >= 1 each

# Comprehensive old-format audit (should still be clean from Plan 01)
grep -rn 'SR-0[0-9]\|SR Candidates\|System Rules\|service topology\|cross-service\|{Service}\|GR-XX' \
  skills/case/ agents/case-briefer.md agents/case-validator.md
# Expected: zero matches
```
</verification>

<success_criteria>
- CASE-03 satisfied: step-discuss.md has TR classification at Step 2.5, mid-discussion, and finalize TR review; step-finalize.md has TR review pass (4g)
- CASE-04 satisfied: step-finalize.md CASES.md output format includes Superseded Operations table (conditional)
- CASE-05 satisfied: step-finalize.md CASES.md output format includes Superseded Rules table (conditional)
- CASE-07 complete: case-validator accepts TR-N and OR-N prefixes AND recognizes Superseded Operations/Rules as valid sections
- CASE-08 satisfied: case-briefer Step 1.5 reads specs/{component}/cases.md first, silent fallback when absent
- All new features follow existing CB pattern (inline detect -> capture -> finalize review -> conditional section)
- No regression in Plan 01 vocabulary migration (old-format audit still clean)
</success_criteria>

<output>
After completion, create `.planning/phases/12-case-updates/12-02-SUMMARY.md`
</output>
