---
phase: 16-e2e-dispatch-alignment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/consolidate/SKILL.md
autonomous: true
requirements:
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - FLOW-04
must_haves:
  truths:
    - "SKILL.md Step 4 dispatch table lists exactly 7 tags matching the e2e-flows.md Input Contract"
    - "The phantom phase_id tag does not appear in Step 4"
    - "Step 4 uses the same Tag/Required/Contents table format as Steps 2 and 5"
    - "Required/Optional marking for each tag matches the agent Input Contract exactly"
  artifacts:
    - path: "skills/consolidate/SKILL.md"
      provides: "Corrected Step 4 dispatch table for e2e-flows agent"
      contains: "specs_dir"
  key_links:
    - from: "skills/consolidate/SKILL.md"
      to: "agents/e2e-flows.md"
      via: "Step 4 dispatch tags match Input Contract"
      pattern: "<objective>.*<changed_components>.*<spec_hashes>"
---

<objective>
Rewrite SKILL.md Step 4's inline parenthetical tag list as a 7-row dispatch table that exactly matches the e2e-flows.md agent Input Contract. Remove the phantom `<phase_id>` tag. After this change, all three dispatch steps (2, 4, 5) in SKILL.md will have self-contained tag tables in identical format.

Purpose: Close INT-01 integration gap -- the e2e-flows dispatch contract in SKILL.md currently lists 4 tags (with 1 phantom), while the agent expects 7. This mismatch means any orchestrator following SKILL.md Step 4 literally would send an incomplete dispatch to the e2e-flows agent.

Output: Updated `skills/consolidate/SKILL.md` with correct Step 4 dispatch table.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-e2e-dispatch-alignment/16-CONTEXT.md
@.planning/phases/16-e2e-dispatch-alignment/16-RESEARCH.md

@skills/consolidate/SKILL.md (the file being modified -- Step 4 is the rewrite target)
@agents/e2e-flows.md (source of truth for the 7-tag set -- Input Contract lines 21-31)

<interfaces>
<!-- Source of truth: e2e-flows.md Input Contract (lines 21-31) -->
<!-- The SKILL.md Step 4 dispatch table MUST list exactly these 7 tags -->

| Tag | Required | How Agent Uses It |
|-----|----------|-------------------|
| `<objective>` | Yes | Action description: "Generate/update E2E flow documentation for changed components." |
| `<changed_components>` | Yes | JSON manifest of components updated this consolidation run |
| `<spec_hashes>` | Yes | JSON from hash-sections.ts for hash comparison |
| `<existing_flows>` | No | JSON array of existing flow file paths |
| `<new_flows>` | No | JSON array of developer-confirmed new flow names |
| `<project_file>` | Yes | Path to PROJECT.md for component topology reference |
| `<specs_dir>` | Yes | Path to `specs/` directory root |

<!-- Format reference: Step 2 dispatch table (SKILL.md lines 108-124) -->
<!-- Format reference: Step 5 dispatch table (SKILL.md lines 209-222) -->
<!-- Both use: | Tag | Required | Contents | -->

<!-- PHANTOM TAG: <phase_id> is in Step 2 (for spec-consolidator) but does NOT belong in Step 4 -->
<!-- Root cause: executor copied it from Step 2 during v2 rewrite (commit 0a6b5c9) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SKILL.md Step 4 dispatch table</name>
  <files>skills/consolidate/SKILL.md</files>
  <read_first>
    - skills/consolidate/SKILL.md (full file -- understand current Step 4 structure at lines 191-205, and reference Step 2 table format at lines 108-124 and Step 5 table format at lines 209-222)
    - agents/e2e-flows.md (lines 1-31 -- Input Contract is the source of truth for the 7-tag set)
  </read_first>
  <action>
Edit `skills/consolidate/SKILL.md` Step 4 (currently lines 191-205). The step header, gating condition, Deno verification (items 1-3), dispatch instruction (item 5), parse return (item 6), and error handling block all remain unchanged. Only item 4 (line 201) changes.

**Current line 201:**
```
4. Build dispatch prompt with XML tags for the e2e-flows agent (include `<existing_flows>`, `<new_flows>`, `<spec_hashes>`, `<phase_id>`).
```

**Replace with item 4 containing an introductory sentence followed by a 7-row dispatch table:**

```
4. Build dispatch prompt containing these XML tags:

   | Tag | Required | Contents |
   |-----|----------|----------|
   | `<objective>` | Yes | "Generate/update E2E flow documentation for changed components." |
   | `<changed_components>` | Yes | JSON manifest from consolidator results (Step 3) |
   | `<spec_hashes>` | Yes | JSON output from hash-sections.ts (computed in sub-step 2 above) |
   | `<existing_flows>` | No | JSON array of existing flow file paths from `specs/e2e/` (Step 3.5). Omit if none exist. |
   | `<new_flows>` | No | JSON array of developer-confirmed new flow names (Step 3.5). Omit if none confirmed. |
   | `<project_file>` | Yes | Path to PROJECT.md |
   | `<specs_dir>` | Yes | Path to `specs/` directory root |
```

Key constraints:
- Exactly 7 tags. NOT 8 (no `<phase_id>`). NOT 6 (don't drop any).
- `<existing_flows>` and `<new_flows>` are the only Optional (No) tags. The other 5 are Required (Yes).
- The Contents column describes what the orchestrator puts in the tag, using the same style as Steps 2 and 5. References to pipeline steps (Step 3, Step 3.5, sub-step 2) help the orchestrator locate the data.
- Do NOT add a cross-reference note to the agent file (Steps 2 and 5 don't have one; consistency).
- After the table, items 5 and 6 continue unchanged (renumber if needed to maintain the 1-6 list).
- The error handling block after the numbered list stays exactly as-is.
  </action>
  <verify>
    <automated>grep -c "phase_id" skills/consolidate/SKILL.md | grep -q "^1$" && grep -c "objective.*Yes\|changed_components.*Yes\|spec_hashes.*Yes\|existing_flows.*No\|new_flows.*No\|project_file.*Yes\|specs_dir.*Yes" skills/consolidate/SKILL.md | grep -q "^7$" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - SKILL.md Step 4 contains a `| Tag | Required | Contents |` table header
    - The table has exactly 7 data rows (one per tag)
    - Tag names in the table are: `<objective>`, `<changed_components>`, `<spec_hashes>`, `<existing_flows>`, `<new_flows>`, `<project_file>`, `<specs_dir>`
    - `<phase_id>` does NOT appear anywhere in Step 4 (grep for "phase_id" in the Step 4 section returns 0 hits)
    - `<phase_id>` still appears exactly once in Step 2 (line 122 area -- unchanged, legitimate for spec-consolidator)
    - Required column: `<existing_flows>` and `<new_flows>` are "No"; all others are "Yes"
    - The numbered list (1-6) in Step 4 is intact and correctly ordered
    - Lines before Step 4 (Steps 1-3.7) and after Step 4 (Steps 5-7) are unchanged
    - `deno test` passes (regression check -- no code files changed but confirms nothing is broken)
  </acceptance_criteria>
  <done>SKILL.md Step 4 has a 7-row dispatch table matching e2e-flows.md Input Contract exactly. Phantom phase_id removed. Format matches Steps 2 and 5. All three dispatch steps are now self-contained tag tables.</done>
</task>

</tasks>

<verification>
1. **Tag set match:** Extract tag names from SKILL.md Step 4 table rows. Compare against e2e-flows.md Input Contract tag names. Sets must be identical: {objective, changed_components, spec_hashes, existing_flows, new_flows, project_file, specs_dir}.
2. **No phantom:** `grep "phase_id" skills/consolidate/SKILL.md` returns exactly 1 hit (in Step 2, not Step 4).
3. **Format consistency:** Step 4 uses `| Tag | Required | Contents |` header -- same as Steps 2 and 5.
4. **Required/Optional:** `existing_flows` and `new_flows` are "No"; the other 5 are "Yes".
5. **No regressions:** `deno test` passes.
6. **Structural integrity:** Steps 1-3.7 and Steps 5-7 unchanged (diff shows changes only in the Step 4 section).
</verification>

<success_criteria>
SKILL.md Step 4 dispatch table matches e2e-flows.md Input Contract tag-for-tag. The phantom `<phase_id>` is removed from Step 4. All three dispatch steps (2, 4, 5) now have self-contained `| Tag | Required | Contents |` tables. `deno test` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/16-e2e-dispatch-alignment/16-01-SUMMARY.md`
</output>
