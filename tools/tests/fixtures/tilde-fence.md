## Tilde Test

This section contains a tilde-fenced code block with a fake heading inside.

~~~markdown
## Another Fake

This is inside a tilde-fenced code block and should not create a section boundary.
~~~

This paragraph is still inside Tilde Test, after the tilde-fenced block.
